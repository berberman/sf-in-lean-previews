window.docContents[187].resolve({"/Tactics/#Logical-Foundations--Tactics___-More-Basic-Tactics--Rewriting-with-Conditional-Statements":{"contents":"Suppose that we know two numbers have the same double, and we want to\nuse this fact to rewrite one of them into the other. Recall the theorem\ndouble_injective from the previous section:#check double_injective\ndouble_injective (n m : Nat) (h : Nat.double n = Nat.double m) : n = m\nFor example, we can prove:\n\nexample (n m p q : Nat)\n    (h : n.double = m.double)\n    (hm : m + p = q) :\n    n + p = q := by\n  rw [double_injective n m]\n  · assumption\n  · assumption\n\n\nThe use of rw here is a little different from the examples we have seen so far.\nThe theorem double_injective says n = m\nprovided that n.double = m.double, no just n = m.\nWhen we write rw [double_injective n m], Lean uses the conclusion n = m to rewrite\nthe goal, and then asks us to prove the hypothesis needed by double_injective.\nThus we get two goals: the updated main goal, m + p = q, which follows from hm, and the\ncondition from double_injective, n.double = m.double, whicch follows from h.\n\nIf we rewrite with a conditional statement of the form\nP → a = b, then Lean tries to rewrite with a = b, and then\nasks us to prove P in a new subgoal.  If the statement has more\nthan one assumption, then we get one subgoal for each assumption.\n\nProve this by induction on l.theorem nth?_after_last {α : Type}\n    {n : Nat} {l : List α} (h : l.length = n) :\n    nth? l n = none := by\n  sorry\n\n\nProve this by induction on l₁, without using List.length_append.theorem length_append_cons {α : Type} {l₁ l₂ : List α} {x : α} {n : Nat}\n    (h : (l₁ ++ (x :: l₂)).length = n) :\n    ((l₁ ++ l₂).length) + 1 = n := by\n  sorry\n\n\nProve this by induction on l₁, without using List.length_append.\nHint: you might need to use length_append_cons you just proved.theorem length_append_self {α : Type} {n : Nat} {l : List α}\n    (h : l.length = n) :\n    (l ++ l).length = n + n := by\n  induction l generalizing n with\n  | nil =>\n    rw [List.append_nil,  List.length_nil] at *\n    rw [← h]\n  | cons x xs ih =>\n    rw [List.cons_append, List.length_cons] at *\n    rw [← length_append_cons (x := x) rfl]\n    rw [ih (by rfl), ← h]\n    rw [Nat.add_add_add_comm]\n\n\nProve the following principle of induction over two naturals.\ntheorem diagonal_induction (p : Nat → Nat → Prop)\n    (hzz : p 0 0)\n    (hsz : ∀ m, p m 0 → p (m + 1) 0)\n    (hzs : ∀ n, p 0 n → p 0 (n + 1))\n    (hss : ∀ m n, p m n → p (m + 1) (n + 1)) :\n    ∀ m n, p m n := by\n  intro m n\n  induction m generalizing n with\n  | zero =>\n    induction n with\n    | zero => apply hzz\n    | succ n' ih =>\n      apply hzs\n      apply ih\n  | succ m' ih =>\n    induction n with\n    | zero =>\n      apply hsz\n      apply ih\n    | succ n' ih' =>\n      apply hss\n      apply ih\n\n\n","context":"Logical Foundations\u0009Tactics: More Basic Tactics","header":"7.6. Rewriting with Conditional Statements","id":"/Tactics/#Logical-Foundations--Tactics___-More-Basic-Tactics--Rewriting-with-Conditional-Statements"}});