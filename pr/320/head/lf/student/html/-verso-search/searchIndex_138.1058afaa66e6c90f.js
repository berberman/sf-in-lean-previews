window.docContents[138].resolve({"/Typeclasses/#Logical-Foundations--Typeclasses--Using-Typeclasses":{"contents":"Let's check what == meant for List.elem_nat, with notation display turned off:\n\nset_option pp.notation false in\n#check 1 == 2\n\n\nBEq.beq 1 2 : Bool\n\n\nRather than Nat.beq, == turns out to be notation for BEq.beq, a field of exactly\nthe kind of typeclass we just learned to define:\n\n/--\n  `BEq α` is a typeclass for supplying a boolean-valued equality relation on\n  `α`, notated as `a == b`. Unlike `DecidableEq α` (which uses `a = b`), this\n  is `Bool` valued instead of `Prop` valued, and it also does not have any\n  axioms like being reflexive or agreeing with `=`. It is mainly intended for\n  programming applications. See `LawfulBEq` for a version that requires that\n  `==` and `=` coincide.\n\n  Typically we prefer to put the \"more variable\" term on the left,\n  and the \"more constant\" term on the right.\n  -/\n  class BEq (α : Type u) where\n    /-- Boolean equality, notated as `a == b`. -/\n    beq : α → α → Bool\n\n\nWriting x == y makes Lean search for an instance of BEq for the type of x and y, the same\nway it searched for a DefaultValue instance above. For Nat, that instance is:\n\n\n\ninstance (priority := low) : BEq Nat where\n  beq := Nat.beq\n\n\nThis is the instance Lean supplies for [BEq α] when List.elem_poly is called on a\nList Nat — no different from Lean choosing instDefaultValueNat for\nDefaultValue.value earlier when it was equated with (1 : Nat).\n\nProve that List.elem_poly agrees with List.elem_nat when specialized to\nnatural numbers.theorem List.elem_poly_eq_elem_nat (xs : List Nat) (n : Nat) : xs.elem_poly n = xs.elem_nat n := by\n  sorry\n\n\n","context":"Logical Foundations\u0009Typeclasses","header":"11.3. Using Typeclasses","id":"/Typeclasses/#Logical-Foundations--Typeclasses--Using-Typeclasses"}});