window.docContents[78].resolve({"/Logic/#Logical-Foundations--Logic-in-Lean--Logical-Connectives--Falsehood-and-Negation":{"contents":"Up to this point, we have mostly been concerned with proving\n\"positive\" statements -- addition is commutative, appending lists\nis associative, etc.  We are sometimes also interested in negative\nresults, demonstrating that some proposition is not true. Such\nstatements are expressed with the logical negation operator ¬,\nwhich a prefix notation for Not.\n\nTo see how negation works, recall the principle of explosion\nfrom the Tactics chapter, which asserts that, if we assume a\ncontradiction, then any other proposition can be derived.\n\nFollowing this intuition, we could define ¬ P (\"not P\") as\n∀ Q, P → Q.\nLean makes an equivalent but slightly different choice,\ndefining ~ P as P → False, where False is a specific\nunprovable proposition defined in the standard library.\n\n#check (Not : Prop → Prop)\n#print Not\n\nexample : ∀ P, Not P = (P → False) := by intro; rfl\nexample : ∀ P, (¬ P) = (P → False) := by intro; rfl\n\n\nSince False is a contradictory proposition, the principle of\nexplosion also applies to it. If we can get False into the context,\nwe can use cases on it to complete any goal:\n\ntheorem ex_falso_quodlibet (P : Prop) (h : False) : P := by\n  cases h\n\n\nThe Latin ex falso quodlibet means, literally, \"from falsehood\nfollows whatever you like\"; this is another common name for the\nprinciple of explosion.\n\ntheorem not_implies_other_not (P : Prop) (h : ¬ P) :\n    (∀ Q : Prop, P → Q) := by\n  sorry\n\n\nInequality is a very common form of negated statement, so there is a\nspecial notation for it: ≠, which is infix notation for Ne.\n\n#print Ne\n\ntheorem zero_not_one : 0 ≠ 1 := by\n  /- FULL: The proposition `0 ≠ 1` is exactly the same as `¬ (0 = 1)`\n      -- that is, `Not (0 = 1)` -- which unfolds to `(0 = 1) → False`. -/\n  /- FULL: To prove an inequality, we may assume the opposite equality... -/\n  intro contra\n  /- FULL: ...and deduce a contradiction from it. Here, the equality\n      `0 = 1` corresponds to `zero = succ zero`, which contradicts\n      disjointness of constructors `zero` and `succ`, so `contradiction`\n      takes care of it. -/\n  contradiction\n  -- JC: `cases contra` and `injection contra` both also work,\n  -- but is probably harder to explain.\n\n\nIt takes a little practice to get used to working with negation in Lean.\nEven though you may see perfectly well why a claim involving\nnegation holds, it can be a little tricky at first to see how to make\nLean understand it!\n\nHere are proofs of a few familiar facts to help get you warmed up.\n\ntheorem not_False : ¬ False := by\n  intro h; exact h\n\ntheorem contradiction_implies_anything (P Q : Prop) (h : P ∧ ¬ P) : Q := by\n  all_goals\n    let ⟨hP, hnP⟩ := h\n    apply hnP at hP; cases hP\n\ntheorem double_neg (P : Prop) (hP : P) : ¬ ¬ P := by\n  all_goals\n    intro h; apply h; exact hP\n\n\nWrite an informal proof of double_neg:\nTheorem: P implies ¬ ¬ P, for any proposition P.theorem contrapositive (P Q : Prop) (h : P → Q) : (¬ Q → ¬ P) := by\n  sorry\nWrite an informal proof of the proposition\n∀ P : Prop, ¬ (P ∧ ¬ P).De Morgan's Laws, named for Augustus De Morgan, describe how\nnegation interacts with conjunction and disjunction.  The\nfollowing law says that \"the negation of a disjunction is the\nconjunction of the negations.\" There is a dual law\nde_morgan_not_and_not to which we will return at the end of this\nchapter.theorem de_morgan_not_or (P Q : Prop) (h : ¬ (P ∨ Q)) : ¬ P ∧ ¬ Q := by\n  sorry\nSince we are working with natural numbers, we can disprove that\nsucc and pred are inverses of each other:theorem not_succ_pred_n : ¬ (∀ n : Nat, succ (pred n) = n) := by\n  sorry\n\n\n\n\nSince inequality involves a negation, it also requires a little\npractice to be able to work with it fluently. Here is one useful trick.If you are trying to prove a goal that is nonsensical (e.g., the\ngoal state is false = true), apply ex_falso_quodlibet to\nchange the goal to False.This makes it easier to use assumptions of the form ¬ P that may\nbe available in the context -- in particular, assumptions of the\nform x ≠ y.\n\ntheorem not_true_is_false (b : Bool) (h : b ≠ true) : b = false := by\n  -- FOLD\n  cases b\n  case false => rfl\n  case true =>\n    unfold Ne Not at h\n    apply ex_falso_quodlibet\n    apply h; rfl\n  -- /FOLD\n\n\nSince reasoning with ex_falso_quodlibet is quite common,\nLean provides a tactic, exfalso, for applying it.theorem not_true_is_false' (b : Bool) (h : b ≠ true) : b = false := by\n  cases b\n  case false => rfl\n  case true =>\n    unfold Ne Not at h\n    exfalso -- ⟵ here\n    apply h; rfl\n\n\nHIDE: CH: I don't think this was the original intention, but some\nof these quizzes got unnecessarily tricky and pedantic. For\ninstance, the first quiz below makes a big distinction between\nusing the destruct tactic and destructing using an intro pattern,\neven if conceptually there is no difference. Could it be that these\nquizzes were devised when intro patterns were not taught in the\ncourse and an update would be helpful now? Since I don't see the\ngain in tricking a majority of students in giving the \"wrong\"\nanswer, even if it's a perfectly sensible one.\n\nTo prove the following proposition, which tactics will we need\nbesides intro, apply, and exact?∀ X : Prop, ∀ a b : X, a = b ∧ a ≠ b → False\n1. cases, unfold, left, and right2. cases and unfold3. only cases4. left and/or right5. only unfold6. none of the above\n\nexample (X : Prop) (a b : X) : a = b ∧ a ≠ b → False := by\n  intro ⟨h, hn⟩; apply hn; exact h\n\n\nTo prove the following proposition, which tactics will we need\nbesides intro, apply, and exact?∀ P Q : Prop, P ∨ Q → ¬ ¬ (P ∨ Q)\n1. cases, unfold, left, and right2. cases and unfold3. only cases4. left and/or right5. only unfold6. none of the above\n\nexample (P Q : Prop) (h : P ∨ Q) : ¬ ¬ (P ∨ Q) := by\n  intro hn; apply hn; exact h\n\n\nTo prove the following proposition, which tactics will we need\nbesides intro, apply, and exact?∀ P Q : Prop, P → (P ∨ ¬ ¬ Q)\n1. cases, unfold, left, and right2. cases and unfold3. only cases4. left and/or right5. only unfold6. none of the above\n\nexample (P Q : Prop) (h : P) : P ∨ ¬ ¬ Q := by\n  left; exact h\n\n\nTo prove the following proposition, which tactics will we need\nbesides intro, apply, and exact?∀ P Q : Prop, P ∨ Q → (¬ ¬ P) ∨ (¬ ¬ Q)\n1. cases, unfold, left, and right2. cases and unfold3. only cases4. left and/or right5. only unfold6. none of the above\n\nexample (P Q : Prop) (h : P ∨ Q) : (¬ ¬ P) ∨ (¬ ¬ Q) := by\n  cases h\n  case inl hP => left; intro hnP; apply hnP; exact hP\n  case inr hQ => right; intro hnQ; apply hnQ; exact hQ\n\n\nTo prove the following proposition, which tactics will we need\nbesides intro, apply, and exact?∀ A : Prop, 1 = 0 → (A ∨ ¬ A)\n1. contradiction, unfold, left, and right2. contradiction and unfold3. only contradiction4. left and/or right5. only unfold6. none of the above\n\nexample (A : Prop) (h : 1 = 0) : (A ∨ ¬ A) := by\n  contradiction\n\n\n","context":"Logical Foundations\u0009Logic in Lean\u0009Logical Connectives","header":"8.2.3. Falsehood and Negation","id":"/Logic/#Logical-Foundations--Logic-in-Lean--Logical-Connectives--Falsehood-and-Negation"},"/Typeclasses/#Logical-Foundations--Typeclasses--Why-We-Need-Typeclasses":{"contents":"Consider the following function, which checks whether a natural number occurs in a list:\n\ndef List.elem_nat (a : Nat) (xs : List Nat) : Bool :=\n  match xs with\n  | [] => false\n  | b :: tl => bif a == b then true else elem_nat a tl\n\ntheorem List.elem_nat_nil (a : Nat) : [].elem_nat a = false := rfl\n\ntheorem List.elem_nat_cons (a b : Nat) (xs : List Nat) :\n    (b :: xs).elem_nat a = bif a == b then true else elem_nat a xs := rfl\n\n\n#eval [0, 1].elem_nat 0\n#eval [0, 1].elem_nat 1\n#eval [0, 1].elem_nat 2\n\n\nWhat if we want this to work for lists of any element type, not just Nat? Parametric\npolymorphism suggests simply replacing Nat with a type variable α, but that produces a puzzling\nerror:\n\n@dsainati - The Verso compilation is not actually removing this from the generated file despite th e\n-keep flag. It is, however, stripping the message guard, so this results in an error. Once\nwe figure out how we are handling these cases, uncomment this./--\nerror: failed to synthesize instance of type class\n  BEq α\n\nHint: Type class instance resolution failures can be inspected with the `set_option trace.Meta.synthInstance true` command.\n-/\n#guard_msgs in\ndef List.elem_poly {α : Type} (a : α) (xs : List α) : Bool :=\n  match xs with\n  | [] => false\n  | b :: tl => bif a == b then true else elem_poly a tl\n\n\nLean is trying to use typeclasses to work out how == should behave on a value of type α.\nWe'll see exactly why shortly; for now, here's one way to sidestep the problem: have the\ncaller supply the equality test to use.\n\ndef List.elem_poly_eq {α : Type} (eq : α → α → Bool) (a : α) (xs : List α) : Bool :=\n  match xs with\n  | [] => false\n  | b :: tl => bif eq a b then true else elem_poly_eq eq a tl\n\n#eval [0, 1].elem_poly_eq Nat.beq 0\n\n\nThis works, but it's tedious: every caller has to know, and remember to supply, the right equality\nfunction.\n\nTypeclasses automate this — instead of the programmer passing the function\nexplicitly, Lean searches for one and provides it on its own. We specify something we want\nLean to search for by declaring a class with the needed function as a field; a class is like\nan interface in Java or a trait in Rust. Particular implementations of that interface,\ndifferent ones for different types, are called instances. Finally, we add\nthe name of that class as a typeclass constraint on the polymorphic variable that the\nfunction applies to. This directs Lean to use the interface inside the function, and to\nfind and fill in the appropriate instance at call-sites to that function.\nHere is what this looks like for List.elem_poly:\n\ndef List.elem_poly {α : Type} [BEq α] (a : α) (xs : List α) : Bool :=\n  match xs with\n  | [] => false\n  | b :: tl => bif a == b then true else elem_poly a tl\n\ntheorem List.elem_poly_nil [BEq α] (a : α) : [].elem_poly a = false := rfl\n\ntheorem List.elem_poly_cons [BEq α] (a b : α) (xs : List α) :\n    (b :: xs).elem_poly a = bif a == b then true else elem_poly a xs := rfl\n\n#eval [0, 1].elem_poly 0\n\n\nAs Lists noted when we first used it, == on Nat comes from the BEq typeclass,\nand it's what List.elem_poly uses internally wherever it writes ==. This is done using\ninstance implicits, where we place a desired typeclass assumption in square bracket.\nThe [BEq α] constraint is saying that an instance of BEq must be provided at\ncall sites for the particular type α that is used. In the example [0, 1].elem_poly 0,\nthis type is Nat, and the automatically chosen instance corresponds to Nat.beq.\n\nThis is technically incorrect, the instance BEq Nat, which comes from DecidableEq, does not contain Nat.beq. You can see in proofs of List.elem_nat versus List.elem_poly_eq versus List.elem_poly and how Nat.beq and == play different roles.\n\nIn the earlier version of List.elem_poly, α was fully generic, with no typeclass\nconstraint — so the == in its body would have needed to work for every type α, and no\nsingle BEq instance can do that. That's why Lean's search failed.\n\nNow it is time to dig into the details of what we have seen so far.\nWe'll see exactly how [BEq α] gets filled in below, starting with how\nto define a typeclass in the first place.\n\n","context":"Logical Foundations\u0009Typeclasses","header":"11.1. Why We Need Typeclasses","id":"/Typeclasses/#Logical-Foundations--Typeclasses--Why-We-Need-Typeclasses"}});