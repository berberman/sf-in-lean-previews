window.docContents[230].resolve({"/Logic/#Logical-Foundations--Logic-in-Lean--Programming-with-Propositions":{"contents":"The logical connectives that we have seen provide a rich vocabulary\nfor defining complex propositions from simpler ones.\nTo illustrate, let's look at how to express the claim that an element x\noccurs in a list l.\nNotice that this property has a simple recursive structure:\n\n\n\nWe can translate this directly into a straightforward recursive function\ntaking an element and a list and returning... a proposition!\n\ndef List.In {α : Type} (x : α) (xs : List α) : Prop :=\n  match xs with\n  | [] => False\n  | x' :: xs' => x = x' ∨ In x xs'\n\ntheorem List.In_nil {α : Type} {x : α} : ¬ (List.In x []) := by\n  dsimp [List.In]; intro h; assumption\n\ntheorem List.In_cons {α : Type} {x x' : α} {xs : List α} : List.In x (x' :: xs) = (x = x' ∨ List.In x xs) := rfl\n\n\nWhen List.In is applied to a concrete list, it expands into a concrete sequence\nof nested disjunctions.\n\nexample : List.In 4 [1, 2, 3, 4, 5] := by\n  workinclass!\n    dsimp [List.In]; right; right; right; left; rfl\n\nexample (n : Nat) (h : List.In n [2, 4]) : ∃ n' : Nat, n = 2 * n' := by\n  workinclass!\n    dsimp [List.In] at h\n    obtain h | h | ⟨⟨⟩⟩ := h\n    · exists 1\n    · exists 2\n    /- (Notice the use of the empty pattern to discharge the last case.) -/\n\n\nWe can also reason about more generic statements involving List.In.\n\ntheorem List.In_map {α β : Type} {f : α → β} {xs : List α} {x : α} (h : In x xs) :\n    In (f x) (map f xs) := by\n  induction xs with\n  | nil =>\n    exfalso; apply In_nil; assumption\n  | cons x' xs' ih =>\n    rw [In_cons] at h\n    obtain h | h := h\n    · rw [h, map_cons, In_cons]; left; rfl\n    · rw [map_cons, In_cons]; right; exact ih h\n\n\nThis way of defining propositions recursively is very convenient in\nsome cases, less so in others.  In particular, it is subject to the\nusual restrictions regarding definitions of recursive functions,\ne.g., the requirement that they be \"obviously terminating.\"In the next chapter, we will see how to define propositions\ninductively — a different technique with its own strengths and\nlimitations.theorem List.In_map_iff {α β : Type} {f : α → β} {xs : List α} {y : β} :\n    In y (map f xs) ↔ ∃ x, f x = y ∧ In x xs := by\n  constructor\n  · solution!\n      induction xs with\n      | nil =>\n        intro h; rw [map_nil] at h\n        exfalso; apply List.In_nil; assumption\n      | cons x' xs' ih =>\n        intro h\n        rw [List.map_cons, In_cons] at h\n        obtain h | h := h\n        · rw [h]; exists x'\n          constructor\n          · rfl\n          · rw [In_cons]; left; rfl\n        · obtain ⟨x', h₁, h₂⟩ := ih h\n          exists x'\n          constructor\n          · exact h₁\n          · rw [In_cons]; right; exact h₂\n  · solution!\n      intro ⟨x, h₁, h₂⟩\n      rw [← h₁]; apply In_map; exact h₂\n\n\nWe noted above that functions returning propositions can be seen as\nproperties of their arguments. For instance, if p has type\nNat → Prop, then p n says that property p holds of n.Drawing inspiration from List.In, write a recursive function All\nstating that some property p holds of all elements of a list\nl. To make sure your definition is correct, prove the All_In\nlemma below.  (Of course, your definition should not just\nrestate the left-hand side of All_In.)def List.All {α : Type} (p : α → Prop) (l : List α) : Prop := solution!(\n  match l with\n  | [] => True\n  | x :: l' => p x ∧ List.All p l')\ntheorem List.All_nil {α : Type} {a : α → Prop} : List.All a [] := solution!(by constructor)\n\ntheorem List.All_cons {α : Type} {p : α → Prop} {x : α} {l : List α} :\n    List.All p (x :: l) = (p x ∧ All p l) := solution!(rfl)\n\ntheorem List.All_In {α : Type} {p : α → Prop} {l : List α} :\n    (∀ x : α, In x l → p x) ↔ All p l := by\n  solution!\n    induction l with\n    | nil =>\n      constructor\n      · intro _; exact All_nil\n      · intro _ _ h; apply In_nil at h; contradiction\n    | cons x' xs' ih =>\n      obtain ⟨ih₁, ih₂⟩ := ih\n      constructor\n      · intro h\n        rw [All_cons]\n        constructor\n        · apply h; rw [In_cons]; left; rfl\n        · apply ih₁\n          intro x' hx'; apply h\n          rw [In_cons]; right; exact hx'\n      · rw [All_cons]\n        intro ⟨hx, hp⟩ x' h\n        rw [In_cons] at h\n        obtain h₁ | h₂ := h\n        · rw [h₁]; exact hx\n        · apply ih₂; apply hp; exact h₂\nI found this exercise combining too many awkward details for too little conceptual payoff:1. the construction is artificial2. before simp is introduced, bif requires noisy dsimp, rw, and Boolean case equations3. I don't know how to nicely avoid cases h : ... syntax which IIRC we didn't mention beforeComplete the definition of CombineOddEven below. It takes as arguments\ntwo properties of numbers, Odd and Even, and it should return\na predicate p such that p n is equivalent to Odd n when n is odd\nand equivalent to Even n otherwise.def CombineOddEven (Odd Even : Nat → Prop) : Nat → Prop := solution!(\n  fun n => bif Nat.odd n then Odd n else Even n)\nTo test your definition, prove the following facts:theorem combineOddEven_intro (Odd Even : Nat → Prop)\n    (n : Nat)\n    (hOdd : Nat.odd n = true → Odd n)\n    (hEven : Nat.odd n = false → Even n) :\n    CombineOddEven Odd Even n := by\n  solution!\n    dsimp [CombineOddEven]\n    /- `cases h : Nat.odd n` splits on `Nat.odd n` and records\n      the corresponding equation as `h`. -/\n    cases h : Nat.odd n with\n    | false =>\n      apply hEven\n      rw [h]\n    | true =>\n      dsimp\n      apply hOdd\n      exact h\n\ntheorem combineOddEven_elim_odd\n    (Odd Even : Nat → Prop)\n    (n : Nat)\n    (h : CombineOddEven Odd Even n)\n    (hOdd : Nat.odd n = true) : Odd n := by\n  solution!\n    dsimp [CombineOddEven] at h\n    rw [hOdd] at h\n    dsimp at h; exact h\n\ntheorem combineOddEven_elim_even\n    (Odd Even : Nat → Prop)\n    (n : Nat)\n    (h : CombineOddEven Odd Even n)\n    (hOdd : Nat.odd n = false) : Even n := by\n  solution!\n    dsimp [CombineOddEven] at h\n    rw [hOdd] at h\n    dsimp at h; exact h\n\n\n","context":"Logical Foundations\u0009Logic in Lean","header":"8.5. Programming with Propositions","id":"/Logic/#Logical-Foundations--Logic-in-Lean--Programming-with-Propositions"}});