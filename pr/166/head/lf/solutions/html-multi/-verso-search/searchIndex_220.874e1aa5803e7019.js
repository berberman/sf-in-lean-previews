window.docContents[220].resolve({"/Tactics/#Logical-Foundations--Tactics___-More-Basic-Tactics--The--injection--and--contradiction--Tactics":{"contents":"Recall the definition of natural numbers:inductive Nat : Type :=\n  | zero\n  | succ (n : Nat).\nIt is obvious from this definition that every number has one of\ntwo forms: either it is the constructor 0 or it is built by\napplying the constructor .succ to another number.  But there is more\nhere than meets the eye: implicit in the definition are two\nadditional facts:* The constructor .succ is injective (or one-to-one).  That is,\n  if n + 1 = m + 1, it must also be that n = m.* The constructors 0 and .succ are disjoint.  That is, 0 is not\n  equal to n + 1 for any n.Similar principles apply to every inductively defined type:\nall constructors are injective, and the values built from distinct\nconstructors are never equal.  For lists, the cons constructor\nis injective and the empty list nil is different from every\nnon-empty list.  For booleans, true and false are different.\n(Since true and false take no arguments, their injectivity is\nneither here nor there.)  And so on.\n\n\n\n\n\nSOON: FSR'25 - I wrote an explanation for have here,\nthough I feel its inclusion here breaks the flow.\n\nLean's have tactic, used above, adds the given hypothesis\nto the context, but it first requires you to prove the hypothesis\nas a new goal.This technique for injectivity can be generalized to any constructor\nby writing the equivalent of pred -- i.e., writing a function that\n\"undoes\" one application of the constructor.As a convenient alternative, Lean provides a tactic called\ninjection that allows us to exploit the injectivity of any\nconstructor.  Here is an alternate proof of the above theorem\nusing injection:\n\n\n\ntheorem succ_injective' (n m : Nat) :\n    n + 1 = m + 1 →\n    n = m := by\n  intro h\n  injection h with hmn\n\n\nBy writing injection h with hmn at this point, we are asking Lean\nto generate all equations that it can infer from h using the\ninjectivity of constructors (in the present example, the equation\nn = m). This equation is added as a hypothesis (called\nhmn in this case) into the context. Because this equation is exactly our goal,\nin this case the injection tactic is able to automatically close the goal.\n\n\n\nHere's a more interesting example that shows how injection can\nderive multiple equations at once.\n\ntheorem injection_ex1 (n m o : Nat) :\n    [n, m] = [o, o] →\n    n = m := by\n  intro h\n  workinclass!\n    injection h with h1 h2\n    injection h2 with h3\n    rw [h1, h3]\n\n\nThere is also a related tactic, injections, that applies the injection\ntactic to all your hypotheses at once, as many times in a row as it can. Using this\ntactic can avoid needing to repeatedly use injection on lists, for example.\n\ntheorem injection_ex2 (n m o : Nat) :\n    [n, m] = [o, o] →\n    n = m := by\n  intro h\n  workinclass!\n    injections h1 _ h3\n    rw [h1, h3]\n\n\ntheorem injection_ex3 {α : Type} (x y z : α) (l j : List α) :\n    x :: y :: l = z :: j →\n    j = z :: l →\n    x = y := by\n  intro eq1 eq2\n  injections hxz hyl_j\n  have hyl_zl : y :: l = z :: l := by rw [hyl_j, eq2]\n  injections hyz\n  rw [hxz, hyz]\n\n\n\n\nSo much for injectivity of constructors.  What about disjointness?\n\nThe principle of disjointness says that two terms beginning\nwith different constructors (like 0 and succ, or true and false)\ncan never be equal.  This means that, any time we find ourselves\nin a context where we've assumed that two such terms are equal,\nwe are justified in concluding anything we want, since the\nassumption is nonsensical.\n\n\n\n\n\nThe contradiction tactic, which we've already seen for handling\ncases where we have assumed False, also embodies this principle:\nif we have a a hypothesis involving an equality between different\nconstructors (e.g., false = true), contradiction solves the current\ngoal immediately.  Some examples:\n\ntheorem disjoint_ex1 (n m : Nat) :\n    false = true →\n    n = m := by\n  intro contra\n  contradiction\n\ntheorem disjoint_ex2 (n : Nat) :\n    n + 1 = 0 →\n    2 + 2 = 5 := by\n  intro contra\n  contradiction\n\n\nThese examples are instances of a logical principle known as the\nprinciple of explosion, which asserts that a contradictory\nhypothesis entails anything (even manifestly false things!).\n\nIf you find the principle of explosion confusing, remember\nthat these proofs are not simply showing that the conclusion of the\nstatement holds.  Rather, they are showing that, if the\nnonsensical situation described by the premise did somehow hold,\nthen the nonsensical conclusion would hold too (because we'd be\nliving in an inconsistent universe where every statement is true).We'll explore the principle of explosion in more detail in the\nnext chapter.\n\ntheorem disjoint_ex3 {α : Type} (x y z : α) (l : List α) :\n    x :: y :: l = [] →\n    x = z := by\n  solution!\n    intros eq1\n    contradiction\n\n\n\n\nFor a more useful example, we can use contradiction to make a\nconnection between the two different notions of equality (= and\n==) that we have seen for natural numbers.\n\ntheorem beq_0_l (n : Nat) :\n    (0 == n) = true →\n    n = 0 := by\n  intro h\n  -- We can proceed by case analysis on `n`. The first case is trivial.\n  cases n\n  case zero => rfl\n    -- However, the second one doesn't look so simple: assuming\n    -- `(0 == n' + 1) = true`, we must show `n' + 1 = 0`!  The way forward\n    -- is to observe that the assumption itself is nonsensical:\n  case succ n' =>\n    -- If we use `contradiction` here, Lean confirms that the subgoal\n    -- we are working on is impossible and removes it from further\n    -- consideration.\n    contradiction\n\n\nHIDE: APT: Could add an advanced exercise asking them to show\nsomthing like true = false → 0 = 1 using rewrite and a\nfunction definition and using discriminate.  BCP: This might be\nnice, but not sure this is a critical point to make.HIDE: \"There should be more discussion and practice with how to\ndeal with subexpressions that do not allow application of\nhypotheses, for example how to deal with the .succ m in m + (.succ m).\nAgain, I sort of understand what to do with destruct and\ninduction, but it would help to have more exercises that break down\nthe process of making this connection.\"  BCP 9/18: Not sure exactly\nwhat to add, but if anybody has good ideas...HIDE: This relies on the fact that injection only works with\nconstructors. Should this be discussed earlier? Or is this the\nright place to mention it briefly?  BCP 20: I think here is OK,\nthough a longer explanation (including a remark on why you would\nnot want this in general!) would be welcome...HIDE: Robert Rand: I think it's nice to start them off with a\neasy question and also to use more datatypes than Nat and Bool.\n\nAll these quizzes (here and elsewhere) need to be checked!\n\nIn Rocq, there was a line of = signs between premises and conclusion.  They've gotten lost here.  There are probably more instances of this elsewhere!Recall our RGB and Color types:inductive RGB : Type where\n  | red | green | blue\ninductive Color : Type where |\n  black | white | primary (p: RGB)Suppose Lean's proof state looks likex : RGB\ny : RGB\nh : .primary x = .primary y\n\n⊢ y = x\nand we apply the tactic injection h with hxy.  What will happen?(1) \"No goals.\"(2) The tactic fails.(3) Hypothesis h becomes hxy : x = y.(4) None of the above.theorem quiz0 (x y : RGB) :\n    Color.primary x = Color.primary y →\n    x = y := by\n  intro h\n  injection h\n\n\nSuppose Lean's proof state looks likex : Bool\ny : Bool\nh : !x = !y\n\n⊢ y = x\nand we apply the tactic injection h with hxy  What will happen?(A) \"No more goals.\"(B) The tactic fails.(C) Hypothesis h becomes hxy : x = y.(D) None of the above./-- error: Tactic `injection` failed: equality of constructor applications expected\n\nx y : Bool\nh : (!decide (x = !y)) = true\n⊢ y = x -/\n#guard_msgs in\ntheorem quiz1 (x y : Bool) : !x = !y → y = x := by\n  intro h\n  injection h with hxy\n\n\nNow suppose Lean's proof state looks likex : Nat\ny : Nat\nh : x + 1 = y + 1\n\n⊢ y = x\nand we apply the tactic injection h with hxy.  What will happen?(A) \"No more goals.\"(B) The tactic fails.(C) Hypothesis h becomes hxy : x = y.(D) None of the above.theorem quiz2 (x y : Nat) : x + 1 = y + 1 → y = x := by\n  intro h\n  injection h with hxy\n  symm\n  assumption\n\n\nFinally, suppose Lean's proof state looks likex : Nat\ny : Nat\nh : 1 + x = 1 + y\n\n⊢ y = x\nand we apply the tactic injection h with hxy.  What will happen?(A) \"No more goals.\"(B) The tactic fails.(C) Hypothesis h becomes hxy : x = y.(D) None of the above./-- error: Tactic `injection` failed: equality of constructor applications expected\n\nx y : Nat\nh : 1 + x = 1 + y\n⊢ y = x -/\n#guard_msgs in\ntheorem quiz3 (x y : Nat) : 1 + x = 1 + y → y = x := by\n  intro h\n  injection h with hxy\n\n\nHIDE: BCP 9/16: Not sure this theorem is pulling its weight in SF!\nIt's used relatively few places, and there is nothing too\ninteresting to say about it here -- indeed it kind of disrupts the\nflow.  BCP 9/18: I actually found it useful several times in the\nlecture on this chapter, so I think it's best to leave it.\n\n\n\nThe injectivity of constructors allows us to reason that\n∀ (n m : Nat), n + 1 = m + 1 → n = m.  The converse of this\n implication is an instance of a more general fact about both\n constructors and functions, which we will find useful below:\n\ntheorem function_congruence {α β : Type} (f : α → β) (x y : α) :\n    x = y → f x = f y := by\n  intro eq\n  rw [eq]\n\ntheorem eq_implies_succ_equal (n m : Nat) :\n    n = m → n + 1 = m + 1 := by\n  intro eq\n  rw [eq]\n\n\ncan someone double check me on this? I think congr works this way\nbut I want to be sure\n\nIndeed, there is also a tactic named congr that can\nprove such theorems directly.  Given a goal of the form\nf a1 ... an = g b1 ... bn, the tactic congr will produce subgoals\nof the form f = g, a1 = b1, ..., an = bn. At the same time,\nany of these subgoals that are simple enough (e.g., immediately\nprovable by rfl) will be automatically discharged.\n\n\n\ntheorem eq_implies_succ_equal' (n m : Nat) :\n    n = m → n + 1 = m + 1 := by\n  intro eq\n  congr\n\n\nhow is this explanation of congr?\n\nThe congr tactic also accepts a numerical argument,\nwhich tells Lean how deeply to decompose the goal.\nSo, given a goal like ((a, b), (c, d)) = ((e, f), (g, h)),\ncongr 1 only applies congr once to the goal, and would produce\ntwo subgoals: (a, b) = (e, f) and (c, d) = (g, h).\ncongr 2, meanwhile, would apply congr again to\nboth these subgoals, and produce four subgoals: a = e, b = f,\nc = g and d = h. Using congr without an argument always\ndecomposes the goal as deeply as possible.Why does Lean provide this level of flexibility? Depending\non what we are trying to prove, deeper applications\nof congr may make our goal unprovable. Consider\nthis example:\n\n\n\nexample (a b c d : Nat) :\n    a = b → c = d → (a, c + 1) = (b, 1 + d) := by\n  intro eq1 eq2\n  congr\n  /- We now have three goals: `c = 1`, `1 = d`, and `1 = d`,\n     but these are not provable from our hypotheses! `congr`\n     has gone too deep. -/\n  sorry\n  sorry\n  sorry\n\ntheorem eq_implies_succ_proj_equal (a b c d : Nat) :\n    a = b → c = d → (a, c + 1) = (b, 1 + d) := by\n  intro eq1 eq2\n  /- Only shallowly using `congr` here allows us to complete the proof -/\n  congr 1\n  rw [add_comm]\n  congr\n\n\n","context":"Logical Foundations\u0009Tactics: More Basic Tactics","header":"6.2. The  injection  and  contradiction  Tactics","id":"/Tactics/#Logical-Foundations--Tactics___-More-Basic-Tactics--The--injection--and--contradiction--Tactics"},"/Typeclasses/#Logical-Foundations--Typeclasses--First-Example___-Inhabited-Types":{"contents":"Suppose we wanted to specify that a type has at least one inhabitant — i.e.,\nthat it is not empty. We have previously seen structure, which would allow us to express\nthis as\n\nstructure HasOneStruct (α : Type) where\n  one : α\n\n\nAs discussed previously, a structure is just an inductive type that comes with a single constructor, in this case HasOneStruct.mk, along with a few different syntaxes for convenience:\n\ndef nat_hasOneStruct_mk : HasOneStruct Nat := HasOneStruct.mk 1\n\ndef nat_hasOneStruct_brace : HasOneStruct Nat := {one := 1}\n\ndef nat_hasOneStruct_where : HasOneStruct Nat where\n  one := 1\n\ndef nat_hasOneStruct_anonymous : HasOneStruct Nat := ⟨1⟩\n\n\nAll of these are the same value, a structure of type HasOneStruct Nat that we\ncan thing of as carrying a piece of data (a term that is not a proposition), the number 1, as evidence that this type is not empty. We can use this in proofs such as\n\ntheorem nat_hasOneStruct_eq_one : nat_hasOneStruct_mk.one = 1 := rfl\n\n\nto see how this structure provably contains the value that we have placed within it. In Lean, typeclasses work very similarly, and in fact are implemented as structures. The only difference in defining a typeclass is that we instead use the class keyword:\n\nclass HasOne (α : Type) where\n  one : α\n\n\nWhile the definition is essentially the same, including the constructor HasOne.mk, the usage of typeclasses is different, and what allows us to use them to express ad hoc polymorphism. Instead of using def to define inhabitants of this type, we will instead use the instance keyword:\n\nFrom GitHub (@chenson2018): Part of the point here is that we don't usually refer to instances\nexplicitly, even me providing an explicit name is for pedagogy reasons. I'll make it more explicit\nin the text that what we're looking for in the infoview is the presence of this instance.\n\ninstance instHasOneNat : HasOne Nat where\n  one := 1\n\n\nThe definition is exactly the same as above (and any of the alternative syntaxes work as well), but instead of being used as a definition that we pass around, Lean will try to infer its usage through a process called typeclass synthesis or typeclass inference. To help see this, let's define another instance of HasOne for Int, the type of integers ... -2, -1, 0, 1, 2, ...\n\ninstance instHasOneInt : HasOne Int where\n  one := -1\n\n\nInstead of explicitly referring to a structure like we did in the proof nat_hasOneStruct_eq_one, we can instead use HasOne.one, and Lean will use typeclass inference to determine the intended meaning. As an example, you can write\n\nexample : HasOne.one = (1 : Nat) := rfl\nexample : HasOne.one = (-1 : Int) := rfl\n\n\nand based on the type annotations Lean will infer which of these instances you intended to use. So these proofs are equivalent to\n\nexample : instHasOneNat.one = (1 : Nat) := rfl\nexample : instHasOneInt.one = (-1 : Int) := rfl\n\n\nbut without needing to explicitly refer to the instances instHasOneNat and instHasOneInt. Another way that we can inspect what instance is being using with the constructor of a typeclass is the option pp.all. For example looking at\n\nset_option pp.all true in\nexample : HasOne.one = (1 : Nat) := rfl\n\nset_option pp.all true in\nexample : HasOne.one = (-1 : Int) := rfl\n\n\nwe can see that @HasOne.one Nat instHasOneNat and @HasOne.one Int instHasOneInt are the respective terms that appear on the right hand side of each equality.\n\nLean also has a command #synth that allows you to search for an instance of a given type. So\nthe command\n\n#synth HasOne Nat\n\n\nfor instance prints instHasOneNat, informing us that this is the instance found that has type HasOne Nat. In general, the assumption is that for typeclasses like HasOne that include data, as opposed to solely proofs, that these these instances should be unique to avoid ambiguity.\n\n@chenson2018: I don't really want to explain diamonds here, is the above white lie hand-waving okay??\n\n","context":"Logical Foundations\u0009Typeclasses","header":"10.1. First Example: Inhabited Types","id":"/Typeclasses/#Logical-Foundations--Typeclasses--First-Example___-Inhabited-Types"}});