window.docContents[152].resolve({"/Automation/#Logical-Foundations--Automation___-More-Automation--Case-Study___-Regular-Expressions--Definitions":{"contents":"Regular expressions are a formal language for describing sets of\nstrings. Their syntax is defined as follows:\n\ninductive RegExp (α : Type) : Type where\n  | EmptySet\n  | EmptyStr\n  | Char (c : α)\n  | App (r1 r2 : RegExp α)\n  | Union (r1 r2 : RegExp α)\n  | Star (r : RegExp α)\nderiving BEq, DecidableEq, Repr\n\nattribute [pp_nodot] RegExp.Char RegExp.App RegExp.Union RegExp.Star\ninductive RegExp (α : Type) : Type where\n  | EmptySet\n  | EmptyStr\n  | Char (c : α)\n  | App (r1 r2 : RegExp α)\n  | Union (r1 r2 : RegExp α)\n  | Star (r : RegExp α)\nderiving BEq, DecidableEq, Repr\n\nattribute [pp_nodot] RegExp.Char RegExp.App RegExp.Union RegExp.Star\ninductive RegExp (α : Type) : Type where\n  | EmptySet\n  | EmptyStr\n  | Char (c : α)\n  | App (r1 r2 : RegExp α)\n  | Union (r1 r2 : RegExp α)\n  | Star (r : RegExp α)\nderiving BEq, DecidableEq, Repr\n\nattribute [pp_nodot] RegExp.Char RegExp.App RegExp.Union RegExp.Star\n\n\nNote that this definition is polymorphic: Regular\nexpressions in RegExp α describe strings with characters drawn\nfrom α -- which in this exercise we represent as lists with\nelements from α.\n\n(Technical aside: We depart slightly from standard practice in\nthat we do not require the type α to be finite.  This results in\na somewhat different theory of regular expressions, but the\ndifference is not significant for present purposes.)\n\nCH: Do you mean here that this is different because the inductive type doesn't specify α is finite? In Lean the convention is for inductives not to carry Prop-valued typeclasss assumptions, enforcing this only at the theorems that use them. So this could give off a slightly wrong impression.\nDHS: @bcpierce00 What was the purpose of this aside in the original Rocq text? Does it make sense to keep here?\n\nWe connect regular expressions and strings by defining when a\nregular expression matches some string.\n\nInformally this looks as follows:\n\n* The regular expression EmptySet does not match any string.* EmptyString matches the empty string [].* Char x matches the one-character string x.* If re₁ matches s₁, and re₂ matches s₂,\n    then App re₁ re₂ matches s₁ ++ s₂.* If at least one of re₁ and re₂ matches s,\n    then Union re₁ re₂ matches s.* Finally, if we can write some string s as the concatenation\n    of a sequence of strings s = s_1 ++ ... ++ s_k, and the\n    expression re matches each one of the strings s_i,\n    then Star re matches s.In particular, the sequence of strings may be empty, so\n    Star re always matches the empty string [] no matter what\n    re is.\n\nWe can easily translate this intuition into a set of rules,\nwhere we write s =~ re to say that re matches s:\n\nCheck typesetting here (rules should be centered, I think):\n\n─────────────── (mEmpty)\n[] =~ EmptyStr\n\n─────────────── (mChar)\n[x] =~ (Char x)\n\ns₁ =~ re₁     s₂ =~ re₂\n─────────────────────────── (mApp)\n(s₁ ++ s₂) =~ (App re₁ re₂)\n\ns₁ =~ re₁\n───────────────────── (mUnionL)\ns₁ =~ (Union re₁ re₂)\n\ns₂ =~ re₂\n───────────────────── (mUnionR)\ns₂ =~ (Union re₁ re₂)\n\n──────────────── (mStar0)\n[] =~ (Star re)\n\ns₁ =~ re     s₂ =~ (Star re)\n──────────────────────────── (mStarApp)\n(s₁ ++ s₂) =~ (Star re)\n\n\nThis directly corresponds to the following inductive definition:\n\nnamespace RegExp\ninductive ExpMatch {α : Type} : List α → RegExp α → Prop where\n  | mEmpty : ExpMatch [] EmptyStr\n  | mChar (c : α) : ExpMatch [c] (Char c)\n  | mApp (s₁ : List α) (re₁ : RegExp α) (s₂ : List α) (re₂ : RegExp α)\n         (h₁ : ExpMatch s₁ re₁) (h₂ : ExpMatch s₂ re₂)\n       : ExpMatch (s₁ ++ s₂) (App re₁ re₂)\n  | mUnionL (s₁ : List α) (re₁ : RegExp α) (re₂ : RegExp α)\n            (h₁ : ExpMatch s₁ re₁) : ExpMatch s₁ (Union re₁ re₂)\n  | mUnionR (s₂ : List α) (re₁ : RegExp α) (re₂ : RegExp α)\n            (h₂ : ExpMatch s₂ re₂) : ExpMatch s₂ (Union re₁ re₂)\n  | mStar0 (re : RegExp α) : ExpMatch [] (Star re)\n  | mStarApp (s₁ s₂ : List α) (re : RegExp α)\n             (h₁ : ExpMatch s₁ re) (h₂ : ExpMatch s₂ (Star re))\n           : ExpMatch (s₁ ++ s₂) (Star re)\n\ninfix:40 \" =~ \" => ExpMatch\nnamespace RegExp\ninductive ExpMatch {α : Type} : List α → RegExp α → Prop where\n  | mEmpty : ExpMatch [] EmptyStr\n  | mChar (c : α) : ExpMatch [c] (Char c)\n  | mApp (s₁ : List α) (re₁ : RegExp α) (s₂ : List α) (re₂ : RegExp α)\n         (h₁ : ExpMatch s₁ re₁) (h₂ : ExpMatch s₂ re₂)\n       : ExpMatch (s₁ ++ s₂) (App re₁ re₂)\n  | mUnionL (s₁ : List α) (re₁ : RegExp α) (re₂ : RegExp α)\n            (h₁ : ExpMatch s₁ re₁) : ExpMatch s₁ (Union re₁ re₂)\n  | mUnionR (s₂ : List α) (re₁ : RegExp α) (re₂ : RegExp α)\n            (h₂ : ExpMatch s₂ re₂) : ExpMatch s₂ (Union re₁ re₂)\n  | mStar0 (re : RegExp α) : ExpMatch [] (Star re)\n  | mStarApp (s₁ s₂ : List α) (re : RegExp α)\n             (h₁ : ExpMatch s₁ re) (h₂ : ExpMatch s₂ (Star re))\n           : ExpMatch (s₁ ++ s₂) (Star re)\n\ninfix:40 \" =~ \" => ExpMatch\nnamespace RegExp\ninductive ExpMatch {α : Type} : List α → RegExp α → Prop where\n  | mEmpty : ExpMatch [] EmptyStr\n  | mChar (c : α) : ExpMatch [c] (Char c)\n  | mApp (s₁ : List α) (re₁ : RegExp α) (s₂ : List α) (re₂ : RegExp α)\n         (h₁ : ExpMatch s₁ re₁) (h₂ : ExpMatch s₂ re₂)\n       : ExpMatch (s₁ ++ s₂) (App re₁ re₂)\n  | mUnionL (s₁ : List α) (re₁ : RegExp α) (re₂ : RegExp α)\n            (h₁ : ExpMatch s₁ re₁) : ExpMatch s₁ (Union re₁ re₂)\n  | mUnionR (s₂ : List α) (re₁ : RegExp α) (re₂ : RegExp α)\n            (h₂ : ExpMatch s₂ re₂) : ExpMatch s₂ (Union re₁ re₂)\n  | mStar0 (re : RegExp α) : ExpMatch [] (Star re)\n  | mStarApp (s₁ s₂ : List α) (re : RegExp α)\n             (h₁ : ExpMatch s₁ re) (h₂ : ExpMatch s₂ (Star re))\n           : ExpMatch (s₁ ++ s₂) (Star re)\n\ninfix:40 \" =~ \" => ExpMatch\n\n\nreplace with quiz directive\n\nNotice that this clause in our informal definition...* \"The expression EmptySet does not match any string.\"... is not explicitly reflected in the above definition.  Do we\nneed to add something?(A) Yes, we should add a rule for this.(B) No, one of the other rules already covers this case.(C) No, the lack of a rule actually gives us the behavior we\n       want.\n\ntheorem quiz α (s: List α) : ¬(s =~ EmptySet) := by\n  intro contra; inversion contra\ntheorem quiz α (s: List α) : ¬(s =~ EmptySet) := by\n  intro contra; inversion contra\ntheorem quiz α (s: List α) : ¬(s =~ EmptySet) := by\n  intro contra; inversion contra\n\n\nNotice that these rules are not quite the same as the\nintuition that we gave at the beginning of the section. First, we\ndon't need to include a rule explicitly stating that no string is\nmatched by EmptySet; indeed, the syntax of inductive definitions\ndoesn't even allow us to give such a \"negative rule.\" We just\ndon't happen to include any rule that would have the effect of\nEmptySet matching some string.Second, the intuition we gave for Union and Star correspond\nto two constructors each: mUnionL / mUnionR, and mStar0 /\nmStarApp.  The result is logically equivalent to the original\nintuition but more convenient to use in Lean, since the recursive\noccurrences of ExpMatch are given as direct arguments to the\nconstructors, making it easier to perform induction on evidence.\n(The exercises below ask you\nto prove that the constructors given in the inductive declaration\nand the ones that would arise from a more literal transcription of\nthe intuition is indeed equivalent.)Let's illustrate these rules with a few examples.\n\n","context":"Logical Foundations\u0009Automation: More Automation\u0009Case Study: Regular Expressions","header":"9.5.1. Definitions","id":"/Automation/#Logical-Foundations--Automation___-More-Automation--Case-Study___-Regular-Expressions--Definitions"}});