window.docContents[243].resolve({"/Types/#Type-Systems--Types___-Type-Systems--Typed-Arithmetic-Expressions--Progress":{"contents":"The typing relation enjoys two critical properties.\n\nThe first is that well-typed normal forms are not stuck — or conversely,\nif a term is well typed, then either it is a value or it can take at\nleast one step.  We call this progress.\n\nComplete the formal proof of the progress property.  (Make sure you\nunderstand the parts we've given of the informal proof in the following\nexercise before starting — this will save you a lot of time.)theorem progress (t : Tm) (τ : Ty) (hT : <{ ⊢ t ⦂ T }>) : Tm.IsValue t ∨ ∃ t', t ⟶ t' := by\n  solution!\n    induction hT with\n    | tru => exact .inl (.inl .tru)\n    | fls => exact .inl (.inl .fls)\n    | zero => exact .inl (.inr .zero)\n    | ite t₁ t₂ t₃ T h₁ h₂ h₃ ih₁ ih₂ ih₃ =>\n        right\n        cases ih₁ with\n        | inl hv₁ =>\n            cases bool_canonical t₁ h₁ hv₁ with\n            | tru => exact ⟨t₂, .ifTrue t₂ t₃⟩\n            | fls => exact ⟨t₃, .ifFalse t₂ t₃⟩\n        | inr hs₁ => obtain ⟨t₁', h⟩ := hs₁\n                     exact ⟨<{ if t₁' then t₂ else t₃ }>, .ifStep t₁ t₁' t₂ t₃ h⟩\n    | succ t₁ h ih =>\n        cases ih with\n        | inl hv => exact .inl (.inr (.succ t₁ (nat_canonical t₁ h hv)))\n        | inr hs => obtain ⟨t', h'⟩ := hs; exact .inr ⟨<{ succ t' }>, .succStep t₁ t' h'⟩\n    | pred t₁ h ih =>\n        right\n        cases ih with\n        | inl hv =>\n            cases nat_canonical t₁ h hv with\n            | zero => exact ⟨<{ 0 }>, .predZero⟩\n            | succ t₀ hn₀ => exact ⟨t₀, .predSucc t₀ hn₀⟩\n        | inr hs => obtain ⟨t', h'⟩ := hs; exact ⟨<{ pred t' }>, .predStep t₁ t' h'⟩\n    | isZero t₁ h ih =>\n        right\n        cases ih with\n        | inl hv =>\n            cases nat_canonical t₁ h hv with\n            | zero => exact ⟨<{ true }>, .isZeroZero⟩\n            | succ t₀ hn₀ => exact ⟨<{ false }>, .isZeroSucc t₀ hn₀⟩\n        | inr hs => obtain ⟨t', h'⟩ := hs; exact ⟨<{ iszero t' }>, .isZeroStep t₁ t' h'⟩\n\n\nWhat is the relation between the progress property defined here and the\nstrong progress from the Smallstep chapter?(A) No difference — they mean the same thing(B) Progress implies strong progress(C) Strong progress implies progress(D) No relationship(E) Dunno\n\n(C) Strong progress implies progress\n\nComplete the corresponding informal proof.Check the typesetting of this...Theorem: If ⊢ t ⦂ T, then either t is a value or else t ⟶ t' for\nsome t'.Proof: By induction on a derivation of ⊢ t ⦂ T.* If the last rule in the derivation is ite, then t = if t₁ then t₂\n    else t₃, with ⊢ t₁ ⦂ Bool, ⊢ t₂ ⦂ T and ⊢ t₃ ⦂ T.  By the IH,\n    either t₁ is a value or else t₁ can step to some t₁'.* If t₁ is a value, then by the canonical forms lemmas and the fact\n      that ⊢ t₁ ⦂ Bool we have that t₁ is a boolean value\n      (Tm.IsBValue) — i.e., it is either true or false.  If t₁ = true, then t steps to t₂ by\n      ifTrue, while if t₁ = false, then t steps to t₃ by\n      ifFalse.  Either way, t can step, which is what we wanted to\n      show.* If t₁ itself can take a step, then, by ifStep, so can t.- If the last rule in the derivation is `tru`, then `t = true`, which\n  is a boolean value and hence a value.  The cases for `fls` and `zero`\n  are similar.\n\n- If the last rule in the derivation is `succ`, then `t = succ t₁`, with\n  `⊢ t₁ ⦂ Nat`.  By the IH, either `t₁` is a value or else `t₁` can step\n  to some `t₁'`.\n\n  - If `t₁` is a value, then by the canonical forms lemma `t₁` is an\n    `nvalue`, and hence `t` is also an `nvalue` (and hence a value) by\n    `succ`.\n\n  - If `t₁` can take a step, then by `succStep`, so can `t`.\n\n- If the last rule in the derivation is `pred`, then `t = pred t₁`, with\n  `⊢ t₁ ⦂ Nat`.  By the IH, either `t₁` is a value or else `t₁` can step\n  to some `t₁'`.\n\n  - If `t₁` is a value, then (by the same argument as in the previous\n    case) it must be an `nvalue`.  By case analysis on the `nvalue`\n    judgement, there are two cases:\n\n    - If `t₁ = 0`, then `t` can take a step by `predZero`.\n\n    - Otherwise, `t₁ = succ t₁'`, with `t₁'` an `nvalue`.  Hence `t` can\n      again take a step, this time by `predSucc`.\n\n  - Finally, if `t₁` can take a step, then by `predStep`, so can `t`.\n\n- If the last rule in the derivation is `isZero`, then `t = iszero t₁`,\n  with `⊢ t₁ ⦂ Nat`.  By the IH, either `t₁` is a value or else `t₁` steps\n  to some `t₁'`.\n\n  - If `t₁` is a value, it must be an `nvalue`, and there are two cases to\n    consider:\n\n    - If `t₁ = 0`, then `t` can take a step by `isZeroZero`.\n\n    - Otherwise, `t₁ = succ t₁'` where `t₁'` is an `nvalue`.  Hence `t` can\n      take a step by `isZeroSucc`.\n\n  - If `t₁` can take a step, then so can `t`, by `isZeroStep`.\n\n\nThis theorem is more interesting than the strong progress theorem that we\nsaw in the Smallstep chapter, where all normal forms were values.  Here\na term can be stuck, but only if it is ill typed.\n\nQuick review: in the language defined at the start of this chapter...* Every well-typed normal form is a value.(A) True    (B) FalseTRUE: This is the content of the progress theorem.\n\nIn this language...* Every value is a normal form.(A) True    (B) FalseTRUE: This can be proved by induction on values.\n\nIn this language...* The single-step reduction relation is a partial function (i.e., it is\n    deterministic).(A) True    (B) FalseTRUE: This is the determinism theorem.\n\nIn this language...* The single-step reduction relation is a total function.(A) True    (B) FalseFALSE: normal forms do not reduce to anything.\n\n","context":"Type Systems\u0009Types: Type Systems\u0009Typed Arithmetic Expressions","header":"4.1.6. Progress","id":"/Types/#Type-Systems--Types___-Type-Systems--Typed-Arithmetic-Expressions--Progress"}});