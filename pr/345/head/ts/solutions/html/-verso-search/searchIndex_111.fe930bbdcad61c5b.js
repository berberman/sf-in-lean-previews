window.docContents[111].resolve({"/Types/#Type-Systems--Types___-Type-Systems--Typed-Arithmetic-Expressions--Type-Preservation":{"contents":"The second critical property of typing is that, when a well-typed term\ntakes a step, the result is a well-typed term (of the same type).\n\nComplete the formal proof of the preservation property.  (Again, make\nsure you understand the informal proof fragment in the following exercise\nfirst.)theorem preservation (t t' : Tm) (τ : Ty) (hT : <{ ⊢ t ⦂ T }>) (he : t ⟶ t') : <{ ⊢ t' ⦂ T }> := by\n  solution!\n    induction hT generalizing t' with\n    | tru => cases he\n    | fls => cases he\n    | zero => cases he\n    | ite t₁ t₂ t₃ T h₁ h₂ h₃ ih₁ ih₂ ih₃ =>\n        cases he with\n        | ifTrue => exact h₂\n        | ifFalse => exact h₃\n        | ifStep _ c' _ _ hc => exact .ite c' t₂ t₃ T (ih₁ c' hc) h₂ h₃\n    | succ t₁ h ih =>\n        cases he with\n        | succStep _ t₁' hs => exact .succ t₁' (ih t₁' hs)\n    | pred t₁ h ih =>\n        cases he with\n        | predZero => exact .zero\n        | predSucc v hv => cases h with | succ _ hh => exact hh\n        | predStep _ t₁' hs => exact .pred t₁' (ih t₁' hs)\n    | isZero t₁ h ih =>\n        cases he with\n        | isZeroZero => exact .tru\n        | isZeroSucc v hv => exact .fls\n        | isZeroStep _ t₁' hs => exact .isZero t₁' (ih t₁' hs)\n\n\nComplete the following informal proof.Theorem: If ⊢ t ⦂ T and t ⟶ t', then ⊢ t' ⦂ T.Proof: By induction on a derivation of ⊢ t ⦂ T.* If the last rule in the derivation is ite, then t = if t₁ then t₂\n    else t₃, with ⊢ t₁ ⦂ Bool, ⊢ t₂ ⦂ T and ⊢ t₃ ⦂ T.Inspecting the rules for the small-step reduction relation and\n    remembering that t has the form if ..., we see that the only ones\n    that could have been used to prove t ⟶ t' are ifTrue,\n    ifFalse, or ifStep.* If the last rule was ifTrue, then t' = t₂.  But we know that\n      ⊢ t₂ ⦂ T, so we are done.* If the last rule was ifFalse, then t' = t₃.  But we know that\n      ⊢ t₃ ⦂ T, so we are done.* If the last rule was ifStep, then t' = if t₁' then t₂ else t₃,\n      where t₁ ⟶ t₁'.  We know ⊢ t₁ ⦂ Bool so, by the IH, ⊢ t₁' ⦂\n      Bool.  The ite rule then gives us ⊢ if t₁' then t₂ else t₃ ⦂ T,\n      as required.- If the last rule in the derivation were `tru`, then `t = true`.\n  However, `true` does not step to anything, so this case is vacuously\n  true.\n\n- Similarly, neither `fls` nor `zero` could be the final rule in the\n  derivation.\n\n- If the last rule in the derivation is `succ`, then `t = succ t₁` with\n  `⊢ t₁ ⦂ Nat` and `T = Nat`.  The only rule which could have been used to\n  show that `t` steps is `succStep`, in which case `t₁` steps to some\n  `t₁'`.  So, by the IH, `⊢ t₁' ⦂ Nat`, and hence `t' = succ t₁'` also has\n  type `Nat` by `succ`.\n\n- If the last rule in the derivation is `pred`, then `t = pred t₁` with\n  `⊢ t₁ ⦂ Nat`.  There are only three rules which could have been the last\n  rule in the derivation of `pred t₁ ⟶ t'`.\n\n  - If the last rule was `predZero`, then `t' = 0` which has type `Nat`.\n\n  - If the last rule was `predSucc`, then `t₁ = succ t'`; by inversion\n    on the fact that `⊢ t₁ ⦂ Nat` it follows that `⊢ t' ⦂ Nat` as well.\n\n  - If the last rule was `predStep`, then `t₁` steps to some `t₁'`; by the\n    IH `⊢ t₁' ⦂ Nat`, and so `pred t₁'` has type `Nat` as well by\n    `pred`.\n\n- If the last rule in the derivation is `isZero`, then `t = iszero t₁`\n  with `⊢ t₁ ⦂ Nat` and `T = Bool`.  There are only three rules which\n  could have been the last rule in the derivation of `iszero t₁ ⟶ t'`.\n\n  - If the last rule was `isZeroZero`, then `t' = true` which has type\n    `Bool`.\n\n  - If the last rule was `isZeroSucc`, then `t' = false` which has type\n    `Bool`.\n\n  - If the last rule was `isZeroStep`, then `t₁` steps to some `t₁'`.  By\n    the IH, `⊢ t₁' ⦂ Nat` as well, and hence `t' = iszero t₁'` has type\n    `Bool` by `isZero`.\n\n\nNow prove the same property again by induction on the evaluation\nderivation instead of on the typing derivation.  Begin by carefully\nreading and thinking about the first few lines of the above proofs to\nmake sure you understand what each one is doing.  The set-up for this\nproof is similar, but not exactly the same.theorem preservation' (t t' : Tm) (τ : Ty) (hT : <{ ⊢ t ⦂ τ }>) (he : t ⟶ t') : <{ ⊢ t' ⦂ τ }> := by\n  solution!\n    induction he generalizing τ with\n    | ifTrue t₁ t₂ => cases hT with | ite _ _ _ _ h₁ h₂ h₃ => exact h₂\n    | ifFalse t₁ t₂ => cases hT with | ite _ _ _ _ h₁ h₂ h₃ => exact h₃\n    | ifStep c c' t₂ t₃ hc ih =>\n        cases hT with | ite _ _ _ _ h₁ h₂ h₃ => exact .ite c' t₂ t₃ τ (ih .bool h₁) h₂ h₃\n    | succStep t₁ t₁' hs ih => cases hT with | succ _ h => exact .succ t₁' (ih .nat h)\n    | predZero => cases hT with | pred _ h => exact .zero\n    | predSucc v hv => cases hT with | pred _ h => cases h with | succ _ hh => exact hh\n    | predStep t₁ t₁' hs ih => cases hT with | pred _ h => exact .pred t₁' (ih .nat h)\n    | isZeroZero => cases hT with | isZero _ h => exact .tru\n    | isZeroSucc v hv => cases hT with | isZero _ h => exact .fls\n    | isZeroStep t₁ t₁' hs ih => cases hT with | isZero _ h => exact .isZero t₁' (ih .nat h)\n\n\nThe preservation theorem is often called subject reduction, because it\ntells us what happens when the \"subject\" of the typing relation is\nreduced.  This terminology comes from thinking of typing statements as\nsentences, where the term is the subject and the type is the predicate.\n\n","context":"Type Systems\u0009Types: Type Systems\u0009Typed Arithmetic Expressions","header":"4.1.7. Type Preservation","id":"/Types/#Type-Systems--Types___-Type-Systems--Typed-Arithmetic-Expressions--Type-Preservation"}});