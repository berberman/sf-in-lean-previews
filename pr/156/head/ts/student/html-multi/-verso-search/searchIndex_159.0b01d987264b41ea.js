window.docContents[159].resolve({"/Stlc/#Type-Systems--Stlc___-The-Simply-Typed-Lambda-Calculus--Overview":{"contents":"The STLC is built on some collection of base types:\nbooleans, numbers, strings, etc.  The exact choice of base types\ndoesn't matter much -- the definition of the language as well as\nits theoretical properties work out the same no matter what we\nchoose -- so for the sake of brevity let's take just Bool for\nthe moment.  In the next chapter we'll see how to add more\nbase types, and in later chapters we'll enrich the pure STLC with\nother useful constructs like pairs, records, subtyping, and\nmutable state.Starting from boolean constants and conditionals, we add three\nthings:* variables* function abstractions* applicationThis gives us the following collection of abstract syntax\nconstructors (written out first in informal BNF notation -- we'll\nformalize it below) for STLC terms t.\n\n\n\nt ::= x (\"variable\")\n    | \"λ\" x \":\" T \".\" t (\"abstraction\")\n    | t t (\"application\")\n    | \"true\" (\"constant true\")\n    | \"false\" (\"constant false\")\n    | \"if\" t \"then\" t \"else\" t (\"conditional\") ;\n\n\nThe Greek letter λ (\"lambda\") in a function abstraction λx:T. t is what gives\nthe calculus its name.  The variable x is called the parameter to the\nfunction; the term t is its body.  The annotation :T\nspecifies the type of arguments that the function can be applied to.The types of the STLC include Bool, which classifies the\nboolean constants true and false as well as more complex\ncomputations that yield booleans, plus arrow types that classify\nfunctions (as is the case in Lean).\n\n\n\nT ::= \"Bool\"\n    | T \"→\" T ;\n\n\n\n\nSome examples of STLC terms:\n\n λx:Bool. x\n\nThe identity function for booleans.\n\n (λx:Bool. x) true\n\nThe identity function for booleans, applied to the boolean true.\n\n λx:Bool. if x then false else true\n\nThe boolean \"not\" function.\n\n λx:Bool. true\n\nThe constant function that takes every (boolean) argument to\n  true.\n\n\n\n\n\n λx:Bool. λy:Bool. x\n\nA two-argument function that takes two booleans and returns\n  the first one.(As in Lean, a two-argument function in the\n  lambda-calculus is really a one-argument function whose body\n  is also a one-argument function.)\n\n (λx:Bool. λy:Bool. x) false true\n\nA two-argument function that takes two booleans and returns\n  the first one, applied to the booleans false and true.(As in Lean, application associates to the left -- i.e., this\n  expression is parsed as ((λx:Bool. λy:Bool. x) false) true.)\n\n λf:Bool → Bool. f (f true)\n\nA higher-order function that takes a function f (from\n  booleans to booleans) as an argument, applies f to true,\n  and applies f again to the result.\n\n (λf:Bool → Bool. f (f true)) (λx:Bool. false)\n\nThe same higher-order function, applied to the constantly\n  false function.\n\n\n\n\n\nThe last two examples show, the STLC is a language of\nhigher-order functions: we can write down functions that take\nother functions as arguments and/or return other functions as results.The STLC doesn't provide any primitive syntax for defining named\nfunctions: i.e., all functions are \"anonymous.\"  We'll see in chapter\nMoreStlc that it is easy to add named functions -- indeed, the\nfundamental naming and binding mechanisms are exactly the same.\n\nNow reconsider our examples, each along with its type:\n\n* λx:Bool. x has type Bool → Bool* (λx:Bool. x) true has type Bool* λx:Bool. if x then false else true has type Bool → Bool* λx:Bool. true has type Bool → Bool* λx:Bool. λy:Bool. x has type Bool → Bool → Bool\n                        (i.e., Bool → (Bool → Bool))* (λx:Bool. λy:Bool. x) false true has type Bool\n\nThe last two, higher-order examples are left off the list on purpose -- working out\ntheir types is the subject of the quizzes that follow.\n\n\n\n\n\nWhat is the type of the following term?λf:Bool → Bool. f (f true)\n(A) Bool → (Bool → Bool)(B) (Bool → Bool) → Bool(C) Bool → Bool(D) Bool(E) none of the above\n\nHow about the type of this one?(λf:Bool → Bool. f (f true)) (λx:Bool. false)\n(A) Bool → (Bool → Bool)(B) (Bool → Bool) → Bool(C) Bool → Bool(D) Bool(E) none of the above\n\n","context":"Type Systems\u0009Stlc: The Simply Typed Lambda-Calculus","header":"5.1. Overview","id":"/Stlc/#Type-Systems--Stlc___-The-Simply-Typed-Lambda-Calculus--Overview"}});