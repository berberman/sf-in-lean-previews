window.docContents[219].resolve({"/Induction/#Induction":{"contents":"[BCP: Old comment -- might be out of date?]\nA lot of the proofs on the naturals rely on how operations on naturals were defined in Basics.lean,\nbut in the stdlib they're slightly different\n(e.g. sub is defined via pred rather than directly by recursion),\nand the notations all go through typeclasses,\nwhich makes the proofs a lot less direct\n(e.g. the existing 0 + n proof refers to Nat.add_succ).\nWe should do one of the following:1. Not use +, -, * notation and instead use add, sub, mul directly; or2. Override stdlib notation with ones pointing to the definitions in Basics.lean.\n\nOption 1 is a very reasonable way to go about this if we're attached to arithmetic being the way we teach induction.\nMy primary concern is that operators and type classes are already so confusing\nthat adding another meaning of + is liable to throw someone way off.\nIs there another context we can teach induction in that also doesn't require a ton of background?\n\nBasics.lean now overrides the typeclasses for -, *, and ^,\nbut not +, since that one is pervasive throughout the stdlib and causes problems;\nI think this works okay and isn't too confusing.If we continue doing arithmetic proofs,\nthis is a good place to introduce equational reasoning via calc.\n\nReaders might expect us to add eqn:H annotations to uses of\ninduction, but this changes the shape of the IH in a nasty way! :-(\nWe should at least comment.  (BCP: Is this still relevant in Lean?)\n\nSOONER: We should also consider adding more examples to clarify\nthe concepts introduced in this chapter. This could help in\nreinforcing the understanding of induction principles.\n\nLATER: In 3/22, MRC and BCP discussed \"inlining\" IndPrinciples\ninto earlier chapters, thus eliminating it as a chapter. This\nchapter, Induction, is the first place a change would occur.  We\nwould present [nat_ind] here. Then in Lists/Poly we'd present\n[list_ind], and the rest would go in IndProp and ProofObjects. The\nmain wrinkle is that we'd need to introduce [apply] here instead of\nin Tactics if we want to preserve the presentation. The discussion\nis preserved here: https://github.com/DeepSpec/sfdev/pull/471.\n\nLATER: Now that we've added Steve's nice late-policy exercise in\nBasics.v, the assignment for that chapter is probably hard enough.  Now\nwhat about this chapter?  Can/should we make it a notch or two\nharder?\n\n\n\n\n\n\n\n\n\n\n\n\n","context":"Logical Foundations","header":"3. Induction: Proof by Induction","id":"/Induction/#Induction"},"/UsingLean/#Logical-Foundations--UsingLean___-Using-the-full-power-of-a-proof-assistant--Structuring-proofs-with--calc":{"contents":"In Lean proofs, long rw chains are useful, but they are sometimes hard to\nread because the intermediate goals are invisible. Furthermore, sometimes we\nknow exactly how we want to manipulate the terms of a proof, but don't want\nto have the tactics like add_comm and add_assoc \"guess\" which subterms to\nrewrite.\n\nThe calc tactic writes down the intermediate goals of a proof, and allows us to\nspecify exactly which rewrite rules to apply at each step. It is a powerful\ntool for structuring proofs, and is often more readable than long rw chains.\n\ncalc is designed to mimic the style of proofs in mathematics textbooks, which will\noften look something like this:\n\na + (b + c)\n= (a + b) + c        ...   [by associativity of addition]\n= (b + a) + c        ...   [by commutativity of addition]\n= b + (a + c)        ...   [by associativity of addition]\n\n\nNote how we can see each intermediate step of this proof when we\nlook at it this way. Let's look at how we might prove this theorem\n(i.e., that a + (b + c) = b + (a + c)) in Lean.\n\nFirst, a proof in the style we already know.\n\ntheorem add_swap (a b c : Nat) : a + (b + c) = b + (a + c) := by\n  rw [← Nat.add_assoc, Nat.add_comm a b, Nat.add_assoc]\n\n\nHere we present the same theorem, written with calc.\nNote how each intermediate goal is visible in the source.\n\ntheorem add_swap' (a b c : Nat) : a + (b + c) = b + (a + c) := by\n  calc a + (b + c) /- one side of the goal is the argument to `calc`... -/\n    /- ... and each subsequent line is a transformation, with a tactic. -/\n    a + (b + c) = (a + b) + c := by rw [Nat.add_assoc]\n    (a + b) + c = (b + a) + c := by rw [Nat.add_comm a b]\n    /- once a line matches the other side of the equality in the main goal\n       (in this case `(b + (a + c)`), the calc tactic succeeds. -/\n    (b + a) + c = b + (a + c) := by rw [Nat.add_assoc]\n\n\nWe can also write the proof like this to be a bit more concise:\n\ntheorem add_swap'' (a b c : Nat) : a + (b + c) = b + (a + c) := by\n  calc a + (b + c)\n    _ = (a + b) + c := by rw [Nat.add_assoc]\n    _ = (b + a) + c := by rw [Nat.add_comm a b]\n    _ = b + (a + c) := by rw [Nat.add_assoc]\n\n\nWhereas before, the left-hand side of each equality in the calc\ntactic was repeated from the right-hand side of the previous one,\nwe can replace the left-hand side entirely with an _. Now our\nLean proof looks quite a bit like the textbook one we saw earlier!\n\ntheorem succ_mul_succ (n m : Nat) :\n    (n + 1) * (m + 1) = n * m + n + m + 1 := by\n  rw [Nat.add_mul, Nat.one_mul, Nat.mul_add, Nat.mul_one, ← Nat.add_assoc]\nGiven this proof with rw, rewrite it with calc. Reminder that you can use rw? to find\nappropriate rules to rewrite by.theorem succ_mul_succ' (n m : Nat) :\n    (n + 1) * (m + 1) = n * m + n + m + 1 := by\n  sorry\nIf you prefer rw to calc, that's fine! Each has particular uses, and both\nwill be tools in your ever-growing toolbox of tactics.Needs some exercises!!\n\n","context":"Logical Foundations\u0009UsingLean: Using the full power of a proof assistant","header":"4.3. Structuring proofs with  calc","id":"/UsingLean/#Logical-Foundations--UsingLean___-Using-the-full-power-of-a-proof-assistant--Structuring-proofs-with--calc"}});