window.docContents[223].resolve({"/Slang/#Type-Systems--Slang___-Arithmetic-and-Boolean-Expressions--Arithmetic-and-Boolean-Expressions--Optimization":{"contents":"We can now start to get some mileage out of these definitions. Suppose we define a\nfunction that takes an arithmetic expression and slightly simplifies it, changing\nevery occurrence of 0 + e (i.e., .plus (.num 0) e) into just e.\n\ndef Aexp.optimize0plus (a : Aexp) : Aexp :=\n  match a with\n  | num   n          => num n\n  | plus  (num 0) e2 => optimize0plus e2\n  | plus  e1      e2 => plus  (optimize0plus e1) (optimize0plus e2)\n  | minus e1      e2 => minus (optimize0plus e1) (optimize0plus e2)\n  | mult  e1      e2 => mult  (optimize0plus e1) (optimize0plus e2)\n\n\nTo gain confidence that our optimization is doing the right thing, we\ncan test it on some examples and see if the output looks OK.\n\nexample :\n    Aexp.optimize0plus (.plus (.num 2)\n                               (.plus (.num 0)\n                                      (.plus (.num 0) (.num 1))))\n      = .plus (.num 2) (.num 1) := by rfl\n\n\nBut if we want to be certain the optimization is correct -- that\nevaluating an optimized expression always gives the same result as\nthe original -- we should prove it!Here is a first, deliberately explicit, proof, by induction on a. The\ninteresting case is Aexp.plus: because Aexp.optimize0plus treats plus (num 0) e\nspecially, we case-split on the left operand a₁ -- and, when it is a numeral,\non whether that numeral is 0 -- to line the proof up with the function's own\nbranches. Once the constructors are exposed, each case is discharged by\nessentially the same incantation: unfold Aexp.optimize0plus, rewrite Aexp.eval by its\ncharacterizing lemmas, then finish with the induction hypotheses. Notice how\nrepetitive that makes the proof.\n\ntheorem optimize0plus_sound (a : Aexp) :\n    a.optimize0plus.eval = a.eval := by\n  induction a with\n  | num n => rfl\n  | plus a₁ a₂ ih1 ih2 =>\n    cases a₁ with\n    | num n =>\n      cases n with\n      | zero =>\n        simp only [Aexp.optimize0plus, Aexp.eval_plus, Aexp.eval_num, Nat.zero_add]\n        exact ih2\n      | succ n =>\n        simp only [Aexp.optimize0plus, Aexp.eval_plus, Aexp.eval_num]\n        rw [ih2]\n    | plus b₁ b₂ =>\n      simp only [Aexp.optimize0plus, Aexp.eval_plus] at ih1 ⊢\n      rw [ih1, ih2]\n    | minus b₁ b₂ =>\n      simp only [Aexp.optimize0plus, Aexp.eval_plus] at ih1 ⊢\n      rw [ih1, ih2]\n    | mult b₁ b₂ =>\n      simp only [Aexp.optimize0plus, Aexp.eval_plus] at ih1 ⊢\n      rw [ih1, ih2]\n  | minus a₁ a₂ ih1 ih2 =>\n    simp only [Aexp.optimize0plus, Aexp.eval_minus]\n    rw [ih1, ih2]\n  | mult a₁ a₂ ih1 ih2 =>\n    simp only [Aexp.optimize0plus, Aexp.eval_mult]\n    rw [ih1, ih2]\n\n\nWe can do much better. The case analysis we performed by hand -- peeling\nplus apart to reach the plus (num 0) e branch -- is exactly the case\nanalysis that Aexp.optimize0plus itself performs.The fun_induction tactic\ninducts along a function's own recursion structure: fun_induction\nAexp.optimize0plus a hands us one goal per branch of optimize0plus -- the\nspecial plus (num 0) e branch included -- so the nested cases disappear.Before applying fun_induction to a function as complex as Aexp.optimize0plus,\nlet's see how it works on somthing simpler. Recall the definition of Nat.even and Nat.odd:def Nat.even (n : Nat) :=\n  match n with\n  | 0 => true\n  | 1 => false\n  | n' + 2 => even n'\n\ndef Nat.odd (n : Nat) := Nat.even (n + 1)\nNormally, if we perform induction on n, we get two cases - 0 and n' + 1 -\none for each of the cases in the inductive definition of natural numbers.\nFunctional induction on Nat.even, however, gives us three cases - 0, 1, and n' + 2 -\ncorresponding to each of the cases of its definition.example (n : Nat) (h : Nat.even n = true) : Nat.odd n = false := by\n  fun_induction Nat.even n\n  . rfl\n  . contradiction\n  . simp [Nat.odd, Nat.even] at *\n    lia\nNow let's try using fun_induction on Aexp.optimize0plus. When we do this,\nevery goal has the same shape, so we can attack them uniformly\nwith the <;> combinator and a single tactic, simp_all, which rewrites\nAexp.eval by the @[simp] characterizing lemmas and uses the induction hypotheses\n-- which it picks up from the local context automatically -- to close\neach goal. The whole proof collapses to two lines.\n\n\n\ntheorem optimize0plus_sound' (a : Aexp) :\n    a.optimize0plus.eval = a.eval := by\n  fun_induction Aexp.optimize0plus a <;> simp_all\n\n\nSince the Aexp.optimize0plus transformation doesn't change the value of an\nAexp, we should be able to apply it to all the Aexps that appear in a\nBexp without changing the Bexp's value.  Write a function that\nperforms this transformation on Bexps and prove it sound. Use the\ncombinators we've just seen to make the proof as short and elegant as\npossible.def Bexp.optimize0plusB (b : Bexp) : Bexp := sorry\n\ntheorem optimize0plusB_test1 :\n    Bexp.optimize0plusB\n        (.not (.gt (.plus (.num 0) (.num 4)) (.num 8)))\n      = (.not (.gt (.num 4) (.num 8))) := sorry\ntheorem optimize0plusB_test2 :\n    Bexp.optimize0plusB\n        (.and (.le (.plus (.num 0) (.num 4)) (.num 5)) (.bool true))\n      = (.and (.le (.num 4) (.num 5)) (.bool true)) := sorry\ntheorem optimize0plusB_sound (b : Bexp) :\n    b.optimize0plusB.eval = b.eval := by\n  sorry\n\n\nThe optimization implemented by our Aexp.optimize0plus is only one of\nmany possible optimizations on arithmetic and boolean expressions. Write a more\nsophisticated optimizer and prove it correct. (You will probably find it easiest\nto start small -- add just a single, simple optimization and its correctness proof --\nand build up incrementally to something more interesting.)\n\n","context":"Type Systems\u0009Slang: Arithmetic and Boolean Expressions\u0009Arithmetic and Boolean Expressions","header":"2.1.3. Optimization","id":"/Slang/#Type-Systems--Slang___-Arithmetic-and-Boolean-Expressions--Arithmetic-and-Boolean-Expressions--Optimization"},"/Smallstep/#Type-Systems--Smallstep___-Small-step-Operational-Semantics--Small-Step-Slang--Boolean-Expressions":{"contents":"The small-step relation for boolean expressions reduces the arithmetic\nsubexpressions of a comparison (using ⟶a) and then applies the comparison,\nand it short-circuits ¬ and ∧ on boolean literals.We are not actually going to bother to define boolean values, since they\naren't needed in the definition of ⟶b below (why?), though they might be if\nour language were a bit more complicated (why?).Again we show a\nrepresentative sample; neq, le, and gt follow the same pattern as eq.                        a1 ⟶a a1'\n                  --------------------             (eqLeft)\n                  a1 = a2 ⟶b a1' = a2\n\n                IsAValue v1      a2 ⟶a a2'\n                ---------------------------        (eqRight)\n                  v1 = a2 ⟶b v1 = a2'\n\n                  ---------------------            (eq)\n                  n1 = n2 ⟶b (n1 = n2)\n\n                        b1 ⟶b b1'\n                      --------------               (notStep)\n                      ¬ b1 ⟶b ¬ b1'\n\n                    ----------------               (notTrue)\n                    ¬ true ⟶b false\n\n                  ---------------------            (andFalse)\n                  false ∧ b2 ⟶b false\nHere are the formal rules.\n\ninductive BStep : Bexp → Bexp → Prop where\n  | eqLeft (a1 a1' a2 : Aexp) (h : AStep a1 a1') : BStep (.eq a1 a2) (.eq a1' a2)\n  | eqRight (v1 a2 a2' : Aexp) (hv : IsAValue v1) (h : AStep a2 a2') :\n      BStep (.eq v1 a2) (.eq v1 a2')\n  | eq (n1 n2 : Nat) : BStep (.eq (.num n1) (.num n2)) (.bool (decide (n1 = n2)))\n  | neqLeft (a1 a1' a2 : Aexp) (h : AStep a1 a1') : BStep (.neq a1 a2) (.neq a1' a2)\n  | neqRight (v1 a2 a2' : Aexp) (hv : IsAValue v1) (h : AStep a2 a2') :\n      BStep (.neq v1 a2) (.neq v1 a2')\n  | neq (n1 n2 : Nat) : BStep (.neq (.num n1) (.num n2)) (.bool (decide (n1 ≠ n2)))\n  | leLeft (a1 a1' a2 : Aexp) (h : AStep a1 a1') : BStep (.le a1 a2) (.le a1' a2)\n  | leRight (v1 a2 a2' : Aexp) (hv : IsAValue v1) (h : AStep a2 a2') :\n      BStep (.le v1 a2) (.le v1 a2')\n  | le (n1 n2 : Nat) : BStep (.le (.num n1) (.num n2)) (.bool (decide (n1 ≤ n2)))\n  | gtLeft (a1 a1' a2 : Aexp) (h : AStep a1 a1') : BStep (.gt a1 a2) (.gt a1' a2)\n  | gtRight (v1 a2 a2' : Aexp) (hv : IsAValue v1) (h : AStep a2 a2') :\n      BStep (.gt v1 a2) (.gt v1 a2')\n  | gt (n1 n2 : Nat) : BStep (.gt (.num n1) (.num n2)) (.bool (decide (n1 > n2)))\n  | notStep (b1 b1' : Bexp) (h : BStep b1 b1') : BStep (.not b1) (.not b1')\n  | notTrue : BStep (.not (.bool true)) (.bool false)\n  | notFalse : BStep (.not (.bool false)) (.bool true)\n  | andStep (b1 b1' b2 : Bexp) (h : BStep b1 b1') : BStep (.and b1 b2) (.and b1' b2)\n  | andTrueStep (b2 b2' : Bexp) (h : BStep b2 b2') :\n      BStep (.and (.bool true) b2) (.and (.bool true) b2')\n  | andFalse (b2 : Bexp) : BStep (.and (.bool false) b2) (.bool false)\n  | andTrueTrue : BStep (.and (.bool true) (.bool true)) (.bool true)\n  | andTrueFalse : BStep (.and (.bool true) (.bool false)) (.bool false)\n\nscoped notation:40 b:41 \" ⟶b \" b':41 => BStep b b'\n\n\nA boolean example — the left comparison operand reduces first:\n\nexample :\n    (Bexp.le (.plus (.num 1) (.num 1)) (.num 3)) ⟶b (.le (.num 2) (.num 3)) :=\n  .leLeft _ _ _ (.plus 1 1)\n\n\nWhich of these properties does this small-step semantics for Slang\nexpressions satisfy?  (Yes or No for each.)* determinism* strong progress (every non-value takes a step)* values and normal forms coincide (i.e., there are no \"stuck\" terms)* the step relation is normalizing (i.e., evaluation always terminates)Yes to all four.  Expression evaluation always terminates, so ⟶a (and ⟶b) are normalizing.\n\nLet us make good on the first of those answers.  Both step relations are\ndeterministic: the value guards on the \"step the right operand\" rules mean\nthat at most one rule ever applies to a given term.\n\nThe arithmetic step relation is deterministic.  (Structurally this is the\nvalue-based determinism proof from the toy language, repeated for +, −,\nand ×; the impossible cross-cases close because a value num n cannot step.)theorem astep_deterministic : Deterministic AStep := by\n  sorry\n\n\nThe boolean step relation is deterministic too.  The comparison cases (eq,\nneq, le, gt) reduce their operands with ⟶a, so they inherit determinism\nfrom astep_deterministic; ¬ and the short-circuiting ∧ contribute only\nbase cases.theorem bstep_deterministic : Deterministic BStep := by\n  sorry\n\n\n","context":"Type Systems\u0009Smallstep: Small-step Operational Semantics\u0009Small-Step Slang","header":"3.5.2. Boolean Expressions","id":"/Smallstep/#Type-Systems--Smallstep___-Small-step-Operational-Semantics--Small-Step-Slang--Boolean-Expressions"}});