window.docContents[14].resolve({"/UsingLean/#Logical-Foundations--UsingLean___-Using-the-full-power-of-a-proof-assistant--Redefining-Functions-and-Lemmas-over-Nats":{"contents":"Now that we've switched over to using Lean's standard library, we can\nredefine some of the functions from the last few chapters on Nats.\nNote that, for the built-in Nat type, the patterns 0 and n + 1 correspond to\nzero and succ n. Likewise, the pattern n + 2 is equivalent to n + 1 + 1.Prove some of these theorems using the techniques we've discussed this chapter.\n\n\n\ndef Nat.even (n : Nat) :=\n  match n with\n  | 0     => true\n  | 1     => false\n  | n + 2 => even n\n\ndef Nat.odd (n : Nat) := !(even n)\n\ntheorem Nat.odd_def (n : Nat) : n.odd = !(n.even) := rfl\n\ndef Nat.minustwo (n : Nat) : Nat :=\n  match n with\n  | 0    => 0\n  | 1    => 0\n  | n' + 2 => n'\n\ndef Nat.double (n : Nat) : Nat :=\n  match n with\n  | 0    => 0\n  | n' + 1 => double n' + 2\n\n\nNote that we defined these functions in the Nat namespace;\nLean's naming conventions advise that functions on a type should be defined in that type's\nnamepsace in almost all circumstnaces.When we define functions this way,\nsomething interesting happens to the way Lean's InfoView prints them. Take a look at\nthe info view inside the proof of this thoerem (i.e., before the rfl tactic):\n\n\n\ntheorem Nat.even_add_three (n : Nat) : even (n + 3) = even (n + 1) := by\n  rfl\n\n\nInstead of printing the goal the way we wrote it in the theorem statement, Lean\nprints (n + 3).even = (n + 1).even! This is an example of Lean's field notation,\nwhereby Lean prints functions inside the namespace of a type after their first argument,\nseparated by a .. At first glance, this may appear similar to how object-oriented methods work,\nbut it's really just a syntactic variation on the normal fu8nction-application style\nwe've seen so far. That is, Nat.even n and n.even are just different ways to write the exact\nsame term.In previous chapters we disabled this notation by putting set_option pp.fieldNotation false\nat the top of each file, but from now on we will leave it enabled, since  field notation\nis recommended in idiomatic Lean developments.Cut this: \"It can also be disabled just for a specific function\nor constructor by writing attribute [pp_nodot] <Name>.\"  Do they need to know it in SFL?As an example, observe the difference in how Lean prints the goal in the following two examples:\n\n\n\nexample (n : Nat) : Nat.double (n + 0) = Nat.double n := by\n  rfl\n\nattribute [pp_nodot] Nat.double\n\nexample (n : Nat) : Nat.double (n + 0) = Nat.double n := by\n  rfl\n\n\ntheorem Nat.even_succ (n : Nat) :\n    (n + 1).even = !(n.even) := by\n  solution!\n    induction n with\n    | zero =>\n      rfl\n    | succ n' ih =>\n      rw [even, ih, Bool.not_not]\n\n\ntalk about using Nat.add_zero and friends from now on.\n\n(OA) : added lemmas proved for our Nat for Lean's Nat to prevent\n       later files from breaking.\n\ntheorem Nat.even_zero : even 0 = true := by rfl\n\ntheorem Nat.double_zero : double 0 = 0 := by rfl\n\ntheorem Nat.double_succ (n : Nat) : (n + 1).double = n.double + 2 := by rfl\n\n\ntheorem Nat.double_add (n : Nat) : n.double = n + n := by\n  solution!\n    induction n with\n    | zero =>\n      rw [double_zero]\n    | succ n' ih =>\n      rw [double_succ, ih, succ_add n' (n' + 1), add_succ n' n']\n\n\ntheorem Nat.double_mul (n : Nat) : n.double = 2 * n := by\n  solution!\n    rw [double_add, Nat.two_mul]\n\n\n\n\n","context":"Logical Foundations\u0009UsingLean: Using the full power of a proof assistant","header":"4.5. Redefining Functions and Lemmas over Nats","id":"/UsingLean/#Logical-Foundations--UsingLean___-Using-the-full-power-of-a-proof-assistant--Redefining-Functions-and-Lemmas-over-Nats"}});