window.docContents[91].resolve({"/Automation/#Logical-Foundations--Automation___-More-Automation--Case-Study___-Regular-Expressions--Examples":{"contents":"example : [1] =~ Char 1 := by\n  apply mChar\n\nexample : [1, 2] =~ App (Char 1) (Char 2):= by\n  apply mApp [1] <;> constructor\n\n\nNotice how the last example applies mApp to the string\n[1] directly.  Since the goal mentions [1, 2] instead of\n[1] ++ [2], Lean wouldn't be able to figure out how to split\nthe string on its own.Using inversion, we can also show that certain strings do not\nmatch a regular expression:\n\nexample : ¬([1, 2] =~ Char 1) := by\n  intro contra; inversion contra\n\n\nWe can define helper functions for writing down regular\nexpressions. The reg_exp_of_list function constructs a regular\nexpression that matches exactly the string that it receives as an\nargument:\n\ndef reg_exp_of_list {α} (l : List α) :=\n  match l with\n  | [] => EmptyStr\n  | x :: l' => App (Char x) (reg_exp_of_list l')\n\nexample : [1, 2, 3] =~ reg_exp_of_list [1, 2, 3] := by\n  apply mApp [1]; constructor\n  apply mApp [2]; constructor\n  apply mApp [3]; constructor\n  constructor\n\n\nAs a quick exercise, prove that every list matches reg_exp_of_list of itself:theorem regexp_match_of_list α (l : List α) : l =~ reg_exp_of_list l := by\n  sorry\n\n\nWe can also prove general facts about ExpMatch. For instance,\nthe following lemma shows that every string s matched by re\nis also matched by Star re.\n\n\n\ntheorem MStar1 α s (re : RegExp α) (h : s =~ re) : s =~ Star re := by\n  rw [← List.append_nil s]\n  constructor\n  . assumption\n  . constructor\n\n\n(Note the use of List.append_nil to change the goal of the theorem to\nexactly the shape expected by mStarApp.)\n\nThe following lemmas show that the intuition about matching given\nat the beginning of the section can be obtained from the formal\ninductive definition.\n\ntheorem EmptySet_is_empty α (s : List α) : ¬(s =~ EmptySet) := by\n  sorry\n\n\ntheorem MUnion' α (s : List α) (re₁ re₂ : RegExp α) :\n    s =~ re₁ ∨ s =~ re₂ →\n    s =~ Union re₁ re₂ := by\n  sorry\n\n\nThe next lemma is stated in terms of the fold function on Lists:\nIf ss : List (List α) represents a sequence of\nstrings s₁, ..., sₙ, then List.foldr (· ++ ·) ss [] is the result of\nconcatenating them all together.\n\ntheorem MStar' α (ss : List (List α)) (re : RegExp α)\n    (h : ∀ s, s ∈ ss → s =~ re) :\n    ss.foldr (· ++ ·) [] =~ Star re := by\n  sorry\n\n\nIt turns out that the EmptyStr constructor is actually not\nneeded, since the regular expression matching the empty string can\nalso be defined from Star and EmptySet:def EmptyStr' {α : Type} := @Star α (EmptySet)\nState and prove that this EmptyStr' definition matches exactly\nthe same strings as the EmptyStr constructor.\n\nSince the definition of ExpMatch has a recursive\nstructure, we might expect that proofs involving regular\nexpressions will often require induction on evidence.\n\n\n\nFor example, suppose we want to prove the following intuitive\nfact: If a string s is matched by a regular expression re,\nthen all elements of s must occur as character literals\nsomewhere in re.\n\nTo state this as a theorem, we first define a function re_chars\nthat lists all characters that occur in a regular expression:\n\ndef reChars {α : Type} (re : RegExp α) : List α :=\n  match re with\n  | EmptySet => []\n  | EmptyStr => []\n  | Char x => [x]\n  | App re₁ re₂ => reChars re₁ ++ reChars re₂\n  | Union re₁ re₂ => reChars re₁ ++ reChars re₂\n  | Star re => reChars re\n\n\nNow, the main theorem:\n\ntheorem in_re_match {α : Type} {s : List α} {re : RegExp α} {x : α}\n    (hmatch : s =~ re) (hin : x ∈ s) : x ∈ reChars re := by\n  induction hmatch with\n  | mEmpty => simp at hin\n  | mChar c => simp only [reChars]; assumption\n  | mApp _ _ _ _ ih₁ ih₂ =>\n\n  /- Something interesting happens in the `mApp` case.  We obtain\n    _two_ induction hypotheses: One that applies when `x` occurs in\n    `s₁` (which is matched by `re₁`), and a second one that applies when `x`\n    occurs in `s₂` (matched by `re₂`). -/\n    simp only [reChars, List.mem_append] at *\n    cases hin with\n    | inl hin₁ => left; exact ih₁ hin₁\n    | inr hin₂ => right; exact ih₂ hin₂\n  | mUnionL _ _ ih =>\n    simp only [reChars, List.mem_append]; left; exact ih hin\n  | mUnionR _ _ ih =>\n    simp only [reChars, List.mem_append]; right; exact ih hin\n  | mStar0 => simp at hin\n  | mStarApp _ _ _ _ ih₁ ih₂ =>\n\n  /- Here again we get two induction hypotheses, and they illustrate\n    why we need induction on evidence for `ExpMatch`, rather than\n    induction on the regular expression `re`: The latter would only\n    provide an induction hypothesis for strings that match `re`, which\n    would not allow us to reason about the case `In x ∈ s₂`. -/\n    simp only [List.mem_append] at hin\n    cases hin with\n    | inl hin₁ => exact ih₁ hin₁\n    | inr hin₂ => exact ih₂ hin₂\n\n\nWrite a recursive function reNotEmpty that tests whether a\nregular expression matches some string. Prove that your function\nis correct.\n\n","context":"Logical Foundations\u0009Automation: More Automation\u0009Case Study: Regular Expressions","header":"10.5.2. Examples","id":"/Automation/#Logical-Foundations--Automation___-More-Automation--Case-Study___-Regular-Expressions--Examples"},"/Basics/#Logical-Foundations--Basics___-Functional-Programming-in-Lean--A-First-Taste-of-Proofs":{"contents":"Now that we've defined some basic functions on booleans, let's see how to\nprove some simple properties of those functions. Here is a simple rule\nabout &&:For any boolean value b, (MyBool.true && b) = b\nThis is an example of a proposition, a logical claim that we can try to prove.\nIt says that MyBool.true && b is equal to b for every MyBool b.How do we write this proposition in Lean?  Like this:theorem true_and : ∀ (b : MyBool), (MyBool.true && b) = b\nThe keyword theorem indicates that we are stating (and eventually proving)\na proposition; the text after the first : is the proposition we want to prove.You'll notice that this proposition looks a lot like the informal one we began with,\nwith some additional symbols in front.\nThe ∀ symbol, pronounced \"forall\",\nis a universal quantifier: it \"quantifies\" the variable b that appears\nin the proposition. Quantifying a variable with a ∀ means that the proposition\napplies to all possible values of its type; we annotate b\nwith the type MyBool to signify that\nthe proposition holds for all bs of type MyBool.Now that we've stated the theorem we'd like to prove, let's see the proof.\n\n\n\ntheorem true_and : ∀ (b : MyBool), (MyBool.true && b) = b := by\n  intro b\n  rfl\n\n\nWhat does this mean?First the by keyword signals\nthat what follows is a sequence of tactics.\nThe intro b and rfl after the by\nare examples of tactics. If you hover over a tactic's name, Lean shows\nits documentation.Tactics manipulate the proof state, which you can see the in the Lean InfoView panel.\nThe proof state is divided by the symbol ⊢, pronounced turnstile. The part\nbefore it is the context, and the part after it is\nthe goal. The context records what we know\nat some given point in the proof; the goal is what we are trying to prove\nat that point.Each tactic manipulates the goal, the context, or both, to get things\ninto a configuration that is closer to being \"solved\". A tactic can also\nclose (solve) the current goal, finishing its proof.Let's walk through the example above with this terminology in mind.\n\n\n\ntheorem true_and_explained : ∀ (b : MyBool), (MyBool.true && b) = b := by\n  /- Move your cursor (click) here to see the initial proof state in\n     the InfoView. If you are viewing the book online,\n     instead click on the white button after `by`.\n     The context (before the ⊢) is empty.\n     The goal is `∀ (b : MyBool), (MyBool.true && b) = b`. -/\n  intro b\n  /- Now click here (or the white button after `intro b`)\n     to see the new proof state that results from the\n     tactic. Notice how `intro b` has changed the _context_: it now\n     contains `b : MyBool`.\n\n    The `intro` tactic is used to name variables quantified by a `∀`.\n    Since we are trying to prove a property of all `MyBools`, we\n    proceed by introducing an unknown `MyBool` `b` and prove\n    the property holds for `b`, regardless of what it is.  Informally,\n    this move can be read, \"We want to prove <some property> for all\n    `MyBool`s `b`. So suppose `b` is some arbitrary `MyBool`...\n    <and then go on to prove the property for this particular `b`>...\"\n    Since `b` was chosen arbitrarily, we've now proved the property\n    for all `b`.\n\n    A proof of a theorem beginning with a ∀ will typically start with\n    an `intro`.\n\n    As in the `example`s above, we can use the `rfl` tactic,\n    which closes goals about equality where both sides are equal to\n    one another according to the principle of reflexivity. Now,\n    inspecting our goal will show that it is `(MyBool.true && b) = b`,\n    which may not appear to be equal to itself. However, the tactic\n    _evaluates_ both sides of the equality before comparing them. In\n    this case, if we look at the definition of `and`, we can see that,\n    when its first argument is `MyBool.true`, the result is its second\n    argument. So the two terms `MyBool.true && b` and `b` are in fact\n    equal because one evaluates to the other.\n  -/\n  rfl\n  /- The proof is now done! The Lean InfoView tells us there are \"No goals\". -/\n\n\nIt's also important to point out that, as with languages like Python and Haskell,\nLean is whitespace-sensitive. That is, the indentation in proofs is important and changing\nit can change the meaning of the proof, usually causing the proof to break. Suppose we had\ninstead written the following:theorem true_and_wrong : ∀ (b : MyBool), (MyBool.true && b) = b := by\n  intro b\n    rfl\nTo see the error message in the Lean file,\nchange sf_expect_failure_in to sf_expect_failure_in? temporarily.\nYou should see the following message.Tactic `introN` failed: There are no additional binders or `let` bindings in the goal to introduce\n\nb : MyBool\n⊢ (true && b) = b\nLean complains because the rfl is not at the same level of indentation as the {tactic}intro b,\nso it does not recognize these two tactics as being sequential in the way they should be.In general, sequential tactics applied to the same goal must be on subsequent lines at the same\nlevel of indentation or separated on the same line by a ; like so:theorem true_and' : ∀ (b : MyBool), (MyBool.true && b) = b := by\n  intro b; rfl\n\n\nHere's a simple proof for you to try.\nRemove sorry and fill in the proof.theorem false_or : ∀ (b : MyBool), (MyBool.false || b) = b := by\n  sorry\nWhile in this book we often use sorry as a placeholder for you to\nreplace with an actual proof, in general, sorry tells Lean that we want to skip trying\nto prove a theorem and just accept it as a given.  This can be useful for developing longer proofs.Be careful, though: every time you say sorry you are leaving\na door open for total nonsense to enter Lean's safe, formally\nchecked world!theorem really_bad : MyBool.true = MyBool.false := by sorry\nThe facts we've seen so far about booleans are quite simple, so the tactics we need to\nprove them are also quite simple. Over the course of this book we are going to\nintroduce new tactics and proof techniques gradually, enriching the propositions we can prove along the way.Now that we've seen how to define our own booleans and prove some basic\nproperties about them, let's switch to Lean's built-in Bool type, which has the same structure\nbut comes with a lot of useful functions and lemmas.\n\n\n\nend MyBool\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n","context":"Logical Foundations\u0009Basics: Functional Programming in Lean","header":"2.3. A First Taste of Proofs","id":"/Basics/#Logical-Foundations--Basics___-Functional-Programming-in-Lean--A-First-Taste-of-Proofs"},"/Poly/#Logical-Foundations--Poly___-Polymorphism-and-Higher-Order-Functions--Polymorphism--Polymorphic-Options":{"contents":"Our last polymorphic type for now is polymorphic options.\nLean's standard library provides Option α, with constructors\nnone and some. (We already saw NatOption in the Lists chapter.)\nLet's briefly look at the definition:namespace OptionPlayground\n\ninductive Option (α : Type) : Type where\n  | none : Option α\n  | some (x : α) : Option α\n\nend OptionPlayground\n\n\n\n\nWe can now rewrite the nth? function so that it works\nwith any type of list.\n\ndef nth? {α : Type} (l : List α) (n : Nat) : Option α :=\n  match l with\n  | [] => none\n  | x :: l' => match n with\n    | 0 => some x\n    | n' + 1 => nth? l' n'\n\n\nexample : nth? [4, 5, 6, 7] 0 = some 4 := by rfl\nexample : nth? [[1], [2]] 1 = some [2] := by rfl\nexample : nth? [true] 2 = none := by rfl\n\n\nComplete the definition of a polymorphic version of the\nhead? function from the last chapter. Be sure that it\npasses the unit tests below.def head? {α : Type} (l : List α) : Option α := sorry\n\ntheorem head?_nil {α : Type} : head? ([] : List α) = none := sorry\n\ntheorem head?_cons {α : Type} {head : α} {tail : List α} : head? (head :: tail) = some head :=\n  sorry\ntheorem test_head?1 : head? [1, 2] = some 1 := sorry\n\ntheorem test_head?2 : head? [[1], [2]] = some [1] := sorry\n\n\n","context":"Logical Foundations\u0009Poly: Polymorphism and Higher-Order Functions\u0009Polymorphism","header":"6.1.3. Polymorphic Options","id":"/Poly/#Logical-Foundations--Poly___-Polymorphism-and-Higher-Order-Functions--Polymorphism--Polymorphic-Options"}});