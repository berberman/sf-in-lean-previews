window.docContents[59].resolve({"/Induction/#Logical-Foundations--Induction___-Proof-by-Induction--More-Exercises--Aside___-Using-Code-Actions-to-Generate-Match-Skeletons":{"contents":"Lean's language server can suggest code actions, which are\nsmall editor commands that modify the source code.\nIn VS Code, a lightbulb icon appears on the left\nwhen a code action is available at your cursor.\nYou can click the icon or open the code action menu with Ctrl + .\non Windows/Linux or Command + . on macOS.\nFor more information, see the\nLean 4 VSCode extension manual.\n\nSome code actions can generate the explicit branches needed for pattern\nmatching. This is especially useful when working with match expressions,\nor with tactics such as cases and induction,\nwhich we saw in previous chapters.\n\nLet's look at an example using induction.\nFor example, suppose we start with the following incomplete proof:example (n : Nat) : Nat.beq n n := by\n  induction n\nPut your cursor on induction n and open the code action menu.\nYou should see\n\"Generate an explicit pattern match for 'induction'.\" in the list.\nIf you choose this action,\nLean adds an explicit branch for each constructor:example (n : Nat) : Nat.beq n n := by\n  induction n with\n  | zero => sorry\n  | succ n _ => sorry\nThis gives us basic structure of the proof without requiring us to write each\nbranch by hand. We can then focus on proving each case.One possible proof is:example (n : Nat) : Nat.beq n n := by\n  induction n with\n  | zero => exact (beq_self zero)\n  | succ n ih => rw [Nat.beq, ih]\nNote that Lean used _ for the induction hypothesis in the generated succ branch.\nAt that point, Lean didn't know whether the unfinished proof would need to refer to the hypothesis.\nSince we use it in rw, we replace _ with the name ih.In later chapters, we will see some tactics that can make such\ninaccessible names available again.The same trick also works for match expressions.\nFor example, suppose we start withdef isZero (n : Nat) : Bool :=\n  match n\nLean can generate the missing branches:def isZero (n : Nat) : Bool :=\n  match n with\n  | 0 => _\n  | n + 1 => _\n\n\nYou can use code actions freely to fill out induction,\ncase, and match branches in this book.\n\nTip: By default, rewrite and rw rewrite left to right, i.e.,\nthey transform the hypothesis or goal being rewritten from the form on\nthe left side of the equality to the right side. To rewrite from\nright to left, use rewrite [← h] or rw [← h], where ← is entered\nas \\l or \\<-.theorem mul_two (p : Nat) :\n    two * p = p + p := by\n  solution!\n    induction p with\n    | zero => rw [mul_zero, add_zero]\n    | succ p' ih =>\n      rw [mul_succ, ih, two_eq_succ_one, succ_eq_add_one, succ_eq_add_one]\n      rw [add_assoc, add_assoc, ←add_assoc p' p' one]\n      rw [add_comm p' one, add_comm p']\n\n\n\n\nUse have (or rw with explicit arguments) to help prove\nadd_shuffle3. You don't need to use induction.theorem add_shuffle3 (n m p : Nat) :\n    add (add n m) p = add (add n p) m := by\n  solution!\n    rw [← add_assoc, add_comm m p, add_assoc]\ntheorem succ_mul (m n : Nat) :\n    (succ n) * m = (n * m) + m := by\n  solution!\n    induction m with\n    | zero => rw [mul_zero, mul_zero, add_zero]\n    | succ m ih =>\n      rw [mul_succ, ih, add_succ, add_comm _ n,\n          add_assoc n _ m, add_comm n, mul_succ, add_succ]\nNow prove commutativity of multiplication.theorem mul_comm (m n : Nat) :\n    m * n = n * m := by\n  solution!\n    induction n with\n    | zero =>\n      rw [mul_zero, zero_mul]\n    | succ n' ih =>\n      rw [mul_succ, ih, succ_mul]\n\n\nTake a piece of paper.  For each of the following theorems, first\nthink about whether (a) it can be proved using only\nsimplification and rewriting, (b) it also requires case\nanalysis (cases), or (c) it also requires induction.  Write\ndown your prediction.  Then fill in the proof.  (There is no need\nto turn in your piece of paper; this is just to encourage you to\nreflect before you hack!)theorem ble_refl (n : Nat) :\n    Nat.ble n n = true := by\n  solution!\n    induction n with\n    | zero       => rw [zero_ble]\n    | succ n' ih => rw [succ_ble_succ]; exact ih\n\ntheorem andb_false (b : Bool) :\n    (b && false) = false := by\n  solution!\n    cases b with\n    | false => rw [Bool.false_and]\n    | true  => rw [Bool.true_and]\n\ntheorem all3_spec (b c : Bool) :\n    ((b && c) || ((!b) || (!c))) = true := by\n  solution!\n    cases b with\n    | true => cases c with\n      | false => rfl\n      | true => rfl\n    | false => rfl\n\ntheorem right_distrib (n m p : Nat) :\n    (n + m) * p = (n * p) + (m * p) := by\n  solution!\n    induction p with\n    | zero => rw [mul_zero, mul_zero, mul_zero, add_zero]\n    | succ p' ih =>\n      rw [mul_succ, mul_succ, mul_succ, ih]\n      rw [add_assoc ((n * p') + (m * p')),\n          add_shuffle3 (n * p') (m * p'),\n          add_assoc ((n * p') + n)]\n\ntheorem left_distrib (n m p : Nat) :\n    p * (n + m) = (p * n) + (p * m) := by\n  solution!\n    rw [mul_comm p, mul_comm p, mul_comm p]\n    rw [right_distrib]\n\ntheorem mul_assoc (n m p : Nat) :\n    n * (m * p) = (n * m) * p := by\n  solution!\n    induction p with\n    | zero       => rw [mul_zero, mul_zero, mul_zero]\n    | succ p' ih => rw [mul_succ, mul_succ, ← ih, left_distrib]\n\n\n","context":"Logical Foundations\u0009Induction: Proof by Induction\u0009More Exercises","header":"3.6.1. Aside: Using Code Actions to Generate Match Skeletons","id":"/Induction/#Logical-Foundations--Induction___-Proof-by-Induction--More-Exercises--Aside___-Using-Code-Actions-to-Generate-Match-Skeletons"},"/UsingLean/#Logical-Foundations--UsingLean___-Using-the-Full-Power-of-a-Proof-Assistant--Structuring-Proofs-with--calc":{"contents":"In Lean proofs, long rw chains are useful, but they are sometimes\nhard to read because the intermediate goals are invisible. Furthermore,\nsometimes we know exactly how we want to manipulate the terms of a proof, but\ndon't want to have the tactics like Nat.add_comm and\nNat.add_assoc \"guess\" which subterms to rewrite.\n\nThe calc tactic writes down the intermediate goals of a proof, and\nallows us to specify exactly which rewrite rules to apply at each step. It is designed\nto mimic the style of proofs in mathematics textbooks, which will often look something like this:\n\nn + (m + k)\n= (n + m) + k        ...   [by associativity of addition]\n= (m + n) + k        ...   [by commutativity of addition]\n= m + (n + k)        ...   [by associativity of addition]\n\n\nNote how we can see each intermediate step of this proof when we\nlook at it this way. Let's look at how we might prove this theorem\n(i.e., that n + (m + k) = m + (n + k)) in Lean.\n\nFirst, a proof in the style we already know.\n\nexample (n m k : Nat) : n + (m + k) = m + (n + k) := by\n  rw [← Nat.add_assoc, Nat.add_comm n m, Nat.add_assoc]\n\n\nHere we present the same theorem, written with calc.\nNote how each intermediate goal is visible in the source.\n\nexample (n m k : Nat) : n + (m + k) = m + (n + k) := by\n  calc n + (m + k) /- one side of the goal is the argument to `calc`...\n       ... and each subsequent line is a transformation, with a tactic. -/\n    n + (m + k) = (n + m) + k := by rw [Nat.add_assoc]\n    (n + m) + k = (m + n) + k := by rw [Nat.add_comm n m]\n    /- once a line matches the other side of the equality in the main goal\n       (in this case `m + (n + k)`), the calc tactic succeeds. -/\n    (m + n) + k = m + (n + k) := by rw [Nat.add_assoc]\n\n\nWe can also write the proof like this to be a bit more concise:\n\nexample (n m k : Nat) : n + (m + k) = m + (n + k) := by\n  calc n + (m + k)\n    _ = (n + m) + k := by rw [Nat.add_assoc]\n    _ = (m + n) + k := by rw [Nat.add_comm n m]\n    _ = m + (n + k) := by rw [Nat.add_assoc]\n\n\nWhereas before, the left-hand side of each equality in the\ncalc tactic was repeated from the right-hand side of the\nprevious one, we can replace the left-hand side entirely with an _.\nNow our Lean proof looks quite a bit like the textbook one we saw earlier!\n\ntheorem succ_mul_succ (n m : Nat) :\n    (n + 1) * (m + 1) = n * m + n + m + 1 := by\n  rw [Nat.add_mul, Nat.one_mul, Nat.mul_add, Nat.mul_one, ← Nat.add_assoc]\nGiven this proof with rw, rewrite it with calc.\nRecall that you can use rw? to find appropriate rules to rewrite by.theorem succ_mul_succ' (n m : Nat) :\n    (n + 1) * (m + 1) = n * m + n + m + 1 := by\n  solution!\n    calc (n + 1) * (m + 1)\n      _ = n * (m + 1) + 1 * (m + 1) := by rw [Nat.add_mul]\n      _ = n * (m + 1) + (m + 1)     := by rw [Nat.one_mul]\n      _ = (n * m + n * 1) + (m + 1) := by rw [Nat.mul_add]\n      _ = (n * m + n) + (m + 1)     := by rw [Nat.mul_one]\n      _ = n * m + n + m + 1         := by rw [← Nat.add_assoc]\nIf you prefer rw to calc, that's fine! Each has particular\nuses, and both will be tools in your ever-growing toolbox of tactics.\n\n","context":"Logical Foundations\u0009UsingLean: Using the Full Power of a Proof Assistant","header":"4.3. Structuring Proofs with  calc","id":"/UsingLean/#Logical-Foundations--UsingLean___-Using-the-Full-Power-of-a-Proof-Assistant--Structuring-Proofs-with--calc"}});