window.docContents[103].resolve({"/UsingLean/#Logical-Foundations--UsingLean___-Using-the-Full-Power-of-a-Proof-Assistant--Definitional-Simplification___--dsimp":{"contents":"Often, rather than repeatedly rewriting by a known equation like\nrw [Nat.mul_zero, Nat.mul_zero] to solve a goal like\nn * (m * 0) = 0,\nwe just want to simplify the function (here Nat.mul) automatically when we can.\n\nThe dsimp tactic (\"definitionally simplify\") unfolds definitions\nand performs definitional simplifications. You can give it hints in\nsquare brackets: dsimp [f] tells it to unfold the definition of f.\nYou can also simplify a hypothesis h in the context by writing\ndsimp [...] at h. dsimp will also close goals by rfl when possible.\n\ndef square (n : Nat) : Nat := n * n\n\ndef triple (n : Nat) : Nat := n + n + n\n\n\nWhen the goal depends on a fact about an unknown value, rfl fails.\nHere, dsimp makes progress, exposing a goal the fact can close.\n\nexample (n m : Nat) (h : n + n = m) : triple n = m + n := by\n  -- rfl will not work here!\n  dsimp [triple]\n  -- The goal can now be rewritten by `h`.\n  rw [h]\n\n\nAs we have seen, rw can also unfold definitions. In this example,\neither style is fine: use dsimp [triple] when you want to emphasize\ndefinitional simplification, or rw [triple, h] when the proof is just\na sequence of rewrites.\n\nexample (n m : Nat) (h : n + n = m) : triple n = m + n := by\n  -- `rw [triple]` unfolds `triple n`.\n  rw [triple, h]\n\n\nComplete this proof, using dsimp or rw as appropriate.example (n m : Nat) (h : m = n) : triple m = n + (n + n) := by\n  sorry\n\n\ndsimp at h also works on hypotheses, which rfl can't touch.\n\nexample (n : Nat) (h : square n = 16) : n * n = 16 := by\n  dsimp [square] at h\n  exact h\n\n\nAside: rw [...] at h also works on hypotheses too, as does rw? at h\n\nexample (n m : Nat) (h : 2 * n = m * 2) : n + n = m + m := by\n  rw [Nat.mul_comm, Nat.mul_two, Nat.mul_two] at h\n  exact h\n\n\nBut rw rewrites only one instance of a definition at a time.\nWhen a hypothesis mentions the same function at several different\narguments, each one needs its own rewrite.\n\nexample (n m k : Nat) (h : square n + square m + square k = 0) :\n    n * n + m * m + k * k = 0 := by\n  rw [square, square, square] at h\n  exact h\n\n\ndsimp unfolds every instance at once, so one hint suffices no\nmatter how many times the definition appears.\n\nexample (n m k : Nat) (h : square n + square m + square k = 0) :\n    n * n + m * m + k * k = 0 := by\n  dsimp [square] at h\n  exact h\n\n\ndsimp also takes definitional steps such as + 0,\nso it can finish goals that rfl would close.\n\nexample (n : Nat) : square n + 0 = n * n := by\n  dsimp [square]\n\n\nLike rw and exact, dsimp also has a ? version\nthat searches for functions to simplify by. Many Lean tactics have ?\nversions; try it out if you are unsure.\n\n\n\n","context":"Logical Foundations\u0009UsingLean: Using the Full Power of a Proof Assistant","header":"4.4. Definitional Simplification:  dsimp","id":"/UsingLean/#Logical-Foundations--UsingLean___-Using-the-Full-Power-of-a-Proof-Assistant--Definitional-Simplification___--dsimp"}});