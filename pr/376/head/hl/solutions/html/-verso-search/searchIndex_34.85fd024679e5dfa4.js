window.docContents[34].resolve({"/Slang/#Hoare-Logic--Slang___-Arithmetic-and-Boolean-Expressions--Arithmetic-and-Boolean-Expressions--Optimization":{"contents":"We can now start to get some mileage out of these definitions. Suppose we define a\nfunction that takes an arithmetic expression and slightly simplifies it, changing\nevery occurrence of 0 + e (i.e., .plus (.num 0) e) into just e.\n\nnamespace Aexp\n\ndef optimize0plus (a : Aexp) : Aexp :=\n  match a with\n  | num   n          => num n\n  | plus  (num 0) e₂ => optimize0plus e₂\n  | plus  e₁      e₂ => plus  (optimize0plus e₁) (optimize0plus e₂)\n  | minus e₁      e₂ => minus (optimize0plus e₁) (optimize0plus e₂)\n  | mult  e₁      e₂ => mult  (optimize0plus e₁) (optimize0plus e₂)\n\n\nTo gain confidence that our optimization is doing the right thing, we\ncan test it on some examples and see if the output looks OK.\n\nexample :\n    Aexp.optimize0plus (.plus (.num 2)\n                               (.plus (.num 0)\n                                      (.plus (.num 0) (.num 1))))\n      = .plus (.num 2) (.num 1) := by rfl\n\n\nBut if we want to be certain the optimization is correct -- that\nevaluating an optimized expression always gives the same result as\nthe original -- we should prove it!Here is a first, deliberately explicit, proof, by induction on a. The\ninteresting case is Aexp.plus: because Aexp.optimize0plus treats plus (num 0) e\nspecially, we case-split on the left operand a₁ -- and, when it is a numeral,\non whether that numeral is 0 -- to line the proof up with the function's own\nbranches. Once the constructors are exposed, each case is discharged by\nessentially the same incantation: unfold Aexp.optimize0plus, rewrite Aexp.eval by its\ncharacterizing lemmas, then finish with the induction hypotheses. Notice how\nrepetitive that makes the proof.\n\ntheorem optimize0plus_sound (a : Aexp) :\n    a.optimize0plus.eval = a.eval := by\n  induction a with\n  | num n => rfl\n  | plus a₁ a₂ ih₁ ih₂ =>\n    cases a₁ with\n    | num n =>\n      cases n with\n      | zero =>\n        simp only [optimize0plus, eval_plus, eval_num, Nat.zero_add]\n        exact ih₂\n      | succ n =>\n        simp only [optimize0plus, eval_plus, eval_num]\n        rw [ih₂]\n    | plus b₁ b₂ =>\n      simp only [optimize0plus, eval_plus] at ih₁ ⊢\n      rw [ih₁, ih₂]\n    | minus b₁ b₂ =>\n      simp only [optimize0plus, eval_plus] at ih₁ ⊢\n      rw [ih₁, ih₂]\n    | mult b₁ b₂ =>\n      simp only [optimize0plus, eval_plus] at ih₁ ⊢\n      rw [ih₁, ih₂]\n  | minus a₁ a₂ ih₁ ih₂ =>\n    simp only [optimize0plus, eval_minus]\n    rw [ih₁, ih₂]\n  | mult a₁ a₂ ih₁ ih₂ =>\n    simp only [optimize0plus, eval_mult]\n    rw [ih₁, ih₂]\n\n\nWe can do much better. The case analysis we performed by hand -- peeling\nplus apart to reach the plus (num 0) e branch -- is exactly the case\nanalysis that Aexp.optimize0plus itself performs.The fun_induction tactic\ninducts along a function's own recursion structure: fun_induction\nAexp.optimize0plus a hands us one goal per branch of optimize0plus -- the\nspecial plus (num 0) e branch included -- so the nested cases disappear.Before applying fun_induction to a function as complex as Aexp.optimize0plus,\nlet's see how it works on somthing simpler. Recall the definition of Nat.even and Nat.odd:def Nat.even (n : Nat) :=\n  match n with\n  | 0 => true\n  | 1 => false\n  | n' + 2 => even n'\n\ndef Nat.odd (n : Nat) := Nat.even (n + 1)\nNormally, if we perform induction on n, we get two cases - 0 and n' + 1 -\none for each of the cases in the inductive definition of natural numbers.\nFunctional induction on Nat.even, however, gives us three cases - 0, 1, and n' + 2 -\ncorresponding to each of the cases of its definition.example (n : Nat) (h : Nat.even n = true) : Nat.odd n = false := by\n  fun_induction Nat.even n\n  . rfl\n  . contradiction\n  . simp [Nat.odd, Nat.even] at *\n    lia\nNow let's try using fun_induction on Aexp.optimize0plus. When we do this,\nevery goal has the same shape, so we can attack them uniformly\nwith the <;> combinator and a single tactic, simp_all, which rewrites\nAexp.eval by the @[simp] characterizing lemmas and uses the induction hypotheses\n-- which it picks up from the local context automatically -- to close\neach goal. The whole proof collapses to two lines.\n\n\n\ntheorem optimize0plus_sound' (a : Aexp) :\n    a.optimize0plus.eval = a.eval := by\n  fun_induction Aexp.optimize0plus a <;> simp_all\n\nend Aexp\n\n\nSince the Aexp.optimize0plus transformation doesn't change the value of an\nAexp, we should be able to apply it to all the Aexps that appear in a\nBexp without changing the Bexp's value.  Write a function that\nperforms this transformation on Bexps and prove it sound. Use the\ncombinators we've just seen to make the proof as short and elegant as\npossible.def Bexp.optimize0plus (b : Bexp) : Bexp := solution!(\n  match b with\n  | bool b    =>  bool b\n  | eq a₁ a₂  =>  eq a₁.optimize0plus a₂.optimize0plus\n  | neq a₁ a₂ =>  neq a₁.optimize0plus a₂.optimize0plus\n  | le a₁ a₂  =>  le a₁.optimize0plus a₂.optimize0plus\n  | gt a₁ a₂  =>  gt a₁.optimize0plus a₂.optimize0plus\n  | not b₁    =>  not (optimize0plus b₁)\n  | and b₁ b₂ =>  and (optimize0plus b₁) (optimize0plus b₂))\n\ntheorem Bexp.optimize0plus_test1 :\n    Bexp.optimize0plus\n        (.not (.gt (.plus (.num 0) (.num 4)) (.num 8)))\n      = (.not (.gt (.num 4) (.num 8))) := solution!(by rfl)\ntheorem Bexp.optimize0plus_test2 :\n    Bexp.optimize0plus\n        (.and (.le (.plus (.num 0) (.num 4)) (.num 5)) (.bool true))\n      = (.and (.le (.num 4) (.num 5)) (.bool true)) := solution!(by rfl)\ntheorem Bexp.optimize0plus_sound (b : Bexp) :\n    b.optimize0plus.eval = b.eval := by\n  solution!\n    fun_induction Bexp.optimize0plus b <;> simp_all [Aexp.optimize0plus_sound]\n\n\nThe optimization implemented by our Aexp.optimize0plus is only one of\nmany possible optimizations on arithmetic and boolean expressions. Write a more\nsophisticated optimizer and prove it correct. (You will probably find it easiest\nto start small -- add just a single, simple optimization and its correctness proof --\nand build up incrementally to something more interesting.)\n\n","context":"Hoare Logic\u0009Slang: Arithmetic and Boolean Expressions\u0009Arithmetic and Boolean Expressions","header":"2.1.3. Optimization","id":"/Slang/#Hoare-Logic--Slang___-Arithmetic-and-Boolean-Expressions--Arithmetic-and-Boolean-Expressions--Optimization"}});