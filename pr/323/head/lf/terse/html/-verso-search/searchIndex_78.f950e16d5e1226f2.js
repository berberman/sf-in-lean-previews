window.docContents[78].resolve({"/Basics/#Logical-Foundations--Basics___-Functional-Programming-in-Lean--A-First-Taste-of-Proofs--New-Types-from-Old":{"contents":"\n\nA more interesting type definition:\n\ninductive RGB : Type where\n  | red\n  | green\n  | blue\n\ninductive Color : Type where\n  | black\n  | white\n  | primary (p : RGB)\n\n\n\n\n\n\nWe can define functions on colors using pattern matching, just as\nwe did for Day and Bool.\n\ndef monochrome (c : Color) : Bool :=\n  match c with\n  | Color.black => Bool.true\n  | Color.white => Bool.true\n  | Color.primary p => Bool.false\n\n\n\n\nWe can use a wildcard pattern _ to match something we don't care about:\n\ndef monochrome' (c : Color) : Bool :=\n  match c with\n  | Color.black => Bool.true\n  | Color.white => Bool.true\n  | Color.primary _ => Bool.false\n\n\nWe can use a constant argument to Color.primary to match a specific primary color:\n\ndef isRed (c : Color) : Bool :=\n  match c with\n  | Color.black => Bool.false\n  | Color.white => Bool.false\n  | Color.primary RGB.red => Bool.true\n  | Color.primary _ => Bool.false\n\n\nvariable (c : Color) (r : RGB)\n\n\n\n\nAn alternative way to write the same function would be to explicitly\nnest match statements:\n\ndef isRed' (c : Color) : Bool :=\n  match c with\n  | Color.black => Bool.false\n  | Color.white => Bool.false\n  | Color.primary r =>\n    match r with\n    | RGB.red => Bool.true\n    | _ => Bool.false\n\n\nThis isRed' function produces the same result as\nisRed but illustrates the use of a pattern variable.\n\n\n\n","context":"Logical Foundations\u0009Basics: Functional Programming in Lean\u0009A First Taste of Proofs","header":"2.3.3. New Types from Old","id":"/Basics/#Logical-Foundations--Basics___-Functional-Programming-in-Lean--A-First-Taste-of-Proofs--New-Types-from-Old"},"/Logic/#Logical-Foundations--Logic-in-Lean--Logical-Connectives--Falsehood-and-Negation":{"contents":"Up to this point, we have mostly been concerned with proving\n\"positive\" statements — addition is commutative, appending lists\nis associative, etc.  We are sometimes also interested in negative\nresults, demonstrating that some proposition is not true. Such\nstatements are expressed with the logical negation operator ¬,\nwhich is a prefix notation for Not.\n\nTo see how negation works, recall the principle of explosion\nfrom the Tactics chapter, which asserts that, if we assume a\ncontradiction, then any other proposition can be derived.\n\nFollowing this intuition, we could define ¬ a (\"not a\") as\n∀ c, a → c.\nLean makes an equivalent but slightly different choice,\ndefining ¬ a as a → False, where False is a specific\nunprovable proposition defined in the standard library.\n\n#check Not\n#print Not\n\nexample (a : Prop) : Not a = (a → False) := rfl\nexample (a : Prop) : (¬ a) = (a → False) := rfl\n\n\nNot (a : Prop) : Prop\n\n\n@[implicit_reducible] def Not : Prop → Prop :=\nfun a => a → False\n\n\nSince False is a contradictory proposition, the principle of\nexplosion also applies to it. If we can get False into the context,\nwe can use cases on it to complete any goal:\n\ntheorem ex_falso_quodlibet (a : Prop) (h : False) : a := by\n  cases h\n\n\n\n\n\n\nInequality is a very common form of negated statement, so there is a\nspecial notation for it: ≠, which is infix notation for Ne.\n\n#print Ne\n\n\n@[reducible] def Ne.{u} : {α : Sort u} → α → α → Prop :=\nfun {α} a b => ¬a = b\n\n\ntheorem zero_not_one : 0 ≠ 1 := by\n  /- The proposition `0 ≠ 1` is exactly the same as `¬ (0 = 1)`\n      — that is, `Not (0 = 1)` — which unfolds to `(0 = 1) → False`. -/\n  /- To prove an inequality, we may assume the opposite equality... -/\n  intro contra\n  /- ...and deduce a contradiction from it. Here, the equality\n      `0 = 1` corresponds to `zero = succ zero`, which contradicts\n      disjointness of constructors `zero` and `succ`, so `contradiction`\n      takes care of it. -/\n  contradiction\n\n\nIt takes a little practice to get used to working with negation in Lean.\nEven though you may see perfectly well why a claim involving\nnegation holds, it can be a little tricky at first to see how to make\nLean understand it!\n\nHere are proofs of a few familiar facts to help get you warmed up.\n\ntheorem not_False : ¬ False := by\n  intro h; exact h\n\ntheorem contradiction_implies_anything (a b : Prop) (h : a ∧ ¬ a) : b := by\n  sorry\n\ntheorem double_neg (a : Prop) (ha : a) : ¬ ¬ a := by\n  sorry\n\n\n\n\nSince inequality involves a negation, getting comfortable\nwith it also often requires a little practice.A useful trick: if you are trying to prove a nonsensical goal,\napply ex_falso_quodlibet to change the goal to False. This\nmakes it easier to use assumptions of the form ¬ a, and in\nparticular of the form x ≠ y.\n\n\n\ntheorem not_true_is_false (b : Bool) (h : b ≠ true) : b = false := by\n  cases b with\n  | false => rfl\n  | true =>\n    rw [Ne, Not] at h\n    apply ex_falso_quodlibet\n    apply h\n    rfl\n\n\n\n\nTo prove the following proposition, which tactics will we need\nbesides intro, apply, and exact?∀ α : Type, ∀ x y : α, x = y ∧ x ≠ y → False\n1. intro, apply, and exact suffice2. cases3. left and/or right4. cases and left and/or right5. none of the aboveexample (α : Type) (x y : α) : x = y ∧ x ≠ y → False := by\n  intro h; cases h with\n  | _ h₁ h₂ => apply h₂; exact h₁\n\n\nTo prove the following proposition, which tactics will we need\nbesides intro, apply, and exact?∀ a b : Prop, a ∨ b → ¬ ¬ (a ∨ b)\n1. intro, apply, and exact suffice2. cases3. left and/or right4. cases and left and/or right5. none of the aboveexample (a b : Prop) (h : a ∨ b) : ¬ ¬ (a ∨ b) := by\n  intro hn; apply hn; exact h\n\n\nTo prove the following proposition, which tactics will we need\nbesides intro, apply, and exact?∀ a b : Prop, a → (a ∨ ¬ ¬ b)\n1. intro, apply, and exact suffice2. cases3. left and/or right4. cases and left and/or right5. none of the aboveexample (a b : Prop) (h : a) : a ∨ ¬ ¬ b := by\n  left; exact h\n\n\nTo prove the following proposition, which tactics will we need\nbesides intro, apply, and exact?∀ a b : Prop, a ∨ b → (¬ ¬ a) ∨ (¬ ¬ b)\n1. intro, apply, and exact suffice2. cases3. left and/or right4. cases and left and/or right5. none of the aboveexample (a b : Prop) (h : a ∨ b) : (¬ ¬ a) ∨ (¬ ¬ b) := by\n  cases h with\n  | inl ha => left; intro hna; apply hna; exact ha\n  | inr hb => right; intro hnb; apply hnb; exact hb\n\n\nTo prove the following proposition, which tactics will we need\nbesides intro, apply, and exact?∀ a : Prop, 1 = 0 → (a ∨ ¬ a)\n1. intro, apply, and exact suffice2. contradiction3. left and/or right4. contradiction and left and/or right5. none of the aboveexample (a : Prop) (h : 1 = 0) : (a ∨ ¬ a) := by\n  contradiction\n\n\n","context":"Logical Foundations\u0009Logic in Lean\u0009Logical Connectives","header":"8.2.3. Falsehood and Negation","id":"/Logic/#Logical-Foundations--Logic-in-Lean--Logical-Connectives--Falsehood-and-Negation"},"/Typeclasses/#Logical-Foundations--Typeclasses--Why-We-Need-Typeclasses":{"contents":"Consider the following function, which checks whether a natural number occurs in a list:\n\ndef List.elem_nat (a : Nat) (xs : List Nat) : Bool :=\n  match xs with\n  | [] => false\n  | b :: tl => bif a == b then true else elem_nat a tl\n\ntheorem List.elem_nat_nil (a : Nat) : [].elem_nat a = false := rfl\n\ntheorem List.elem_nat_cons (a b : Nat) (xs : List Nat) :\n    (b :: xs).elem_nat a = bif a == b then true else elem_nat a xs := rfl\n\n\n#eval [0, 1].elem_nat 0\n#eval [0, 1].elem_nat 1\n#eval [0, 1].elem_nat 2\n\n\nWhat if we want this to work for lists of any element type, not just Nat? Parametric\npolymorphism suggests simply replacing Nat with a type variable α, but that produces a puzzling\nerror:\n\ndef List.elem_poly {α : Type} (a : α) (xs : List α) : Bool :=\n  match xs with\n  | [] => false\n  | b :: tl => bif a == b then true else elem_poly a tl\n\n\nfailed to synthesize instance of type class\n  BEq α\n\nHint: Type class instance resolution failures can be inspected with the `set_option trace.Meta.synthInstance true` command.\n\n\nLean is trying to use typeclasses to work out how == should behave on a value of type α.\nWe'll see exactly why shortly; for now, here's one way to sidestep the problem: have the\ncaller supply the equality test to use.\n\ndef List.elem_poly_eq {α : Type} (eq : α → α → Bool) (a : α) (xs : List α) : Bool :=\n  match xs with\n  | [] => false\n  | b :: tl => bif eq a b then true else elem_poly_eq eq a tl\n\n#eval [0, 1].elem_poly_eq Nat.beq 0\n\n\nThis works, but it's tedious: every caller has to know, and remember to supply, the right equality\nfunction.\n\nTypeclasses automate this — instead of the programmer passing the function\nexplicitly, Lean searches for one and provides it on its own. We specify something we want\nLean to search for by declaring a class with the needed function as a field; a class is like\nan interface in Java or a trait in Rust. Particular implementations of that class\nare called instance; each type can have its own instance of a class. For functions that would\nuse such instances, we specify\nthe name of the class in a instance implicit on the polymorphic variable that the\ninstance's function applies to. This directs Lean to rely on the class inside the function, and to\nfind and fill in the appropriate instance when the function is called.\n\nHere is what this looks like for List.elem_poly:\n\ndef List.elem_poly {α : Type} [BEq α] (a : α) (xs : List α) : Bool :=\n  match xs with\n  | [] => false\n  | b :: tl => bif a == b then true else elem_poly a tl\n\ntheorem List.elem_poly_nil {α : Type} [BEq α] (a : α) : [].elem_poly a = false := rfl\n\ntheorem List.elem_poly_cons {α : Type} [BEq α] (a b : α) (xs : List α) :\n    (b :: xs).elem_poly a = bif a == b then true else elem_poly a xs := rfl\n\n#eval [0, 1].elem_poly 0\n\n\nComparing List.elem_poly_eq with List.elem_poly, we see three differences.\nFirst, List.elem_poly_eq takes an explicit parameter eq,\nwhereas List.elem_poly specifies an instance implicit [BEq α]. The instance implicit\nindicates that an instance of BEq must be provided at\ncall sites for the particular type α that is used.\nSecond, whereas List.elem_poly_eq invokes parameter eq to test equality,\nList.elem_poly uses == instead. As Lists noted when we first used it,\n== on Nat comes from the BEq typeclass.\nFinally, whereas [0, 1].elem_poly_eq Nat.beq 0 passes the equality\nfunction Nat.beq explicitly, in [0, 1].elem_poly 0 Lean fills it in\nautomatically based on the type Nat of the List.\n\nThis is technically incorrect, the instance BEq Nat, which comes from DecidableEq, does not contain Nat.beq. You can see in proofs of List.elem_nat versus List.elem_poly_eq versus List.elem_poly and how Nat.beq and == play different roles.\n\nGoing back to the earlier version of List.elem_poly, without the instance implicit, we\ncan now understand the error message: α was fully generic\n— so the == in its body would have needed to work for every type α, and no\nsingle BEq instance can do that. So Lean's search failed.\n\nNow it is time to dig into the details of what we have seen so far.\nWe'll see exactly how [BEq α] gets filled in below, starting with how\nto define a typeclass in the first place.\n\n","context":"Logical Foundations\u0009Typeclasses","header":"11.1. Why We Need Typeclasses","id":"/Typeclasses/#Logical-Foundations--Typeclasses--Why-We-Need-Typeclasses"}});