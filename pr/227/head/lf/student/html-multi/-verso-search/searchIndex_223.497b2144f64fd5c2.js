window.docContents[223].resolve({"/Logic/#Logical-Foundations--Logic-in-Lean--Applying-Theorems-to-Arguments":{"contents":"Lean treats proofs as first-class objects.\nThere is a great deal to be said about this, but it is not necessary\nto understand it all to use Lean. This section gives just a taste.\n\n\n\n\n\nWe have seen that we can use #check to ask Lean whether an expression\nhas a given type:\n\n#check (Nat.add : Nat → Nat → Nat)\n\n\nWe can also use it to check what theorem a particular identifier refers to:\n\n#check Nat.add_comm\n\n\nNat.add_comm (n m : Nat) : n + m = m + n\n\n\n#check Nat.add_assoc\n\n\nNat.add_assoc (n m k : Nat) : n + m + k = n + (m + k)\n\n\nLean checks the statements of the Nat.add_comm and Nat.add_assoc theorems\nin the same way that it checks the type of any term (e.g. Nat.add).\nLeaving off the colon and the type, Lean prints these types\nin the infoview for us.\n\nWhy?\n\nThe reason is that the identifier Nat.add_comm actually refers to a\nproof object — a logical derivation establishing the truth of the\nstatement ∀ n m : Nat, n + m = m + n. The type of this object\nis the proposition that it is a proof of.\n\nThe type of an ordinary function tells us what we can do with it.\n\n* If we have a term of type Nat → Nat → Nat, we can give it\n    two Nats as arguments and get a Nat back.\nSimilarly, the statement of a theorem tells us what we can use\nthat theorem for.* If we have a term of type ∀ n m : Nat, n = m → n + n = m + n,\n    and we provide it two numbers n and m and a third \"arugment\"\n    of type n = m, we get back a proof object of type n + n = m + m.\n\nOperationally, this analogy goes even further: by applying a theorem\nas if it were a function, i.e., applying it to values and hypotheses\nwith matching types, we can specialize its result without having to\nresort to intermediate assertions. For example, suppose we wanted\nto prove the follwing result:\n\n\n\nexample (x y z : Nat) : x + (y + z) = (z + y) + x := by\n  rw [Nat.add_comm]\n  rw [Nat.add_comm]\n\n\n\n\nunsolved goals\na b c : Prop\nn m : Nat\nα✝ : Type\ne1 e2 x✝¹ y✝¹ : α✝\nα β : Type\nx✝ x' y✝ : α\nl l' : List α\nf g : α → β\np : α → Prop\nx y z : Nat\n⊢ x + (y + z) = z + y + x\n\n\nIt appears at first sight that we ought to be able to prove this\nbe rewriting with Nat.add_comm twice to make the two sides match.\nThe problem is that the second rewrite undoes the effect\nof the first, leaving us back where we started...\n\nWe encountered similar issues back in the Induction chapter, and we\nsaw that we can fix them by applying Nat.add_comm to the arguments we want it\nto be instantiated with, in much the same way as we apply\na polymorphic function to a type argument. Then the rewrite is forced\nto happen exactly where we want it.\n\nexample (x y z : Nat) : x + (y + z) = (z + y) + x := by\n  rw [Nat.add_comm]\n  rw [Nat.add_comm z y]\n\n\nIf we really wanted, we could in fact do it for both rewrites.example (x y z : Nat) : x + (y + z) = (z + y) + x := by\n  rw [Nat.add_comm x (y + z)]\n  rw [Nat.add_comm z y]\n\n\nThe fact that implications are functions means we can prove them by\nexplicitly providing a function.\n\ntheorem identity {a : Prop} : a → a := fun h => h\n\n\n\n\nSuppose we haven m : Nat\nh₁ : n = m\nh₂ : b = 42\ntrans_eq : ∀ {α : Type} {x y z : α}, x = y → y = z → x = z\nWhat is the type of this \"proof object\"?@trans_eq Nat n m 42 h₁ h₂\n1. n = m2. 42 = n3. n = 424. Does not typecheckexample (n m : Nat) (h₁ : n = m) (h₂ : m = 42)\n   (trans_eq : ∀ {α : Type} {x y z : α}, x = y → y = z → x = z) : True := by\n  have : n = 42 := @trans_eq Nat n m 42 h₁ h₂\n  sorry\n\n\nSuppose, again, we haven m : Nat\nh₁ : n = m\nh₂ : b = 42\ntrans_eq : ∀ {α : Type} {x y z : α}, x = y → y = z → x = z\nWhat is the type of this proof object?trans_eq h₁ h₂\n1. n = m2. 42 = n3. n = 424. Does not typecheckexample (n m : Nat) (h₁ : n = m) (h₂ : m = 42)\n   (trans_eq : ∀ {α : Type} {x y z : α}, x = y → y = z → x = z) : True := by\n    have : n = 42 := trans_eq h₁ h₂\n    sorry\n\n\nSuppose, again, we haven m : Nat\nh₁ : n = m\nh₂ : b = 42\ntrans_eq : ∀ {α : Type} {x y z : α}, x = y → y = z → x = z\nWhat is the type of this proof object?@trans_eq Nat m 42 n h₂\n1. m = n2. m = n → 42 = n3. 42 = n → m = n4. Does not typecheckexample (n m : Nat) (h₁ : n = m) (h₂ : m = 42)\n   (trans_eq : ∀ {α : Type} {x y z : α}, x = y → y = z → x = z) : True := by\n   have : 42 = n → m = n := @trans_eq Nat m 42 n h₂\n   sorry\n\n\nSuppose, again, we haven m : Nat\nh₁ : n = m\nh₂ : b = 42\ntrans_eq : ∀ {α : Type} {x y z : α}, x = y → y = z → x = z\nWhat is the type of this proof object?@trans_eq _ 42 n m\n1. n = m → m = 42 → n = 422. 42 = n → n = m → 42 = m3. n = 42 → 42 = m → n = m4. Does not typecheckexample (n m : Nat) (h₁ : n = m) (h₂ : m = 42)\n   (trans_eq : ∀ {α : Type} {x y z : α}, x = y → y = z → x = z) : True := by\n    have : 42 = n → n = m → 42 = m := @trans_eq _ 42 n m\n    sorry\n\n\nSuppose, again, we haven m : Nat\nh₁ : n = m\nh₂ : b = 42\ntrans_eq : ∀ {α : Type} {x y z : α}, x = y → y = z → x = z\nWhat is the type of this proof object?trans_eq h₂ h₁\n1. b = a2. 42 = a3. a = 424. Does not typecheckexample (n m : Nat) (h₁ : n = m) (h₂ : m = 42)\n   (trans_eq : ∀ {α : Type} {x y z : α}, x = y → y = z → x = z) : True := by\n    have := trans_eq h₂ h₁\nApplication type mismatch: The argument\n  h₁\nhas type\n  n = m\nbut is expected to have type\n  42 = ?m.13\nin the application\n  trans_eq h₂ h₁\n\n\n\n\n","context":"Logical Foundations\u0009Logic in Lean","header":"8.6. Applying Theorems to Arguments","id":"/Logic/#Logical-Foundations--Logic-in-Lean--Applying-Theorems-to-Arguments"},"/Preface/#Logical-Foundations--Preface--Welcome":{"contents":"This is the entry point to a series of electronic textbooks on various\naspects of Software Foundations, the mathematical underpinnings of\nreliable software.  Topics in the series include basic concepts of\nlogic, functional programming, computer-assisted theorem proving,\noperational semantics, logics and techniques for reasoning about\nprograms, static type systems, property-based random testing, and\nverification of practical C code.  The exposition is intended for a\nbroad range of readers, from advanced undergraduates to PhD students\nand researchers.  No specific background in logic or programming\nlanguages is assumed, though a degree of mathematical maturity will be\nhelpful.\n\nThe principal novelty of the series is that it is one hundred percent\nformalized and machine-checked: each text is literally a script for\nLean.  The books are intended to be read alongside (or inside) an\ninteractive session with Lean.  All the details in the text are fully\nformalized in Lean, and almost all of the exercises are designed to be\nworked using Lean.\n\nThis book, Logical Foundations in Lean, lays groundwork for the\nothers, introducing the reader to the basic ideas of functional\nprogramming, formal logic, and Lean itself.\n\n","context":"Logical Foundations\u0009Preface","header":"1.1. Welcome","id":"/Preface/#Logical-Foundations--Preface--Welcome"}});