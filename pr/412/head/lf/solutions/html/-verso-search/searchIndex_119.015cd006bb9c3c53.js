window.docContents[119].resolve({"/Automation/#Logical-Foundations--Automation___-More-Automation--The--lia--Tactic":{"contents":"The lia tactic implements a decision procedure for linear integer\narithmetic: propositional formulas whose atoms are linear\nconstraints over the natural numbers and integers. This is exactly the\nfragment of first-order logic (see the Logic chapter)\nobtained by restricting connectives and quantifiers to arithmetic\nbuilding blocks.If the goal is a universally quantified formula made out of* numeric constants, addition (+ and succ), subtraction (- and pred),\n    and multiplication by constants,* equality (= and ≠) and ordering (≤ and <), and* the logical connectives ∧, ∨, ¬, and →,then invoking lia will either solve the goal, or fail because the\ngoal is actually false: within this fragment, lia is a complete\ndecision procedure. lia reasons about\nthe goal together with any hypotheses already in the local context: each\nhypothesis is used exactly as if it had been written into the goal as an\nantecedent with →.Outside this fragment, lia can fail even when the goal is true —\nfor example, on n * n ≥ n, which multiplies two variables together rather\nthan a constant and a variable. Such a failure only means lia\ncouldn't decide the goal, not that the goal is false. Note that, when\nfailing, lia may mention another tactic, called grind.\nThis is another, more powerful tactic that subsumes lia, but we\nwill not use it here.Anything in the goal or hypotheses that isn't built from these arithmetic\npieces — including an arbitrary proposition like x ∈ l — lia\nsimply treats as an opaque atom. So lia can also solve goals that\nare purely propositional, with no arithmetic in them at all, as long as the\nonly way such atoms are combined is with ∧, ∨, ¬, and →.\n\nexample (m n o p : Nat) :\n    m + n ≤ n + o ∧ o + 3 = p + 3 →\n    m ≤ p := by\n  lia\n\nexample (m n : Nat) :\n    m + n = n + m := by\n  lia\n\nexample (m n p : Nat) :\n    m + (n + p) = m + n + p := by\n  lia\n\nexample (a b c d : Prop) :\n    (a → b) → (b → c) → (c → d) → (a → d) := by\n  lia\n\nexample (α : Type) (x : α) (l₁ l₂ l₃ : List α)\n  (h₁ : x ∈ l₁ → x ∈ l₂) (h₂ : x ∈ l₂ → x ∈ l₃) : x ∈ l₁ → x ∈ l₃ := by\n  lia\n\n\nThe lia tactic can solve many of the cases of our old Perm3.In example.\n\ntheorem Perm3_In_better_with_lia (α : Type) (x : α) (l₁ l₂ : List α)\n    (hPerm : Perm3 l₁ l₂) (hIn : x ∈ l₁) : x ∈ l₂ := by\n  induction hPerm with\n  | swap12 =>\n    rw [List.mem_cons, List.mem_cons, List.mem_cons] at *\n    obtain h | h | h | h := hIn\n    /- In addition to basic arithmetic, `lia` can also discharge goals\n      that are simple facts about logic. -/\n    . lia -- was right; left; assumption\n    . lia -- was left; assumption\n    . lia -- was right; right; left; assumption\n    . lia -- was contradiction\n  | swap23 =>\n  /- Here, we solve _all_ goals — and skip the `obtain` — with\n    the <;> tactic combinator, which we saw in the `Induction` chapter. -/\n    rw [List.mem_cons, List.mem_cons, List.mem_cons] at * <;> lia\n  | trans _ _ ih₁₂ ih₂₃ =>\n    lia -- was apply ih₂₃; apply ih₁₂; apply hIn\n\n\n","context":"Logical Foundations\u0009Automation: More Automation","header":"10.1. The  lia  Tactic","id":"/Automation/#Logical-Foundations--Automation___-More-Automation--The--lia--Tactic"}});