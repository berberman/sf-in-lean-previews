window.docContents[250].resolve({"/Automation/#Logical-Foundations--Automation___-More-Automation--The--simp--Tactic":{"contents":"The simp tactic is Lean's simplifier. It is one of the most powerful\ntools in the language, and it is used heavily in real Lean developments.\n\nThe tactic simplifies the target (the goal and/or one or more hypotheses)\nby repeatedly rewriting it using a set of lemmas.\nAt each step it tries every lemmas in its available set the way\nfirst would, applies whichever one matches\nvia rw, and repeats until no lemma applies anywhere.\nLike repeat, it fails outright if it never manages to apply a rewrite\n(\"simp made no progress\"), rather than succeeding as a\nno-op the way try simp would.\n\n\n\nsimp's available set of lemmas begins with a default set and can be extended\nto include theorems labeled @[simp].\nIndeed, the characterizing lemmas we've been writing for\nour definitions all throughout this book are examples\nof these simplification lemmas, or\nsimp lemmas as they're called by Lean programmers, only\nwe have refrained from annotating them as such (until now!).\n\n\n\nnamespace simp_lemmas_example\n\n/- `add_zero` and `add_succ` are the `simp` lemmas for `+`. -/\n@[simp]\ntheorem add_zero (n : Nat) : n + 0 = n := by rfl\n\n@[simp]\ntheorem add_succ (n m : Nat) : n + (m + 1) = (n + m) + 1 := by rfl\n\n\nInstead of manually rewriting by the characterizing lemmas in the example below,\nsimp does it automatically.\n\ntheorem add_succ_nested (n m : Nat) :\n    n + (m + 1 + 1) = (n + m + 1) + 1 := by\n  simp\n\n\nThe other way to extend simp's set of available lemmas is to list them\nexplicitly, by writing simp [<theorems>]. If you want simp to only use those,\nyou can use simp only [<theorems>]. As with rw, you can also supply a\ndefinition to simp to simplify using that definition.\n\n\n\ntheorem add_succ_nested_2 (n m : Nat) :\n    n + (m + 1 + 1) = (n + m + 1) + 1 := by\n  simp only [add_succ, add_zero]\n\n\nIf you want to know what simp is doing, you can run simp?.\n\ntheorem add_succ_nested_3 (n m : Nat) :\n    n + (m + 1 + 1) = (n + m + 1) + 1 := by\n  simp?\n\nend simp_lemmas_example\n\n\nIn the InfoView, you will see\n\nTry this:\n  [apply] simp only [add_succ, Nat.add_zero, Nat.add_left_cancel_iff]\n\n\nClick the [apply] button to replace simp? with\nthe suggested replacement. You should always do this for your final proof scripts, just\nas was recommended for rw? and exact? in the\nUsingLean chapter.Interestingly, we can see for this example that simp used the Nat version\nof add_zero, not our own added above, and also pulled in Nat.add_left_cancel_iff\nwhich is not strictly needed. But the combination works, even if it is not minimal.As with apply and rw, simp can also simplify\nhypotheses — invoking simp as simp [<lemmas>] at h\nruns the simplifier at hypothesis h.\n\n\n\nexample α x (l₁ l₂ l₃ : List α)\n    (h₁ : x ∈ l₁ ++ l₂)\n    (h₂ : x ∈ l₂ ++ l₃) :\n    x ∈ l₁ ++ l₃ ∨ x ∈ l₂ := by\n  simp at h₁; simp at h₂; simp; lia\n\n\nWe could equally well have written\n\nexample α x (l₁ l₂ l₃ : List α)\n    (h₁ : x ∈ l₁ ++ l₂)\n    (h₂ : x ∈ l₂ ++ l₃) :\n    x ∈ l₁ ++ l₃ ∨ x ∈ l₂ := by\n  simp at *; lia\n\n\n\n\nIf we want to mutually simplify everywhere, we can use simp_all, which\nsimplifies in all hypotheses and in the goal at the same time. Tactic simp_all\nis not the same as simp at *. The latter\nsimplifies each target independently, whereas simp_all\nadditionally lets the (simplified) hypotheses simplify each other and the goal,\niterating to a joint fixpoint.\n\nHere's an example that illustrates the difference:\n\n  example (a b : Nat) (h1 : a = 0) (h2 : a + b = 5) : b = 5 := by\n    simp at *\n\n\nThis fails with:\n\n`simp` made no progress\n\n\nBut simp_all closes the goal:\n\n  example (a b : Nat) (h1 : a = 0) (h2 : a + b = 5) : b = 5 := by\n    simp_all\n\n\nWe can dramatically simplify our Perm3_In_shortest theorem using simp_all:\n\ntheorem Perm3_In_shortest (α : Type) (x : α) (l₁ l₂ : List α)\n    (hPerm : Perm3 l₁ l₂) (hIn : x ∈ l₁) : x ∈ l₂ := by\n  induction hPerm <;> simp_all <;> lia\n\n\nShould we add something like the following, to explain conditional rewrites?Recall from Tactics that rewriting with a theorem h₁ -> h₂ -> a = b uses\na = b and leaves h₁ and h₂ as new subgoals. We can include such theorems\nin simp's available set, too, but it handles the premises differently:\nit tries to discharge h₁, h₂ too, recursively, using simp again (by default),\nand only fires the rewrite if it succeeds.\nIf it can't discharge a premise, it just doesn't use that lemma.Claude tested three variants of this against the real toolchain (lake env lean).\ndouble_injective from Tactics doesn't actually work here: its conclusion is the\nbare variable n = m, which isn't a usable rewrite pattern (simp lemmas need a real\ncompound term on the left), so simp [double_injective] fails outright with \"simp made\nno progress\" before it ever gets anywhere near the n.double = m.double premise.A conditional lemma that does have a proper compound left-hand side, like\nNat.sub_add_cancel : n ≤ m -> m - n + n = m, tells a cleaner three-part story. With\nhle : n ≤ m in context:example (n m : Nat) (hle : n ≤ m) : m - n + n = m := by\n  simp [Nat.sub_add_cancel]        -- fails: \"simp made no progress\"\n\nexample (n m : Nat) (hle : n ≤ m) : m - n + n = m := by\n  simp [Nat.sub_add_cancel, hle]   -- succeeds\n\nexample (n m : Nat) (hle : n ≤ m) : m - n + n = m := by\n  simp_all [Nat.sub_add_cancel]    -- succeeds, without naming hle\nThe first case shows the discharge step only consults the active simp set (default set\nplus whatever's explicitly listed) -- not arbitrary context hypotheses -- so hle\nsitting right there doesn't help; simp just skips the lemma, exactly the \"no error, no\nleftover subgoal\" behavior claimed above. The second shows discharge working once hle\nis added to the set. The third previews simp_all (introduced a bit further down this\nchapter): it folds every hypothesis into the active set automatically, so it discharges\nthe premise without hle being named at all. That third point makes this example worth\nplacing right here rather than after simp_all -- it's a preview, and it ties the two\nsections together.\n\n\n\n","context":"Logical Foundations\u0009Automation: More Automation","header":"10.3. The  simp  Tactic","id":"/Automation/#Logical-Foundations--Automation___-More-Automation--The--simp--Tactic"},"/Lists/#Logical-Foundations--Lists___-Working-with-Structured-Data--Reasoning-About-Lists--List-Exercises___-Part-2":{"contents":"\n\nopen NatList\n\n\nUsing rfl to prove Nat.ble 1 (count 1 l + 1) = true in the following count_member_nonZero exercise feels like defeq abuse.\nHowever, Nat.ble doesn't seem to have characterizing lemmas:theorem _root_.Nat.ble_zero (m : Nat) : Nat.ble 0 m = true := by rfl\ntheorem _root_.Nat.ble_succ_zero (m : Nat) :\n  Nat.ble (m + 1) 0 = false := by rfl\ntheorem _root_.Nat.ble_succ_succ (m n : Nat) (h : Nat.ble m n = true) :\n  Nat.ble (m + 1) (n + 1) = true := h\ntheorem count_member_nonzero (l : NatList) :\n    Nat.ble 1 (count 1 (1 :: l)) = true := by\n  solution!\n    rw [count_cons_same rfl]\n    rw [Nat.ble_succ_succ]\n    rw [Nat.ble_zero]\ntheorem ble_n_Sn (n : Nat) :\n    Nat.ble n (n + 1) = true := by\n  induction n with\n  | zero       =>\n    exact Nat.ble_zero _\n  | succ n' ih =>\n    rw [Nat.ble_succ_succ]\n    exact ih\n\n\nHere are a couple of little theorems to prove about your\ndefinition above.theorem count_member_nonZero (l : NatList) :\n    Nat.ble 1 (count 1 (1 :: l)) = true := by\n  solution!\n    rw [count_cons_same] <;> rfl\nThe following lemma about Nat.ble might help you in the next\nexercise (it will also be useful in later chapters).theorem ble_self_succ (n : Nat) :\n    Nat.ble n (n + 1) = true := by\n  induction n with\n  | zero       => rfl\n  | succ n' ih => rw [Nat.ble]; exact ih\n\n\nThere is a nicer solution to this exercise that doesn't require the contrived\ntheorem statement that has 0s instead of arbitrary numbers. The only issue\nis that it requires using case analysis on an expression (n == n') instead\nof a variable. I think we should probably teach this by this point anyway, so if we do,\nthen we can use the following exercise instead of the one we currently have.theorem remove_does_not_increase_count' (n : Nat) (l : NatList) :\n    Nat.ble (count n (removeOne n l)) (count n l) = true := by\n  solution!\n    induction l with\n    | nil =>\n      rw [removeOne_nil, count_nil]\n      rfl\n    | cons n' s' ih =>\n        cases h : (n == n') with\n        | false =>\n          rw [removeOne_cons_diff _ _ _ h, count_cons_diff _ _ _ h, count_cons_diff _ _ _ h]\n          exact ih\n        | true =>\n          rw [removeOne_cons_same _ _ _ h, count_cons_same _ _ _ h, ble_self_succ]\n\n\ntheorem remove_does_not_increase_count (l : NatList) :\n    Nat.ble (count 0 (removeOne 0 l)) (count 0 l) = true := by\n  solution!\n    induction l with\n    | nil =>\n      rw [removeOne_nil, count_nil]\n      rfl\n    | cons n s' ih =>\n      cases n with\n      | zero =>\n        rw [removeOne_cons_same _ _ _ rfl, count_cons_same _ _ _ rfl, ble_self_succ]\n      | succ n' =>\n        rw [removeOne_cons_diff _ _ _ rfl, count_cons_diff _ _ _ rfl, count_cons_diff _ _ _ rfl]\n        exact ih\nWrite down an interesting theorem count_append about lists\ninvolving the functions count and append, and prove it.\n(You may find that the difficulty of the proof depends on how you defined count!)This is the obvious theorem, and everyone came up with\nit.  But how hard it is to prove (in terms of Rocq mechanics)\ndepends critically on how the student defined count -- the\nsolution for which has not been given at this point, and is not so\nobvious. BCP 9/16: For the moment, I've just added an explicit\nwarning to this effect - not sure whether we can do better. (Is\nthere a hint we could give about how count should have been\ndefined, to make this easier?  There's no problem giving a hint\nhere, since they'll already have solved the count exercise once\nbefore getting to this point.) MRC 1/19: The proof uses cases\non a term that is not merely an identifier. That usage has not\nbeen introduced yet. APT 21: Added a hint about that. MRC 2/22:\nEven if the exercise is optional, it ought to be solvable with\nthe material introduced thus far. It is not. I note that BCP\nhas rejected the proof in the exercise above for count_removeOne\nbecause it uses cases on a term rather than identifier.cases, induction, if and match all support naming.\nOne can write match hv : (v == h) with instead of cases hv : (v == h) with, or evenif hv : (v == h) then\n  rw [count_cons_same _ _ _ hv, count_cons_same _ _ _ hv, Nat.succ_cons, ← ih]\nelse\n  rw [Bool.not_eq_true] at hv\n  rw [count_cons_diff _ _ _ hv, count_cons_diff _ _ _ hv]\n  exact ih\nin the following exercise.More information in the reference: <https://lean-lang.org/doc/reference/latest/find/?domain=Verso.Genre.Manual.section&name=pattern-matching>theorem count_append (l₁ l₂ : NatList) (n : Nat) :\n    count n (l₁ ++ l₂) = (count n l₁) + (count n l₂) := by\n  induction l₁ with\n  | nil =>\n    rw [nil_append, count_nil, Nat.zero_add]\n  | cons h s1' ih =>\n    rw [cons_append]\n    cases hv : (n == h) with\n    | false =>\n      rw [count_cons_diff _ _ _ hv, count_cons_diff _ _ _ hv]\n      exact ih\n    | true =>\n      rw [count_cons_same _ _ _ hv, count_cons_same _ _ _ hv, Nat.succ_add, ← ih]\nProve that every involution is injective.Involutions were defined above in reverse_involutive. An injective\nfunction is one-to-one: it maps distinct inputs to distinct\noutputs, without any collisions.theorem involutive_injective (f : Nat → Nat)\n  (hInv : ∀ n : Nat, n = f (f n)) :\n    (∀ n₁ n₂ : Nat, f n₁ = f n₂ → n₁ = n₂) := by\n  solution!\n    intro n₁ n₂ h\n    rw [hInv n₁, hInv n₂, h]\nProve that reverse is injective. Do not prove this by induction —\nthat would be hard. Instead, reuse the same proof technique that\nyou used for involutive_injective. (But: Don't try to use that\nexercise directly as a lemma: the types are not the same!)theorem reverse_injective (l₁ l₂ : NatList)\n    (h : l₁.reverse = l₂.reverse) : l₁ = l₂ := by\n  solution!\n    rw [← reverse_involutive l₁, ← reverse_involutive l₂, h]\n\n\n","context":"Logical Foundations\u0009Lists: Working with Structured Data\u0009Reasoning About Lists","header":"5.3.3. List Exercises, Part 2","id":"/Lists/#Logical-Foundations--Lists___-Working-with-Structured-Data--Reasoning-About-Lists--List-Exercises___-Part-2"}});