window.docContents[23].resolve({"/StlcProp/#Type-Systems--StlcProp___-Properties-of-STLC--Additional-Exercises":{"contents":"\n\n(Officially optional, but strongly recommended!) Without peeking\nat their statements above, write down the progress and\npreservation theorems for the simply typed lambda-calculus (as Lean\ntheorems).  You can write sorry for the proofs.At least one person was confused by what to name these. We\ncould simplify life by giving the names explicitly and just\nomitting the bodies.  Indeed, once we do that we could autograde\nthis by demanding that what they write be identical to what we\nwrote above! Maybe a better way to solve this would be to have the\nfollowing template.  BCP 21: Yes, do this!!COMMENT: untagged (* .. *) comment at prose position in the Rocq source — probably an error there; decide whether it is book prose or an author note\nSee progress and preservation from before.Suppose we add a new term zap with the following reduction rule---------                  (ST_Zap)\nt --> zap\nand the following typing rule:-------------------           (T_Zap)\nΓ ⊢ zap ⦂ T\nWhich of the following properties of the STLC remain true in\nthe presence of these rules?  For each property, write either\n\"remains true\" or \"becomes false.\" If a property becomes\nfalse, give a counterexample.* Determinism of step* Becomes false. For instance (if true then false else true) ⟶ false\nand (if true then false else true) ⟶ zap.* Progress* Remains true. Every term (including zap) can take a step to zap.* Preservation* Remains true. zap can have any type.Suppose instead that we add a new term foo with the following\nreduction rules:-----------------                (ST_Foo1)\n(\\x:A, x) --> foo\n\n  ------------                   (ST_Foo2)\n  foo --> true\nWhich of the following properties of the STLC remain true in\nthe presence of this rule?  For each one, write either\n\"remains true\" or else \"becomes false.\" If a property becomes\nfalse, give a counterexample.* Determinism of step        - Becomes false. The term [(\\x:Bool, x) true] might step\nto either [true] by the rule Step.appAbs or\nto [foo true] by the rule Step.app1 and ST_Foo1.\n* Progress* Remains true. We are only adding to the step relation, and\nthis can never damage progress.* Preservation        - Becomes false. For example,\n[⊢ \\x:Bool,x ⦂ Bool->Bool] and [(\\x:Bool,x) --> foo] by (ST_Foo1),\nbut, since we have no typing rules for foo, we cannot prove that\n[⊢ foo ⦂ Bool->Bool].\nSuppose instead that we remove the rule Step.app1 from the step\nrelation. Which of the following properties of the STLC remain\ntrue in the presence of this rule?  For each one, write either\n\"remains true\" or else \"becomes false.\" If a property becomes\nfalse, give a counterexample.* Determinism of step* Remains true. Removing reduction rules can only make step\nmore deterministic.* Progress* Becomes false. For example,\n((\\x:Bool→Bool, \\y:Bool→Bool, x) (λz:Bool. z)) (λz:Bool. z)\nis well typed, but stuck.* Preservation* Remains true. Removing reduction rules can't break preservation.Suppose instead that we add the following new rule to the\nreduction relation:----------------------------------        (ST_FunnyIfTrue)\n(if true then t₁ else t₂) --> true\nWhich of the following properties of the STLC remain true in\nthe presence of this rule?  For each one, write either\n\"remains true\" or else \"becomes false.\" If a property becomes\nfalse, give a counterexample.* Determinism of step* Becomes false, for instance:\n(if true then false else false) ⟶ false and\n(if true then false else false) ⟶ true* Progress* Remains true. We are only adding to the step relation, and\nthis can never damage progress.* Preservation* Becomes false. For example,\n⊢ if true then (λx:Bool. x) else (λx:Bool. x) ⦂ Bool→Bool\nand (if true then (λx:Bool. x) else (λx:Bool. x)) ⟶ true\nbut it's not the case that ⊢ true ⦂ Bool → Bool.Suppose instead that we add the following new rule to the typing\nrelation:Γ ⊢ t₁ ⦂ Bool->Bool->Bool\n    Γ ⊢ t₂ ⦂ Bool\n---------------------------------       (T_FunnyApp)\n   Γ ⊢ t₁ t₂ ⦂ Bool\nWhich of the following properties of the STLC remain true in\nthe presence of this rule?  For each one, write either\n\"remains true\" or else \"becomes false.\" If a property becomes\nfalse, give a counterexample.* Determinism of step* Remains true. We are only adding to the typing relation, and\nthis can never damage determinism of step.* Progress        - Remains true. Since the new rule still requires that [t₁] is\na function we can still apply Step.appAbs to show progress.\n* Preservation* Becomes false. For example,\n⊢ λx:Bool. λy:Bool. x true ⦂ Bool\nand (λx:Bool. λy:Bool. x) true ⟶ λy:Bool. true\nbut it's not the case that ⊢ λy:Bool. true ⦂ BoolSuppose instead that we add the following new rule to the typing\nrelation:Γ ⊢ t₁ ⦂ Bool\nΓ ⊢ t₂ ⦂ Bool\n------------------------            (T_FunnyApp')\nΓ ⊢ t₁ t₂ ⦂ Bool\nWhich of the following properties of the STLC remain true in\nthe presence of this rule?  For each one, write either\n\"remains true\" or else \"becomes false.\" If a property becomes\nfalse, give a counterexample.* Determinism of step* Remains true. We are not changing the step relation.* Progress* Becomes false. For instance, true true is a term that\nbecomes typable (at type Bool), but which is stuck.* Preservation* Remains true. There are 3 ways t₁ t₂ can reduce. For\nStep.app1 and Step.app2 we can still apply the induction\nhypothesis. To reduce t₁ t₂ using Step.appAbs\nt₁ would need to be a function, but functions don't have\ntype Bool.Suppose we add the following new rule to the typing relation\nof the STLC:---------------------- (T_FunnyAbs)\n⊢ \\x:Bool,t ⦂ Bool\nWhich of the following properties of the STLC remain true in\nthe presence of this rule?  For each one, write either\n\"remains true\" or else \"becomes false.\" If a property becomes\nfalse, give a counterexample.* Determinism of step* Remains true. We're not changing the step relation.* Progress        - Becomes false. For instance [if (\\x:Bool,false) then false\nelse false] is a term that would become typable, although it\nis stuck.\n* Preservation* Remains true. λx:Bool. t doesn't step.\n\n\n\nend Stlc\n\n\n\n\n","context":"Type Systems\u0009StlcProp: Properties of STLC","header":"6.7. Additional Exercises","id":"/StlcProp/#Type-Systems--StlcProp___-Properties-of-STLC--Additional-Exercises"}});