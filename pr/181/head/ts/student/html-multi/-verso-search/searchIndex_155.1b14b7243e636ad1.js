window.docContents[155].resolve({"/StlcProp/#Type-Systems--StlcProp___-Properties-of-STLC--Progress":{"contents":"The progress theorem tells us that closed, well-typed\nterms are not stuck: either a well-typed term is a value, or it\ncan take a reduction step.  The proof is a relatively\nstraightforward extension of the progress proof we saw in the\nTypes chapter.  We give the proof in English first, then\nthe formal version.\n\n\n\ntheorem progress (t : Tm) (T : Ty) (hT : <{ ∅ ⊢ ~t ⦂ ~T }>) :\n    t.IsValue ∨ ∃ t', t ⟶ t' := by\n  generalize hΓ : (∅ : Context) = Γ at hT\n  induction hT with\n  | var Γ x T₁ h =>\n    subst hΓ\n    -- Contradictory: variables cannot be typed in an empty context.\n    rw [PartialMap.apply_empty] at h\n    cases h\n  | abs => exact .inl (.abs ..)\n  | tru => exact .inl .tru\n  | fls => exact .inl .fls\n  | app Γ T₁ T₂ t₁ t₂ h₁ h₂ ih₁ ih₂ =>\n    -- `t = t₁ t₂`.  Proceed by cases on whether `t₁` is a value or steps.\n    right\n    cases ih₁ hΓ with\n    | inl hv₁ =>\n      cases ih₂ hΓ with\n      | inl hv₂ =>\n        obtain ⟨x, u, rfl⟩ := canonical_forms_fun t₁ _ _ (hΓ ▸ h₁) hv₁\n        exact ⟨<{ [~x := ~t₂] ~u }>, .appAbs x T₂ u t₂ hv₂⟩\n      | inr hs₂ => obtain ⟨t₂', h⟩ := hs₂; exact ⟨<{ ~t₁ ~t₂' }>, .app2 t₁ t₂ t₂' hv₁ h⟩\n    | inr hs₁ => obtain ⟨t₁', h⟩ := hs₁; exact ⟨<{ ~t₁' ~t₂ }>, .app1 t₁ t₁' t₂ h⟩\n  | ite Γ t₁ t₂ t₃ T₁ h₁ h₂ h₃ ih₁ ih₂ ih₃ =>\n    right\n    cases ih₁ hΓ with\n    | inl hv₁ =>\n      cases canonical_forms_bool t₁ (hΓ ▸ h₁) hv₁ with\n      | inl he => subst he; exact ⟨t₂, .ifTrue t₂ t₃⟩\n      | inr he => subst he; exact ⟨t₃, .ifFalse t₂ t₃⟩\n    | inr hs₁ =>\n      obtain ⟨t₁', h⟩ := hs₁\n      exact ⟨<{ if ~t₁' then ~t₂ else ~t₃ }>, .ifStep t₁ t₁' t₂ t₃ h⟩\n\n\nProof: By induction on the derivation of ⊢ t ⦂ T.* The last rule of the derivation cannot be HasType.var, since a\n  variable is never well typed in an empty context.* The HasType.tru, HasType.fls, and HasType.abs cases are trivial, since in\n  each of these cases we can see by inspecting the rule that t\n  is a value.* If the last rule of the derivation is HasType.app, then t has the\n  form t₁ t₂ for some t₁ and t₂, where ⊢ t₁ ⦂ T₂ → T\n  and ⊢ t₂ ⦂ T₂ for some type T₂.  The induction hypothesis\n  for the first subderivation says that either t₁ is a value or\n  else it can take a reduction step.* If t₁ is a value, then consider t₂, which by the\n      induction hypothesis for the second subderivation must also\n      either be a value or take a step.* Suppose t₂ is a value.  Since t₁ is a value with an\n          arrow type, it must be a lambda abstraction; hence t₁ t₂ can take a step by Step.appAbs.* Otherwise, t₂ can take a step, and hence so can t₁ t₂ by Step.app2.* If t₁ can take a step, then so can t₁ t₂ by Step.app1.* If the last rule of the derivation is HasType.ite, then t = if t₁ then t₂ else t₃, where t₁ has type Bool.  The first IH\n  says that t₁ either is a value or takes a step.* If t₁ is a value, then since it has type Bool it must be\n      either true or false.  If it is true, then t steps to\n      t₂; otherwise it steps to t₃.* Otherwise, t₁ takes a step, and therefore so does t (by\n      Step.ifStep).\n\nShow that progress can also be proved by induction on terms\ninstead of induction on typing derivations.theorem progress' (t : Tm) (T : Ty) (hT : <{ ∅ ⊢ ~t ⦂ ~T }>) :\n    t.IsValue ∨ ∃ t', t ⟶ t' := by\n  sorry\n\n\n","context":"Type Systems\u0009StlcProp: Properties of STLC","header":"6.2. Progress","id":"/StlcProp/#Type-Systems--StlcProp___-Properties-of-STLC--Progress"}});