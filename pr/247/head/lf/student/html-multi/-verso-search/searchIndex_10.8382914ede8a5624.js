window.docContents[10].resolve({"/Induction/#Logical-Foundations--Induction___-Proof-by-Induction--Nat-to-Bin-and-Back-to-Nat":{"contents":"namespace NatToBin\n\n\nRecall the Bin type we defined in Basics:\n\ninductive Bin : Type where\n  | z\n  | b0 (n : Bin)\n  | b1 (n : Bin)\n\n\nBefore you start working on the next exercise, replace the stub\ndefinitions of incr and binToNat, below, with your solution\nfrom Basics.  That will make it possible for this file to be graded\non its own.\n\ndef incr (m : Bin) : Bin\n  := sorry\n\n\ntheorem incr_z : incr .z = .b1 .z := sorry\ntheorem incr_b0 m : incr (.b0 m) = .b1 m := sorry\ntheorem incr_b1 m : incr (.b1 m) = .b0 (incr m) := sorry\n\n\ndef binToNat (m : Bin) : Nat\n  := sorry\n\ntheorem binToNat_z : binToNat .z = zero := sorry\ntheorem binToNat_b0 m : binToNat (.b0 m) = mul (binToNat m) two := sorry\ntheorem binToNat_b1 m : binToNat (.b1 m) = add (mul (binToNat m) two) one := sorry\n\nattribute [pp_nodot] Bin.b0 Bin.b1\n\n\nIn Basics, we did some unit testing of binToNat, but we\ndidn't prove its correctness. Now we'll do so.\n\nProve that the following diagram commutes — that is, incrementing a binary number and\n  then converting it to a (unary) natural number yields the same result as first converting\n  it to a natural number and then incrementing:       incr Bin ----------------------> Bin\n           |                             |\nbinToNat   |                             |  binToNat\n           |                             |\n           v                             v\n          Nat ------------------------> Nat\n                      succ\nIf you want to change your previous definitions of incr or binToNat\n  to make the property easier to prove, feel free!theorem bin_to_nat_pres_incr (b : Bin) :\n    binToNat (incr b) = (binToNat b) + one := by\n  sorry\n\n\nWrite a function to convert natural numbers to binary numbers.\n  Also write some simplification lemmas for it.def natToBin (n : Nat) : Bin := sorry\n-- FILL IN HERE\nProve that, if we start with any Nat, convert it to Bin, and\nconvert it back, we get the same Nat which we started with.Hint: This proof should go through smoothly using the previous\nexercise about incr as a lemma. If not, revisit your definitions\nof the functions involved and consider whether they are more\ncomplicated than necessary: the shape of a proof by induction will\nmatch the recursive structure of the program being verified, so\nmake the recursions as simple as possible.theorem nat_bin_nat (n : Nat) :\n    binToNat (natToBin n) = n := by\n  sorry\n\n\n","context":"Logical Foundations\u0009Induction: Proof by Induction","header":"3.7. Nat to Bin and Back to Nat","id":"/Induction/#Logical-Foundations--Induction___-Proof-by-Induction--Nat-to-Bin-and-Back-to-Nat"},"/Typeclasses/#Logical-Foundations--Typeclasses--Maps--Total-Maps--Getting-Elements":{"contents":"While TotalMaps happen to be implemented as functions under the hood,\nwe would prefer not to expose this\nfact in its public interface. Accordingly, we define new operations for querying and\nupdating mappings. As a first attempt at a query operation, playing the role that find played for the Lists chapter's\nlist-based maps, we could define a function\nget for getting the value associated with a key:\n\ndef get {α β : Type} (m : TotalMap α β) (a : α) := m.inner a\n\n/-- This exposes implementation-specific details of `TotalMap`.\nAvoid using this outside the `TotalMap` namespace. -/\ntheorem get_def {α β : Type} {m : TotalMap α β} {a : α} : m.get a = m.inner a := by rfl\n\nexample : emptyNatMap.get 2 = 0 := by rfl\n\n\nHere is an example that uses the API lemmas empty_def and get_def:\n\nexample {n : Nat} : emptyNatMap.get n = 0 := by\n  rewrite [get_def, emptyNatMap, empty_def, Nat.default_eq_zero]\n  rfl\n\n\nIn the above example, we use rewrite and rfl instead of the usual rw\nto highlight something interesting. After the rewrites in this proof,\nwe end up with a goal that looks like { inner := fun x => 0 }.inner n = 0, which we can solve\nwith rfl. This is because the projection .inner on a structure of the form { inner := x }\nis definitionally equal to x.\n\nget is the public API counterpart to inner which is an implementation-specific detail of TotalMap.\nBecause get_def \"peeks\" through the abstraction, it should be used sparingly, and only inside the TotalMap namespace.\n\nTo make element-getting more convenient, let's define notation so we can write\nemptyNatMap[2] rather than emptyNatMap.get. We could notate get directly — we'll do\nexactly that for update below — but here we'll instead make \"getting an element\" its own\ntypeclass, MyGetElem, and notate it. Doing so means m[a] resolves to MyGetElem.getElem m a for any\ntype with a MyGetElem instance, not just TotalMap.\n\nUsing typeclasses to define notation is typical in Lean when the same notation is useful\nfor many different types.\nWe have seen the approach already with ==: writing\na == b is notation for BEq.beq, resolved by instance search for whatever type a and b have.\nWe also just saw overloaded notation for EmptyCollection above,\nwhere ∅ is notation for EmptyCollection.emptyCollection.\nOur typeclass MyGetElem is a simpler version of the standard library's GetElem typeclass,\nwhich has many instances such as Array, List, and Vector.\nWe develop it to illustrate the notation-as-typeclass approach.\n\n\n\nend TotalMap\n\n\nThe MyGetElem typeclass takes three type parameters: the map implementation,\nits keys, and its values.\n\nclass MyGetElem (coll : Type) (idx : Type) (elem : outParam Type) where\n  getElem (xs : coll) (i : idx) : elem\n\n\n(Don't worry about the outParam qualifier; it is a hint to Lean that helps typeclass inference.)\n\nThe appropriate instance of MyGetElem for our TotalMap is:\n\ninstance {α β : Type} : MyGetElem (TotalMap α β) α β where\n  getElem m a := m.get a\n\n\nNow we can associate the bracket syntax with MyGetElem.getElem. We've defined custom notation\nbefore — :: and [...] for lists (chapter Lists, including an app_unexpander for\nprinting [...]-notation lists back out), or +/*/== for arithmetic — but always with\ninfixl/infixr or scoped macro; this is the first time we reach for the more general\nnotation/macro_rules forms for getting the m[a]\nsyntax to work.\n\nDon't worry about following the mechanism in detail — the\nmacro_rules and the app_unexpander below are minor technicalities. However,\nif you do wish to learn more, Chapter 5 and 6 of\nMetaprogramming in Lean 4\ncontain more detail.\n\nnamespace MyGetElem\n\nscoped macro_rules | `($xs[$i]) => ``(getElem $xs $i)\n\n@[app_unexpander getElem]\ndef unexpandGetElem : Lean.PrettyPrinter.Unexpander\n  | `($_ $xs $i) => `($xs[$i])\n  | _ => throw ()\nend MyGetElem\n\nopen scoped MyGetElem\n\n\nSince the standard library already declares the x[i] syntax for GetElem,\nwe only need to define the macro_rules, not the notation as we have done previously.\nIt's scoped since we don't want to override the default GetElem everywhere, but\nonly when open scoped MyGetElem is in force.\n\n\n\nSince we provided a MyGetElem instance for TotalMap, we can now use the\nnotation m[a] to access elements of a map m.\n\nnamespace TotalMap\n\ntheorem getElem_def {α  β : Type} (m : TotalMap α β) (a : α) : m[a] = m.get a := by rfl\n\nexample : emptyNatMap[1] = default := by rfl\n\nexample {n : Nat} : emptyNatMap[n] = 0 := by\n  rw [getElem_def, get_def, emptyNatMap, empty_def, Nat.default_eq_zero]\n\n\nWe want the public API of TotalMap to use the m[a] notation instead of m.get a so we provide the reverse direction of getElem_def as a simp lemma; the m[a] notation is the TotalMap API's\nsimp normal form.\n\n@[simp]\ntheorem get_eq_getElem {α β : Type} (m : TotalMap α β) (a : α) : m.get a = m[a] := by rfl\n\nexample {n : Nat} : emptyNatMap.get n = emptyNatMap[n] := by\n  simp\n\n\nThis design minimizes the need to use getElem_def outside concrete examples (which are typically solvable with rfl anyways).\n\n","context":"Logical Foundations\u0009Typeclasses\u0009Maps\u0009Total Maps","header":"11.5.2.1. Getting Elements","id":"/Typeclasses/#Logical-Foundations--Typeclasses--Maps--Total-Maps--Getting-Elements"}});