window.docContents[38].resolve({"/StlcProp/#Type-Systems--StlcProp___-Properties-of-STLC--Preservation--The-Substitution-Lemma":{"contents":"Now we come to the conceptual heart of the proof that reduction\npreserves types — namely, the observation that substitution\npreserves types.\n\nFormally, the so-called substitution lemma says this:\nSuppose we have a term t with a free variable x, and suppose\nwe've assigned a type T to t under the assumption that x has\nsome type U.  Also, suppose that we have some other term v and\nthat we've shown that v has type U.  Then, since v satisfies\nthe assumption we made about x when typing t, we can\nsubstitute v for each of the occurrences of x in t and\nobtain a new term that still has type T.\n\n\n\n\n\n-- IN PROGRESS\ntheorem substitution_preserves_typing (Γ : Context) (x : String) (U : Ty)\n    (t v : Tm) (T : Ty)\n    (hT : <{ ~x ↦ ~U ; ~Γ ⊢ ~t ⦂ ~T }>) (hv : <{ ∅ ⊢ ~v ⦂ ~U }>) :\n    <{ ~Γ ⊢ [~x := ~v] ~t ⦂ ~T }> := by\n  -- By induction on `t`; in each case we get at the derivation of `hT`.\n  induction t generalizing Γ T with\n  | var y =>\n    cases hT with\n    | var _ _ _ h =>\n      by_cases hxy : x = y\n      · subst hxy\n        rw [PartialMap.update_eq] at h\n        rw [subst_var_eq]\n        have hUT : U = T := Option.some.inj h\n        subst hUT\n        exact weakening_empty hv\n      · rw [PartialMap.update_neq hxy] at h\n        rw [subst_var_ne _ _ _ hxy]\n        exact .var _ y _ h\n  | app t₁ t₂ ih₁ ih₂ =>\n    cases hT with\n    | app _ _ _ _ _ h₁ h₂ => rw [subst_app]; exact .app _ _ _ _ _ (ih₁ _ _ h₁) (ih₂ _ _ h₂)\n  | abs y S t₁ ih =>\n    cases hT with\n    | abs _ _ _ _ _ h =>\n      by_cases hxy : x = y\n      · subst hxy\n        rw [subst_abs_eq]\n        rw [PartialMap.update_shadow] at h\n        exact .abs _ _ _ _ _ h\n      · rw [subst_abs_ne _ _ _ _ _ hxy]\n        rw [PartialMap.update_permute (Ne.symm hxy)] at h\n        exact .abs _ _ _ _ _ (ih _ _ h)\n  | tru => cases hT with | tru => rw [subst_tru]; exact .tru _\n  | fls => cases hT with | fls => rw [subst_fls]; exact .fls _\n  | ite c t e ihc iht ihe =>\n    cases hT with\n    | ite _ _ _ _ _ h₁ h₂ h₃ =>\n      rw [subst_ite]\n      exact .ite _ _ _ _ _ (ihc _ _ h₁) (iht _ _ h₂) (ihe _ _ h₃)\n\n\nThe substitution lemma can be viewed as a kind of \"commutation\nproperty.\"  Intuitively, it says that substitution and typing can\nbe done in either order: we can either assign types to the terms\nt and v separately (under suitable contexts) and then combine\nthem using substitution, or we can substitute first and then\nassign a type to [x:=v] t; the result is the same either\nway.Proof: We show, by induction on t, that for all T and\nΓ, if x ↦ U; Γ ⊢ t ⦂ T and ⊢ v ⦂ U, then\nΓ ⊢ [x:=v]t ⦂ T.* If t is a variable there are two cases to consider,\n    depending on whether t is x or some other variable.* If t = x, then from the fact that x ↦ U; Γ ⊢ x ⦂ T we conclude that U = T.  We must show that [x:=v]x = v has type T under Γ, given the assumption that\n        v has type U = T under the empty context.  This\n        follows from the weakening lemma.* If t is some variable y that is not equal to x, then\n        we need only note that y has the same type under x ↦ U; Γ as under Γ.* If t is an abstraction λy:S. t₀, then T = S→T₁ and\n    the IH tells us, for all Γ' and T₀, that if x ↦ U; Γ' ⊢ t₀ ⦂ T₀, then Γ' ⊢ [x:=v]t₀ ⦂ T₀.\n    Moreover, by inspecting the typing rules we see it must be\n    the case that y ↦ S; x ↦ U; Γ ⊢ t₀ ⦂ T₁.The substitution in the conclusion behaves differently\n    depending on whether x and y are the same variable.First, suppose x = y.  Then, by the definition of\n    substitution, [x:=v]t = t, so we just need to show Γ ⊢ t ⦂ T.  Using HasType.abs, we need to show that y ↦ S; Γ ⊢ t₀ ⦂ T₁. But we know y ↦ S; x ↦ U; Γ ⊢ t₀ ⦂ T₁,\n    and the claim follows since x = y.Second, suppose x <> y. Again, using HasType.abs,\n    we need to show that y ↦ S; Γ ⊢ [x:=v]t₀ ⦂ T₁.\n    Since x <> y, we have\n    y ↦ S; x ↦ U; Γ = x ↦ U; y ↦ S; Γ. So\n    we have x ↦ U; y ↦ S; Γ ⊢ t₀ ⦂ T₁. Then, the\n    the IH applies (taking Γ' = y ↦ S; Γ), giving us\n    y ↦ S; Γ ⊢ [x:=v]t₀ ⦂ T₁, as required.* If t is an application t₁ t₂, the result follows\n    straightforwardly from the definition of substitution and the\n    induction hypotheses.* The remaining cases are similar to the application case.\n\nOne technical subtlety in the statement of the above lemma is that\nwe assume v has type U in the empty context — in other\nwords, we assume v is closed.  (Since we are using a simple\ndefinition of substitution that is not capture-avoiding, it doesn't\nmake sense to substitute non-closed terms into other terms.\nFortunately, closed terms are all we need!)\n\nShow that substitutionpreservestyping can also be\nproved by induction on typing derivations instead\nof induction on terms.-- IN PROGRESS\ntheorem substitution_preserves_typing_from_typing_ind (Γ : Context) (x : String) (U : Ty)\n    (t v : Tm) (T : Ty)\n    (hT : <{ ~x ↦ ~U ; ~Γ ⊢ ~t ⦂ ~T }>) (hv : <{ ∅ ⊢ ~v ⦂ ~U }>) :\n    <{ ~Γ ⊢ [~x := ~v] ~t ⦂ ~T }> := by\n  solution!\n    generalize hΓ : (x →ₚ U ; Γ) = Γ₀ at hT\n    induction hT generalizing Γ with\n    | var _ y T₁ h =>\n      subst hΓ\n      by_cases hxy : x = y\n      · subst hxy\n        rw [PartialMap.update_eq] at h\n        rw [subst_var_eq]\n        have hUT : U = T₁ := Option.some.inj h\n        subst hUT\n        exact weakening_empty hv\n      · rw [PartialMap.update_neq hxy] at h\n        rw [subst_var_ne _ _ _ hxy]\n        exact .var _ y _ h\n    | abs _ y _ _ _ hb ih =>\n      subst hΓ\n      by_cases hxy : x = y\n      · subst hxy\n        rw [subst_abs_eq]\n        rw [PartialMap.update_shadow] at hb\n        exact .abs _ _ _ _ _ hb\n      · rw [subst_abs_ne _ _ _ _ _ hxy]\n        exact .abs _ _ _ _ _ (ih _ (PartialMap.update_permute hxy))\n    | app _ _ _ _ _ _ _ ih₁ ih₂ =>\n      rw [subst_app]; exact .app _ _ _ _ _ (ih₁ _ hΓ) (ih₂ _ hΓ)\n    | tru => rw [subst_tru]; exact .tru _\n    | fls => rw [subst_fls]; exact .fls _\n    | ite _ _ _ _ _ _ _ _ ih₁ ih₂ ih₃ =>\n      rw [subst_ite];\n      constructor <;> simp_all\n\n\n","context":"Type Systems\u0009StlcProp: Properties of STLC\u0009Preservation","header":"6.3.2. The Substitution Lemma","id":"/StlcProp/#Type-Systems--StlcProp___-Properties-of-STLC--Preservation--The-Substitution-Lemma"}});