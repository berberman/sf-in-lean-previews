window.docContents[152].resolve({"/Automation/#Logical-Foundations--Automation___-More-Automation--Case-Study___-Regular-Expressions--Definitions":{"contents":"\n\ninductive RegExp (α : Type) : Type where\n  | EmptySet\n  | EmptyStr\n  | Char (c : α)\n  | App (r1 r2 : RegExp α)\n  | Union (r1 r2 : RegExp α)\n  | Star (r : RegExp α)\nderiving BEq, DecidableEq, Repr\n\n-- prevents printing dot-chained, method-call-style like r1.App r2\nattribute [pp_nodot] RegExp.Char RegExp.App RegExp.Union RegExp.Star\n\nnamespace RegExp\n\n\nNote that this definition is polymorphic: regular\nexpressions in RegExp α describe strings with characters drawn\nfrom α — which in this exercise we represent as lists with\nelements from α.\n\n\n\nCH: Do you mean here that this is different because the inductive type doesn't specify α is finite? In Lean the convention is for inductives not to carry Prop-valued typeclass assumptions, enforcing this only at the theorems that use them. So this could give off a slightly wrong impression.\nDHS: @bcpierce00 What was the purpose of this aside in the original Rocq text? Does it make sense to keep here?\n\nWe connect regular expressions and strings by defining when a\nregular expression matches some string.\n\nIt would be convenient to declare the variables below so that inline prose\nthroughout the rest of this section can use α, x, s, s₁, s₂, s₃,\nss, re, re₁, and re₂ without repeating their type annotations, but the\nsame problem described in Logic applies: an unused variable\nis silently added to the local context in basically every proof from here on,\neven when the theorem never mentions it, which makes theorem hover-overs in\nthe HTML book unusable. Until we have a way to declare variables visible only\nfor inline prose (rather than for every lean block), we leave this\ncommented out:-- variable\n--   (α : Type)\n--   (x : α)\n--   (s s₁ s₂ s₃ : List α)\n--   (ss : List (List α))\n--   (re re₁ re₂ : RegExp α)\n\n\nInformally, this looks as follows:\n\n* The regular expression EmptySet does not match any string.* EmptyStr matches the empty string [].* Char x matches the one-character string [x].* If re₁ matches s₁, and re₂ matches s₂,\n    then App re₁ re₂ matches s₁ ++ s₂.* If at least one of re₁ and re₂ matches s,\n    then Union re₁ re₂ matches s.* Finally, if we can write some string s as the concatenation\n    of a sequence of strings s = s₁ ++ ... ++ sₖ, and the\n    expression re matches each one of the strings sᵢ,\n    then Star re matches s.In particular, the sequence of strings may be empty, so\n    Star re always matches the empty string [] no matter what\n    re is.\n\nWe can easily translate this intuition into a set of rules,\nwhere we write s =~ re to say that re matches s:\n\n        ─────────────── (mEmpty)\n        [] =~ EmptyStr\n\n        ─────────────── (mChar)\n        [x] =~ (Char x)\n\n    s₁ =~ re₁     s₂ =~ re₂\n  ─────────────────────────── (mApp)\n  (s₁ ++ s₂) =~ (App re₁ re₂)\n\n           s₁ =~ re₁\n    ───────────────────── (mUnionL)\n    s₁ =~ (Union re₁ re₂)\n\n           s₂ =~ re₂\n    ───────────────────── (mUnionR)\n    s₂ =~ (Union re₁ re₂)\n\n      ──────────────── (mStar0)\n      [] =~ (Star re)\n\ns₁ =~ re     s₂ =~ (Star re)\n──────────────────────────── (mStarApp)\n  (s₁ ++ s₂) =~ (Star re)\n\n\nThis directly corresponds to the following inductive definition:\n\ninductive ExpMatch {α : Type} : List α → RegExp α → Prop where\n  | mEmpty : ExpMatch [] EmptyStr\n  | mChar (c : α) : ExpMatch [c] (Char c)\n  | mApp (s₁ s₂ : List α) {re₁ re₂ : RegExp α}\n         (h₁ : ExpMatch s₁ re₁) (h₂ : ExpMatch s₂ re₂)\n       : ExpMatch (s₁ ++ s₂) (App re₁ re₂)\n  | mUnionL (s₁ : List α) {re₁ re₂ : RegExp α}\n            (h₁ : ExpMatch s₁ re₁) : ExpMatch s₁ (Union re₁ re₂)\n  | mUnionR (s₂ : List α) {re₁ re₂ : RegExp α}\n            (h₂ : ExpMatch s₂ re₂) : ExpMatch s₂ (Union re₁ re₂)\n  | mStar0 (re : RegExp α) : ExpMatch [] (Star re)\n  | mStarApp (s₁ s₂ : List α) {re : RegExp α}\n             (h₁ : ExpMatch s₁ re) (h₂ : ExpMatch s₂ (Star re))\n           : ExpMatch (s₁ ++ s₂) (Star re)\nopen ExpMatch\n\ninfix:40 \" =~ \" => ExpMatch\n\n\nNotice that this clause in our informal definition...\"The expression EmptySet does not match any string.\"... is not explicitly reflected in the above definition.  Do we\nneed to add something?(A) Yes, we should add a rule for this.(B) No, one of the other rules already covers this case.(C) No, the lack of a rule actually gives us the behavior we want.example α (s: List α) : ¬ (s =~ EmptySet) := by\n  intro contra; inversion contra\n\n\n\n\n","context":"Logical Foundations\u0009Automation: More Automation\u0009Case Study: Regular Expressions","header":"10.5.1. Definitions","id":"/Automation/#Logical-Foundations--Automation___-More-Automation--Case-Study___-Regular-Expressions--Definitions"}});