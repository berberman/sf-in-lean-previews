window.docContents[231].resolve({"/Induction/#Logical-Foundations--Induction___-Proof-by-Induction--Review--Proof-by-Induction":{"contents":"\n\nBut the proof that it is also a neutral element on the\n   left gets stuck...\n\nexample : ∀ n : Nat, zero + n = n := by\n  intro n\n  -- `rfl` doesn't work here!\n  sorry\n\n\n\n\nAnd reasoning by cases using cases n doesn't get us much\nfurther: the branch of the case analysis where we assume n = zero\ngoes through just fine, but in the branch where n = n' + 1 for\nsome n' we get stuck in exactly the same way.\n\nexample : ∀ n : Nat, zero + n = n := by\n  intro n\n  cases n with\n  | zero => /- n = zero -/\n    rewrite [add_zero]\n    rfl\n    -- so far so good...\n  | succ n' =>   /- n = succ n' -/\n    -- ...but we're stuck on zero + n'\n    sorry\n\n\n\n\n\n\n\n\nWe need a bigger hammer: the principle of induction over\nnatural numbers...* If P(n) is some proposition involving a natural number n,\nand we want to show that P holds for all numbers, we can\nreason like this:* show that P(zero) holds* show that, if P(n') holds, then so does P(succ n')* conclude that P(n) holds for all n.For example...\n\n\n\ntheorem zero_add : ∀ n : Nat, zero + n = n := by\n  intro n\n  induction n with\n  | zero => /- n = zero -/\n    rewrite [add_zero]\n    rfl\n  | succ n' ih => /- n = succ n' -/\n    /-\n      Goal: zero + (succ n') = succ n'\n      We can rewrite `zero + (succ n')` to `succ (zero + n')`.\n      Then we can rewrite with the induction hypothesis.\n    -/\n    rewrite [add_succ, ih]\n    rfl\n\n\n\n\n\n\nLet's try this one together:\n\ntheorem beq_self : ∀ n : Nat,\n    (n == n) = true := by\n  sorry\n\n\nWe need to make sure this section below is true! It won't be once we switch\n     to the indexed style.\n\n\n\n\n\n\n\n\nHere's another related fact about addition, which we'll\nneed later.  (The proof is left as an exercise.)\n\ntheorem add_comm (n m : Nat) :\n    n + m = m + n := by\n  sorry\n\n\n\n\ntheorem add_assoc (n m p : Nat) :\n    n + (m + p) = (n + m) + p := by\n  sorry\n\n\n\n\nConsider the following function, which doubles its argument:Rule rewrite\n\nBCP: What is \"ASSUME HIDDEN\"??\nASSUME HIDDEN\ndef double (n : Nat) : Nat :=\n  match n with\n  | zero    => zero\n  | succ n' => succ (succ (double n'))\ntheorem double_zero : double zero = zero := by rfl\ntheorem double_succ : ∀ n, double (succ n) = succ (succ (double n)) := by\n  intro n; rfl\n\nattribute [irreducible] double\nThe ASSUME HIDDEN / END ASSUME region markers around this exercise are\nunhandled: ASSUME HIDDEN got swept into the developer note above, and this\nbare END ASSUME line renders as stray book prose in all three build\nproducts. (See BCP's \"What is ASSUME HIDDEN??\" note above.) Either implement\nthe marker or delete both lines.END ASSUMEWe need better typesetting for displays like the following ones:\n\n\n","context":"Logical Foundations\u0009Induction: Proof by Induction\u0009Review","header":"3.2.1. Proof by Induction","id":"/Induction/#Logical-Foundations--Induction___-Proof-by-Induction--Review--Proof-by-Induction"}});