window.docContents[177].resolve({"/Basics/#Logical-Foundations--Basics___-Functional-Programming-in-Lean--Data-and-Functions--Basic-Proofs":{"contents":"Now that we've defined some basic functions on booleans, let's see how to\nprove some simple properties of those functions. Here is a simple rule\nabout &&:* for any boolean value b, (MyBool.true && b) = bThis is an example of a proposition, a logical claim that we can try to prove.\nIt says that MyBool.true && b is equal to b for every MyBool b.How might we write this proposition in Lean?* theorem true_and : ∀ (b : MyBool), (MyBool.true && b) = bThe keyword theorem indicates that we are stating (and eventually proving)\na proposition; the text after the first : is the proposition we want to prove.\nYou'll notice that this proposition looks a lot like the one we wrote above,\nbut with some additional symbols in front.\nThe ∀ symbol, pronounced \"forall\", is\ncalled a universal quantifier because it quantifies the variable b that appears\nin the proposition. Quantifying a variable with a ∀ means that the proposition\napplies to all possible values of its type; here, we annotate b\nwith the type MyBool to signify that\nthe proposition holds for all bs of type MyBool.Now that we've stated the theorem we'd like to prove, let's set about proving it.\n\n\n\ntheorem true_and : ∀ (b : MyBool), (MyBool.true && b) = b := by\n  intro b\n  rfl\n\n\nWhat does this mean?First we have the by keyword, which signals\nto Lean that we are beginning a sequence of tactics.\nThe intro b and rfl that you see after the by\nare examples of tactics. If you hover over a tactic's name, Lean shows\nits documentation, explaining what the tactic does and how to use it.Tactics manipulate the proof state, as you can see the in the Lean InfoView panel.\nThe proof state is divided by the symbol ⊢, called the turnstile. The part\nbefore it is called the context, and the part after it is called\nthe goal. The context records what we know\nat some point in the proof; the goal is what we are trying to prove\nat that point.A tactic manipulates both the goal and the context, to get the goal\ninto a shape that is closer to the one we want. A tactic can also\nclose (solve) the current goal, finishing its proof.Let's walk through the example above with this terminology in mind.\n\n\n\ntheorem true_and_explained : ∀ (b : MyBool), (MyBool.true && b) = b := by\n  /- Move your cursor (click) here to see the initial proof state in\n     the InfoView. If you are viewing the book online,\n     instead click on the white button after `by`.\n     The context (before the ⊢) is empty.\n     The goal is `∀ (b : MyBool), (MyBool.true && b) = b`. -/\n  intro b\n  /- Now click here (or the white button after `intro b`)\n     to see the new proof state that results from the\n     tactic. Notice how `intro b` has changed the _context_: it now\n     contains `b : MyBool`.\n\n    The `intro` tactic is used to name variables quantified by a `∀`.\n    Since we are trying to prove a property of all `MyBools`, we\n    proceed by introducing an unknown `MyBool` `b` and prove\n    the property holds for `b`, regardless of what it is.  Informally,\n    this move can be read, \"We want to prove <some property> for all\n    `MyBool`s `b`. So suppose `b` is some arbitrary `MyBool`...\n    <and then go on to prove the property for this particular `b`>...\"\n    Since `b` was chosen arbitrarily, we've now proved the property\n    for all `b`.\n\n    A proof of a theorem beginning with a ∀ will typically start with\n    an `intro`.\n\n    As in the `example`s above, we can use the `rfl` tactic,\n    which closes goals about equality where both sides are equal to\n    one another according to the principle of reflexivity. Now,\n    inspecting our goal will show that it is `(MyBool.true && b) = b`,\n    which may not appear to be equal to itself. However, the tactic\n    _evaluates_ both sides of the equality before comparing them. In\n    this case, if we look at the definition of `and`, we can see that,\n    when its first argument is `MyBool.true`, the result is its second\n    argument. So the two terms `MyBool.true && b` and `b` are in fact\n    equal because one evaluates to the other.\n  -/\n  rfl\n  /- The proof is now done! The Lean InfoView tells us there are \"No goals\". -/\n\n\nIt's also important to point out that, as with languages like Python and Haskell,\nLean is whitespace-sensitive. That is, the indentation in proofs is important and changing\nit can change the meaning of the proof, usually causing the proof to break. Suppose we had\ninstead written the following:theorem true_and_wrong : ∀ (b : MyBool), (MyBool.true && b) = b := by\n  intro b\n    rfl\nTo see the error message in the Lean file,\nchange sf_expect_failure to sf_expect_failure? temporarily.\nYou should see the following message.Tactic `introN` failed: There are no additional binders or `let` bindings in the goal to introduce\n\nb : MyBool\n⊢ (true && b) = b\nLean complains because the rfl is not at the same level of indentation as the {tactic}intro b,\nso it does not recognize these two tactics as being sequential in the way they should be.In general, sequential tactics applied to the same goal must be on subsequent lines at the same\nlevel of indentation or separated on the same line by a ; like so:theorem true_and' : ∀ (b : MyBool), (MyBool.true && b) = b := by\n  intro b; rfl\n\n\nHere's a simple proof for you to try.\nRemove sorry and fill in the proof.theorem false_or : ∀ (b : MyBool), (MyBool.false || b) = b := by\n  solution!\n    intro b\n    rfl\nWhile in this book we often use sorry as a placeholder for you to\nreplace with an actual proof, in general, sorry tells Lean that we want to skip trying\nto prove a theorem and just accept it as a given.  This can be useful for developing longer proofs.Be careful, though: every time you say sorry you are leaving\na door open for total nonsense to enter Lean's safe, formally\nchecked world!theorem really_bad : MyBool.true = MyBool.false := by sorry\nThe facts we've seen so far about booleans are quite simple, so the tactics we need to\nprove them are also quite simple. Over the course of this book we are going to\nintroduce new tactics and proof techniques gradually, enriching the propositions we can prove along the way.Now that we've seen how to define our own booleans and prove some basic\nproperties about them, let's switch to Lean's built-in Bool type, which has the same structure\nbut comes with a lot of useful functions and lemmas.\n\n\n\nend MyBool\n\n\n","context":"Logical Foundations\u0009Basics: Functional Programming in Lean\u0009Data and Functions","header":"2.1.4. Basic Proofs","id":"/Basics/#Logical-Foundations--Basics___-Functional-Programming-in-Lean--Data-and-Functions--Basic-Proofs"}});