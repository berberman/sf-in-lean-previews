window.docContents[110].resolve({"/Poly/#Logical-Foundations--Poly___-Polymorphism-and-Higher-Order-Functions--Polymorphism--Polymorphic-Pairs":{"contents":"Like inductives, structures can also be made polymorphic.\nIf we generalize the definition NatProd of pairs of natural numbers from last chapter,\nwe get polymorphic pairs, often called products:\n\nstructure MyProd (α β : Type) where\n  fst : α\n  snd : β\n\n\nLean's built-in product type Prod provides a Prod.mk constructor,\nand Prod.fst and Prod.snd functions for accessing the first and\nsecond components of the pair. It also has special syntax for creating products:\n\n#check (1, true)\n#eval (1, true).fst\n#eval (1, true).snd\n\n\n(1, true) : Nat × Bool\n\n\n1\n\n\ntrue\n\n\nYou can also use .1 instead of .fst and .2 instead of .snd:\n\nexample : (3, 5).1 = 3 := by rfl\nexample : (3, 5).2 = 5 := by rfl\n\n\nLean writes the product type Prod α β as α × β.\nIn VS Code you can type \\times or \\x to enter the × symbol.\n\nIt is easy at first to get (x, y) and α × β confused.\nRemember that (x, y) is a value built from two other values,\nwhile α × β is a type built from two other types. If x has\ntype α and y has type β, then (x, y) has type α × β.\n\n\n\n\n\nThe following function takes two lists and combines them\ninto a list of pairs.\n\n\n\n\n\ndef zip {α β : Type} (l₁ : List α) (l₂ : List β) : List (α × β) :=\n  match l₁, l₂ with\n  | [], [] => []\n  | _ :: _, [] => []\n  | [], _ :: _ => []\n  | x :: l₁', y :: l₂' => (x, y) :: zip l₁' l₂'\n\ntheorem zip_nil_left {α β : Type} (l₁ : List α) : zip l₁ [] = ([] : List (α × β)) := by\n  cases l₁ with\n  | nil => rfl\n  | cons h t => rfl\n\ntheorem zip_nil_right {α β : Type} (l₂ : List β) : zip [] l₂ = ([] : List (α × β)) := by\n  cases l₂ <;> rfl\n\ntheorem zip_cons_cons {α β : Type} {x : α} {y : β} {l₁ : List α} {l₂ : List β} :\n   zip (x :: l₁) (y :: l₂) = (x, y) :: zip l₁ l₂ := by rfl\n\n\nNotice that the simplification lemmas zip_nil_left and zip_nil_right are not proofs by rfl.\nThe reason is that l₁ and l₂ are variables, and matching on a variable usually gets stuck, like we have seen before in Induction when proving the zero_add theorem.\nTo overcome this, we destruct the list so that the match knows which branch to take during the computation done by the rfl tactic.\n\nTry answering the following questions on paper and\nchecking your answers in Lean:* What is the type of zip (i.e., what does #check @zip print?)* What does#eval zip [1, 2] [false, false, true, true]\nprint?\n\nWhen working with pairs, we often wish to prove them equal.\nWhen they compute to the same value, we can use rfl as usual:example : (1 + 2, 5) = (3, 2 + 3) := by\n  rfl\nHowever, we don't always have concrete values available.\nFor example, here both pairs are equal, but not definitionally:example {n : Nat} : (n + 1, 0) = (1 + n, 0) := by\n  rfl\nTactic `rfl` failed: The left-hand side\n  (n + 1, 0)\nis not definitionally equal to the right-hand side\n  (1 + n, 0)\n\nα β γ : Type\nx : α\ny : β\nn : Nat\n⊢ (n + 1, 0) = (1 + n, 0)\nOne way to prove this would be to rewrite by Nat.add_comm inside the pair:example {n : Nat} : (n + 1, 0) = (1 + n, 0) := by\n  rw [Nat.add_comm]\nBut this won't always work in general. Let's look at a more involved example.\nRemember surjective_pairing from Lists?\nIn Lean's standard library, this lemma is called Prod.eta,\nand we can use it to rewrite p into (p.fst, p.snd) like this:example {n : Nat} {p : Nat × Nat} (hx_fst : p.fst = n + 1) (hx_snd : p.snd = 0) :\n    (n + 1, 0) = p := by\n  rw [← Prod.eta p]\n  rw [hx_fst, hx_snd]\nHowever, Prod.eta is rarely used directly, since the theorem Prod.ext,\nthe extensionality principle for products, is often easier to work with.\nProd.ext splits the proof into two goals:\nfirst, to show that the fst elements are equal, and second, to show that the snd\nelements are equal.\nHere's an example:\n\n\n\nexample {n : Nat} {p : Nat × Nat} (hx_fst : p.fst = n + 1) (hx_snd : p.snd = 0) :\n    (n + 1, 0) = p := by\n  apply Prod.ext\n  · rw [hx_fst]\n  · rw [hx_snd]\n\n\nNow, use Prod.ext to prove the following.\nRemember that dsimp simplifies projections like (a, b).fst to a.example {m : Nat} {p : Nat × Nat} (hp_snd : p.snd = 4) (hp_fst : p.fst = m) :\n    ((p.fst + 1, 2), (p.fst, 4)) = ((m + 1, p.snd - 2), p) := by\n  solution!\n    apply Prod.ext\n    · dsimp\n      apply Prod.ext\n      · dsimp\n        rw [hp_fst]\n      · dsimp\n        rw [hp_snd]\n    · dsimp\n      apply Prod.ext\n      · rfl\n      · dsimp\n        rw [hp_snd]\n\n\nThe function unzip goes in the other direction from zip: it takes a list of pairs and returns a pair of lists.Fill in the definition of unzip below and write simplification rules that characterize it.\nMake sure it that passes the given unit test.\nProve unzip_test_fst and unzip_test_snd by rewriting with your simplification lemmas instead of using rfl directly.def unzip {α : Type} {β : Type} (l : List (α × β)) : List α × List β := solution!(\n  match l with\n  | [] => ([], [])\n  | (x, y) :: l' =>\n    let (l₁, l₂) := unzip l'\n    (x :: l₁, y :: l₂))\n-- This is a must have. One has to explicitly specify the types of the empty lists, which\n-- can be done in two equivalent ways\ntheorem unzip_nil {α β : Type} : unzip [] = (([], []) : List α × List β) := by\n  solution!\n      rfl\ntheorem unzip_nil' {α β : Type} : unzip ([] : List (α × β)) = ([], []) := by\n  solution!\n      rfl\n\n-- To characterize the cons branch, we can introduce a single `unzip_cons`...\ntheorem unzip_cons {α β : Type} {x : α} {y : β} {l : List (α × β)} :\n    (unzip ((x, y) :: l)) = (x :: (unzip l).fst, y :: (unzip l).snd) := by\n    solution!\n      rfl\n\n-- ... or introduce lemmas `unzip_cons_fst/snd` which individually give both sides of `unzip_cons`\ntheorem unzip_cons_fst {α β : Type} {x : α} {y : β} {l : List (α × β)} :\n    (unzip ((x, y) :: l)).fst = x :: (unzip l).fst := by\n    solution!\n      rfl\n\ntheorem unzip_cons_snd {α β : Type} {x : α} {y : β} {l : List (α × β)} :\n    (unzip ((x, y) :: l)).snd = y :: (unzip l).snd := by\n    solution!\n      rfl\ntheorem unzip_test1 : unzip [(1, false), (2, true)] = ([1, 2], [false, true]) := by\n  solution!\n    rfl\n\ntheorem unzip_test_fst : (unzip [(1, false), (2, true)]).fst = [1, 2] := by\n  solution!\n    rw [unzip_cons_fst, unzip_cons_fst, unzip_nil]\n\ntheorem unzip_test_snd : (unzip [(1, false), (2, true)]).snd = [false, true] := by\n  solution!\n    · rw [unzip_cons_snd, unzip_cons_snd, unzip_nil]\n\ntheorem unzip_test2 : unzip [(1, false), (2, true)] = ([1, 2], [false, true]) := by\n  solution!\n    exact Prod.ext unzip_test_fst unzip_test_snd\n-- These are the same tests but with `unzip_cons` instead\ntheorem unzip_test_fst' : (unzip [(1, false), (2, true)]).fst = [1, 2] := by\n  rw [unzip_cons]\n  dsimp\n  rw [unzip_cons]\n  dsimp\n  rw [unzip_nil]\n\ntheorem unzip_test_snd' : (unzip [(1, false), (2, true)]).snd = [false, true] := by\n  rw [unzip_cons]\n  dsimp\n  rw [unzip_cons]\n  dsimp\n  rw [unzip_nil]\n\ntheorem unzip_test2' : unzip [(1, false), (2, true)] = ([1, 2], [false, true]) := by\n  exact Prod.ext unzip_test_fst unzip_test_snd\n\n\n","context":"Logical Foundations\u0009Poly: Polymorphism and Higher-Order Functions\u0009Polymorphism","header":"6.1.2. Polymorphic Pairs","id":"/Poly/#Logical-Foundations--Poly___-Polymorphism-and-Higher-Order-Functions--Polymorphism--Polymorphic-Pairs"}});