window.docContents[242].resolve({"/Induction/#Logical-Foundations--Induction___-Proof-by-Induction--Proofs-Within-Proofs":{"contents":"In Lean, as in informal mathematics, large proofs are often\nbroken into a sequence of theorems, with later proofs referring to\nearlier theorems.  But sometimes a proof will involve some\nmiscellaneous fact that is too trivial and of too little general\ninterest to bother giving it its own top-level name.  In such\ncases, it is convenient to be able to simply state and prove the\nrequired fact \"in place\".  The have tactic allows us to do this.\n\n\n\ntheorem mult_zero_add' (n m : Nat) :\n    ((zero + n) + zero) * m = n * m := by\n  have h : (zero + n) + zero = n := by\n    rw [zero_add, add_zero]\n  rw [h]\n\n\nThe have tactic introduces a local lemma into the proof.\nWe prove it immediately, and it's available as a hypothesis\nfor the rest of the proof.\n\n\n\nAs another example, suppose we want to prove that\n(n + m) + (p + q) = (m + n) + (p + q). The only difference between\nthe two sides of the = is that the arguments m and n to the\nfirst inner + are swapped, so it seems we should be able to use\nthe commutativity of addition (add_comm) to rewrite one into the\nother.  However, the rw tactic is not very smart about where\nit applies the rewrite.  There are three uses of + here, and\nrw [add_comm] may affect the wrong one...\n\nexample (n m p q : Nat) :\n   (n + m) + (p + q) = (m + n) + (p + q) := by\n  /-\n    We just need to swap (n + m) for (m + n)... seems\n    like add_comm should do the trick!\n    But `rw [add_comm]` might rewrite the wrong `+`!\n  -/\n  rw [add_comm]\n\n\nunsolved goals\nn m p q : Nat\n⊢ p + q + (n + m) = m + n + (p + q)\n\n\n\n\nTo use add_comm at the point where we need it, we can supply\nexplicit arguments: rw [add_comm n m] tells Lean exactly which\n+ to rewrite.  (We can also use have to establish the specific\nequation we want, then rewrite with it.)\n\ntheorem add_rearrange (n m p q : Nat) :\n    (n + m) + (p + q) = (m + n) + (p + q) := by\n  rw [add_comm n m]\n\n\n","context":"Logical Foundations\u0009Induction: Proof by Induction","header":"3.4. Proofs Within Proofs","id":"/Induction/#Logical-Foundations--Induction___-Proof-by-Induction--Proofs-Within-Proofs"},"/Logic/#Logical-Foundations--Logic-in-Lean--Working-with-Decidable-Properties":{"contents":"We've seen two different ways of expressing logical claims in Lean:\nwith booleans (of type Bool), and with propositions (of type Prop).\nHere are the key differences between Bool and Prop:\n\n|                     | `Bool` | `Prop` |\n| ------------------- | ------ | ------ |\n| decidable?          | yes    | no     |\n| useable with match? | yes    | no     |\n\n\nThe crucial difference between the two worlds is decidability.\nEvery (closed) expression of type Bool can be simplified in a finite\nnumber of steps to either true or false — i.e., there is a terminating\nmechanical procedure for deciding whether or not it is true.This means that, for example, the type Nat → Bool is inhabited only by\nfunctions that, given a Nat, always yield either true or false in\nfinite time; this, in turn, means (by a standard computability argument)\nthat there is no function in Nat → Bool that checks whether a given\nnumber is the code of a terminating Turing machine.By contrast, the type Prop includes both decidable and undecidable\nmathematical propositions; in particular, the type Nat → Prop\ndoes contain functions representing properties like\n\"the nth Turing machine halts.\"The second table row follows directly from this essential difference.\nTo evaluate a pattern match (or conditional) on a boolean, we need to know\nwhether the scrutinee evaluates to true or false; this only works for\nBool, not Prop.\n\n\n\nSince Prop includes both decidable and undecidable properties,\nwe have two options when we want to formalize a property that happens\nto be decidable: we can express it either as a boolean computation,\nor as a function into Prop.\n\nFor instance, to claim that a number n is even,\nwe can say either that Nat.even n evaluates to true...\n\nexample : Nat.even 42 = true := rfl\n\n\n... or that there exists some k such that n = double k.\n\nexample : Nat.Even 42 := by rw [Nat.Even]; exists 21\n\n\nOf course, it would be deeply strange if these two characterizations\nof evenness did not describe the same set of natural numbers!\nFortunately, they do!\n\nTo prove this, we first need two helper lemmas.\n\ntheorem even_double (k : Nat) :\n    Nat.even (Nat.double k) = true := by\n  induction k with\n  | zero => rw [Nat.double_zero]; rfl\n  | succ k' ih => rw [Nat.double_succ]; exact ih\n\n\nSame issue as CombineOddEven.\n\ntheorem even_double_conv (n : Nat) : ∃ k : Nat,\n    n = bif Nat.even n then Nat.double k else Nat.double k + 1 := by\n  solution!\n    induction n with\n    | zero =>\n      rw [Nat.even_zero]\n      exists 0  -- (`0 = Nat.double 0` is closed by `exists`'s final `rfl`)\n    | succ n' ihn =>\n      obtain ⟨k', ihk⟩ := ihn\n      rw [Nat.even_succ]\n      cases h : Nat.even n' with\n      | false =>\n        rw [h] at ihk; rw [not] at *; rw [cond_false] at ihk\n        exists (k' + 1); rw [ihk, cond_true, Nat.double_succ]\n      | true =>\n        rw [h] at ihk; rw [not] at *; rw [cond_true] at ihk\n        exists k'; congr\n\n\nNow the main theorem:\n\ntheorem Nat.even_bool_prop (n : Nat) : Nat.even n = true ↔ Even n := by\n  constructor\n  · intro h\n    obtain ⟨k, hk⟩ := even_double_conv n\n    rw [h] at hk; rw [cond_true] at hk; rw [Even]; exists k\n  · intro ⟨k, hk⟩; rw [hk]; apply even_double\n\n\nIn view of this theorem, we can say that the boolean computation Nat.even n\nis reflected in the truth of the proposition ∃ k, n = Nat.double k.\n\n\n\nSimilarly, to state that two numbers n and m are equal,\nwe can say either\n\n1. that n == m returns true, or2. that n = m.\n\nAgain, these two notions are equivalent:\n\n(For the reverse direction we need the simple fact that == is\nreflexive.)\n\nEither get rid of the development of beq story\nor use our own beq on Nat.\n\nDon't worry too much about Nat.beq_eq_true_eq yet,\nwe need this from Lean because n == m is a wrapper of DecidableEq Nat.\nWe will go over this in the Typeclasses chapter.\n\ntheorem beq_eq_true (n m : Nat) :\n    (n == m) = true ↔ n = m := by\n  rw [Nat.beq_eq_true_eq]\n\n\nSo what should we do in situations where some claim could be formalized\nas either a proposition or a boolean computation?\nWhich should we choose?\n\nIn general, both can be useful. For example, booleans are more useful\nfor defining functions, since we can test whether they are true using\nconditional expressions.\n\ndef is_even_prime (n : Nat) : Bool :=\n  bif n == 2 then true else false\n\n\nBeyond the fact that non-computable properties are possible\nin general to phrase as boolean computations, even many computable\nproperties are easier to express using Prop than Bool, since\nrecursive function definitions are subject to significant restrictions.\nFor instance, the next chapter shows how to define the property that\na regular expression matches a given string using Prop.\nDoing the same with Bool would amount to writing a regular expression\nmatching algorithm, which would be more complicated, harder to understand,\nand harder to reason about than a simple (non-algorithmic) definition\nof this property.Conversely, an important side benefit of stating facts using booleans\nis enabling some proof automation through computation with terms, a\ntechnique known as proof by reflection.Consider the following statement:\n\nThe most direct way to prove this is to give the value of k explicitly.\n\nexample : Nat.Even 100 := by\n  exists 50\n\n\nThe proof of the corresponding boolean statement is simpler,\nbecause we don't have to invent the witness 50:\ncomputation does it for us!\n\nexample : Nat.even 100 = true := rfl\n\n\nNow, the useful observation is that, since the two notions are equivalent,\nwe can use the boolean formulation to prove the other one\nwithout mentioning the value 50 explicitly:\n\nexample : Nat.Even 100 := by\n  obtain ⟨h, _⟩ := Nat.even_bool_prop 100\n  apply h; rfl\n\n\nAlthough we haven't gained much in terms of proof-script simplicity\nin this case, larger proofs can often be made considerably simpler\nby the use of reflection.\n\nAs an extreme example, a famous mechanized proof of the even more famous\nfour colour theorem uses reflection to reduce the analysis of hundreds\nof different cases to a boolean computation.\n\nAnother advantage of booleans is that the negation of a claim about\nbooleans is straightforward to state and (when true) to prove:\nsimply flip the expected boolean result.\n\nexample : Nat.even 101 = false := rfl\n\n\nIn contrast, propositional negation can be difficult to work with directly.\nFor example, suppose we state the nonevenness of 101 propositionally:\n\nProving this directly — by assuming that there is some n such that\n101 = Nat.double n and then somehow reasoning to a contradiction —\nwould be rather complicated.\n\nBut if we convert it to a claim about the boolean Nat.even function,\nwe can let Lean do the work for us.\n\nexample : ¬ Nat.Even 101 := by\n  workinclass!\n    intro h; apply (Nat.even_bool_prop 101).mpr at h\n    rw [Nat.even] at h; contradiction\n\n\nConversely, there are situations where it can be easier to work with\npropositions rather than booleans. In particular, knowing that\n(n == m) = true is generally of little direct help in the middle of\na proof involving n and m. But if we convert the statement to\nthe equivalent form n = m, then we can easily rewrite with it.\n\ntheorem add_beq_true (n m p : Nat) (h : (n == m) = true) :\n    (n + p == m + p) = true := by\n  workinclass!\n    apply (beq_eq_true n m).mp at h\n    rw [h, BEq.rfl]\n\n\nWe'll come back to\nreflection and decidable propositions in a later chapter,\nbut it serves as a good example showing the different strengths\nof booleans and general propositions.\nBeing able to cross back and forth between the boolean and propositional\nworlds will often be convenient in later chapters.\n\nThe following theorems relate the propositional connectives studied\nin this chapter to the corresponding boolean operations.theorem andb_true_iff (b1 b2 : Bool) :\n    (b1 && b2) = true ↔ b1 = true ∧ b2 = true := by\n  solution!\n    constructor\n    · intro h\n      cases b1 with\n      | false => rw [and] at h; contradiction\n      | true => rw [and] at h; exact ⟨rfl, h⟩\n    · intro h\n      cases b1 with\n      | false => exfalso; cases h.left\n      | true => rw [and]; exact h.right\n\ntheorem orb_true_iff (b1 b2 : Bool) :\n    (b1 || b2) = true ↔ b1 = true ∨ b2 = true := by\n  solution!\n    constructor\n    · intro h\n      cases b1 with\n      | false => rw [or] at h; right; exact h\n      | true => rw [or] at h; left; rfl\n    · intro h\n      cases b1 with\n      | false =>\n        obtain h | h := h\n        · contradiction\n        · rw [or]; exact h\n      | true => rw [or]\nGiven a boolean operator beq for testing equality of elements\nof some type α, we can define a function beqList for testing\nequality of lists with elements in α. Complete the definition\nof the beqList function below. To make sure that your definition\nis correct, prove the lemma beqList_true_iff.def beqList {α : Type} (beq : α → α → Bool) (xs ys : List α) : Bool := solution!(\n  match xs, ys with\n  | [], [] => true\n  | x :: xs, y :: ys => beq x y && beqList beq xs ys\n  | _, _ => false)\n\ntheorem beqList_nil_nil {α : Type} {beq : α → α → Bool} :\n    beqList beq [] [] = true := solution!(rfl)\n\ntheorem beqList_cons_cons {α : Type} {beq : α → α → Bool}\n    {x y : α} {xs ys : List α} :\n    beqList beq (x :: xs) (y :: ys) = (beq x y && beqList beq xs ys) :=\n  solution!(rfl)\n\ntheorem beqList_nil_cons {α : Type} {beq : α → α → Bool}\n    {x : α} {xs : List α} : beqList beq [] (x :: xs) = false := solution!(rfl)\n\ntheorem beqList_cons_nil {α : Type} {beq : α → α → Bool}\n    {x : α} {xs : List α} : beqList beq (x :: xs) [] = false := solution!(rfl)\n\ntheorem beqList_true_iff α (beq : α → α → Bool)\n    (h : ∀ (x y : α), beq x y = true ↔ x = y) :\n    ∀ {xs ys : List α}, beqList beq xs ys = true ↔ xs = ys := by\n  solution!\n    intro xs;\n    induction xs with\n    | nil =>\n      intro ys\n      cases ys with\n      | nil =>\n        rw [beqList_nil_nil]\n        constructor\n        · intro; rfl\n        · intro; rfl\n      | cons y ys =>\n        rw [beqList_nil_cons]\n        constructor\n        · intro; contradiction\n        · intro; contradiction\n    | cons x xs ih =>\n      intro ys\n      cases ys with\n      | nil =>\n        rw [beqList_cons_nil]\n        constructor\n        · intro; contradiction\n        · intro; contradiction\n      | cons y ys =>\n        rw [beqList_cons_cons]\n        obtain ⟨h₁, h₂⟩ := andb_true_iff (beq x y) (beqList beq xs ys)\n        obtain ⟨hx, hy⟩ := h x y\n        obtain ⟨ih₁, ih₂⟩ := ih\n        constructor\n        · intro h\n          congr\n          · exact hx (h₁ h).left\n          · exact ih₁ (h₁ h).right\n        · intro h\n          injection h with hxy hxsys\n          apply h₂\n          exact ⟨hy hxy, ih₂ hxsys⟩\n\n\nProve the theorem below, which relates List.allb, from the exercise\nTactics.forall_exists_challenge, to the List.All property defined above.Copy the definition of List.allb from Tactics here so that this file can be\ngraded on its own.def List.allb {α : Type} (test : α → Bool) (l : List α) : Bool := solution!(\n  match l with\n  | [] => true\n  | x :: xs' => test x && allb test xs')\n\ntheorem List.allb_nil {α : Type} {test : α → Bool} : allb test [] = true := solution!(rfl)\n\ntheorem List.allb_cons {α : Type} {test : α → Bool} {x : α} {l : List α} :\n    allb test (x :: l) = (test x && allb test l) := solution!(rfl)\n\ntheorem List.allb_true_iff α {test : α → Bool} {l : List α} :\n    allb test l = true ↔ All (fun x => test x = true) l := by\n  solution!\n    induction l with\n    | nil =>\n      rw [allb_nil]\n      constructor\n      · intro _\n        apply All_nil\n      · intro _\n        rfl\n    | cons x xs' ih =>\n      obtain ⟨h₁, h₂⟩ := andb_true_iff (test x) (allb test xs')\n      obtain ⟨ih₁, ih₂⟩ := ih\n      rw [allb_cons, All_cons]\n      constructor\n      · intro h; exact ⟨(h₁ h).left, ih₁ (h₁ h).right⟩\n      · intro ⟨h₁', h₂'⟩; exact h₂ ⟨h₁', ih₂ h₂'⟩\n(Ungraded thought question) Are there any important properties of\nthe function List.allb which are not captured by this specification?This theorem exactly captures the input-output behavior of List.allb.\nHowever, it does not say anything about the running time.\n\n","context":"Logical Foundations\u0009Logic in Lean","header":"8.7. Working with Decidable Properties","id":"/Logic/#Logical-Foundations--Logic-in-Lean--Working-with-Decidable-Properties"},"/Postscript/#Logical-Foundations--Postscript--Looking-Forward":{"contents":"The next volumes carry these ideas into programming-language theory\nand program verification:\n\n* Type Systems develops operational semantics and type systems,\n  including the simply typed lambda calculus and the progress and\n  preservation theorems.* Hoare Logic introduces imperative programs and Hoare logic,\n  a framework for stating and proving correctness properties of programs\n  with mutable state.\n\nBoth volumes build directly on the definitions and proof techniques\nintroduced here.\n\n","context":"Logical Foundations\u0009Postscript","header":"12.2. Looking Forward","id":"/Postscript/#Logical-Foundations--Postscript--Looking-Forward"}});