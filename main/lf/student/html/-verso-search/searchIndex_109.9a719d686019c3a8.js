window.docContents[109].resolve({"/Lists/#Logical-Foundations--Lists___-Working-with-Structured-Data--Partial-Maps":{"contents":"As a final illustration of how data structures can be defined in\nLean, here is a simple partial map data type, analogous to the\nmap or dictionary data structures found in most programming\nlanguages.\n\nFirst, we define a new type MyId to serve as the \"keys\" of our\npartial maps.\n\nstructure MyId where\n  val : Nat\n\n\nInternally, a MyId is just a number.  Introducing a separate type\nby wrapping each Nat makes definitions more readable and gives us\nflexibility to change representations later if we want to.\n\n\n\nWe'll also need an equality test for MyIds:\n\ndef MyId.beq (x₁ x₂ : MyId) : Bool :=\n  x₁.val == x₂.val\n\n\ntheorem MyId.beq_refl (x : MyId) : MyId.beq x x = true := by\n  sorry\n\n\n\n\nNow we define the type of partial maps:\n\ninductive PartialMap : Type where\n  | empty : PartialMap\n  | record (i : MyId) (n : Nat) (m : PartialMap) : PartialMap\n\n\nThis declaration can be read: \"There are two ways to construct a\nPartialMap: either using the constructor empty to represent an\nempty partial map, or applying the constructor record to\na key, a value, and an existing PartialMap to construct a\nPartialMap with an additional key-to-value mapping.\"\n\nnamespace PartialMap\n\n\n\n\nThe update function overrides the entry for a given key in a\npartial map by shadowing it with a new one (or simply adds a new\nentry if the given key is not already present).\n\ndef update (d : PartialMap) (x : MyId) (value : Nat) : PartialMap :=\n  record x value d\n\n\nLast, the find function searches a PartialMap for a given\nkey.  It returns none if the key was not found and some val if\nthe key was associated with val. If the same key is mapped to\nmultiple values, find will return the first one it encounters.\n\n\n\n\n\ndef find (x : MyId) (d : PartialMap) : NatOption :=\n  match d with\n  | empty => .none\n  | record y n d' =>\n    bif MyId.beq x y then .some n\n    else find x d'\n\n\nIs the following claim true or false?∀ (d : PartialMap) (x : MyId) (n : Nat),\n-----------------------------------------\n  find x (update d x n) = .some n\n(A) True\n(B) False\n(C) Not sure\n\nIs the following claim true or false?∀ (d : PartialMap) (x y : MyId) (o : Nat)\n  (h : MyId.beq x y = false),\n-----------------------------------------\n  find x (update d y o) = find x d\n(A) True\n(B) False\n(C) Not sure\n\ntheorem update_eq (d : PartialMap) (x : MyId) (n : Nat) :\n    find x (update d x n) = .some n := by\n  sorry\ntheorem update_neq (d : PartialMap) (x y : MyId) (o : Nat) :\n    MyId.beq x y = false → find x (update d y o) = find x d := by\n  sorry\n\n\nend PartialMap\n\n\n\n\nend Lists\n\n\n","context":"Logical Foundations\u0009Lists: Working with Structured Data","header":"5.5. Partial Maps","id":"/Lists/#Logical-Foundations--Lists___-Working-with-Structured-Data--Partial-Maps"}});