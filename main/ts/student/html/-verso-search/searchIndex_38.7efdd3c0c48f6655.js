window.docContents[38].resolve({"/StlcProp/#Type-Systems--StlcProp___-Properties-of-STLC--Preservation--The-Substitution-Lemma":{"contents":"Now we come to the conceptual heart of the proof that reduction\npreserves types — namely, the observation that substitution\npreserves types.\n\nFormally, the so-called substitution lemma says this:\nSuppose we have a term t with a free variable x, and suppose\nwe've assigned a type τ to t under the assumption that x has\nsome type τ'.  Also, suppose that we have some other term v and\nthat we've shown that v has type τ'.  Then, since v satisfies\nthe assumption we made about x when typing t, we can\nsubstitute v for each of the occurrences of x in t and\nobtain a new term that still has type τ.\n\n\n\n\n\ntheorem substitution_preserves_typing (Γ : Context) (x : String) (τ' : Ty)\n    (t v : Tm) (τ : Ty)\n    (hτ : <{ ~x ↦ ~τ' ; ~Γ ⊢ ~t ⦂ ~τ }>) (hv : <{ ∅ ⊢ ~v ⦂ ~τ' }>) :\n    <{ ~Γ ⊢ [~x := ~v] ~t ⦂ ~τ }> := by\n  -- By induction on `t`; in each case we get at the derivation of `hτ`.\n  induction t generalizing Γ τ with\n  | var y =>\n    cases hτ with\n    | var _ _ _ h =>\n      by_cases hxy : x = y\n      · subst hxy\n        rw [PartialMap.update_eq] at h\n        rw [subst_var_eq]\n        have hτ'τ : τ' = τ := by\n          apply Option.some.inj\n          exact h\n        subst hτ'τ\n        apply weakening_empty\n        exact hv\n      · rw [PartialMap.update_neq hxy] at h\n        rw [subst_var_ne _ _ _ hxy]\n        constructor\n        exact h\n  | app t₁ t₂ ih₁ ih₂ =>\n    cases hτ with\n    | app _ _ _ _ _ h₁ h₂ =>\n      rw [subst_app]\n      constructor\n      · apply ih₁\n        exact h₁\n      · apply ih₂\n        exact h₂\n  | abs y σ t₁ ih =>\n    cases hτ with\n    | abs _ _ _ _ _ h =>\n      by_cases hxy : x = y\n      · subst hxy\n        rw [subst_abs_eq]\n        rw [PartialMap.update_shadow] at h\n        constructor\n        exact h\n      · rw [subst_abs_ne _ _ _ _ _ hxy]\n        rw [PartialMap.update_permute (Ne.symm hxy)] at h\n        constructor\n        apply ih\n        exact h\n  | tru =>\n    cases hτ with\n    | tru =>\n      rw [subst_tru]\n      constructor\n  | fls =>\n    cases hτ with\n    | fls =>\n      rw [subst_fls]\n      constructor\n  | ite c t e ihc iht ihe =>\n    cases hτ with\n    | ite _ _ _ _ _ h₁ h₂ h₃ =>\n      rw [subst_ite]\n      constructor\n      · apply ihc\n        exact h₁\n      · apply iht\n        exact h₂\n      · apply ihe\n        exact h₃\n\n\nThe substitution lemma can be viewed as a kind of \"commutation\nproperty.\"  Intuitively, it says that substitution and typing can\nbe done in either order: we can either assign types to the terms\nt and v separately (under suitable contexts) and then combine\nthem using substitution, or we can substitute first and then\nassign a type to [x:=v] t; the result is the same either\nway.Proof: We show, by induction on t, that for all τ and\nΓ, if x ↦ τ' ; Γ ⊢ t ⦂ τ and ∅ ⊢ v ⦂ τ', then\nΓ ⊢ [x:=v]t ⦂ τ.* If t is a variable there are two cases to consider,\n    depending on whether t is x or some other variable.* If t = x, then from the fact that x ↦ τ' ; Γ ⊢ x ⦂ τ we conclude that τ' = τ.  We must show that [x:=v]x = v has type τ under Γ, given the assumption that\n        v has type τ' = τ under the empty context.  This\n        follows from the weakening lemma.* If t is some variable y that is not equal to x, then\n        we need only note that y has the same type under x ↦ τ' ; Γ as under Γ.* If t is an abstraction λy:σ. t₀, then τ = σ → τ₁ and\n    the IH tells us, for all Γ' and τ₀, that if x ↦ τ' ; Γ' ⊢ t₀ ⦂ τ₀, then Γ' ⊢ [x:=v]t₀ ⦂ τ₀.\n    Moreover, by inspecting the typing rules we see it must be\n    the case that y ↦ σ ; x ↦ τ' ; Γ ⊢ t₀ ⦂ τ₁.The substitution in the conclusion behaves differently\n    depending on whether x and y are the same variable.First, suppose x = y.  Then, by the definition of\n    substitution, [x:=v]t = t, so we just need to show Γ ⊢ t ⦂ τ.  Using HasType.abs, we need to show that y ↦ σ ; Γ ⊢ t₀ ⦂ τ₁. But we know y ↦ σ ; x ↦ τ' ; Γ ⊢ t₀ ⦂ τ₁,\n    and the claim follows since x = y.Second, suppose x ≠ y. Again, using HasType.abs,\n    we need to show that y ↦ σ ; Γ ⊢ [x:=v]t₀ ⦂ τ₁.\n    Since x ≠ y, we have\n    y ↦ σ ; x ↦ τ' ; Γ = x ↦ τ' ; y ↦ σ ; Γ. So\n    we have x ↦ τ' ; y ↦ σ ; Γ ⊢ t₀ ⦂ τ₁. Then, the\n    IH applies (taking Γ' = y ↦ σ ; Γ), giving us\n    y ↦ σ ; Γ ⊢ [x:=v]t₀ ⦂ τ₁, as required.* If t is an application t₁ t₂, the result follows\n    straightforwardly from the definition of substitution and the\n    induction hypotheses.* The remaining cases are similar to the application case.\n\nOne technical subtlety in the statement of the above lemma is that\nwe assume v has type τ' in the empty context — in other\nwords, we assume v is closed.  (Since we are using a simple\ndefinition of substitution that is not capture-avoiding, it doesn't\nmake sense to substitute non-closed terms into other terms.\nFortunately, closed terms are all we need!)\n\nShow that substitutionpreservestyping can also be\nproved by induction on typing derivations instead\nof induction on terms.theorem substitution_preserves_typing_from_typing_ind (Γ : Context) (x : String) (τ' : Ty)\n    (t v : Tm) (τ : Ty)\n    (hτ : <{ ~x ↦ ~τ' ; ~Γ ⊢ ~t ⦂ ~τ }>) (hv : <{ ∅ ⊢ ~v ⦂ ~τ' }>) :\n    <{ ~Γ ⊢ [~x := ~v] ~t ⦂ ~τ }> := by\n  sorry\n\n\n","context":"Type Systems\u0009StlcProp: Properties of STLC\u0009Preservation","header":"6.3.2. The Substitution Lemma","id":"/StlcProp/#Type-Systems--StlcProp___-Properties-of-STLC--Preservation--The-Substitution-Lemma"}});