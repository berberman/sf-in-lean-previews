window.docContents[23].resolve({"/StlcProp/#Type-Systems--StlcProp___-Properties-of-STLC--Additional-Exercises":{"contents":"\n\n(Officially optional, but strongly recommended!) Without peeking\nat their statements above, write down the progress and\npreservation theorems for the simply typed lambda-calculus (as Lean\ntheorems).  You can write sorry for the proofs.At least one person was confused by what to name these. We\ncould simplify life by giving the names explicitly and just\nomitting the bodies.  Indeed, once we do that we could autograde\nthis by demanding that what they write be identical to what we\nwrote above! Maybe a better way to solve this would be to have the\nfollowing template.  BCP 21: Yes, do this!!theorem progress_statement :\n    FILL IN HERE := by\n  apply progress\n\ntheorem preservation_statement :\n    FILL IN HERE := by\n  apply preservation\nSee progress and preservation above.  Their statements are:theorem progress_statement (t : Tm) (τ : Ty) (hτ : <{ ∅ ⊢ ~t ⦂ ~τ }>) :\n    t.IsValue ∨ ∃ t', t ⟶ t' := by\n  sorry\n\ntheorem preservation_statement (t t' : Tm) (τ : Ty)\n    (hτ : <{ ∅ ⊢ ~t ⦂ ~τ }>) (hs : t ⟶ t') : <{ ∅ ⊢ ~t' ⦂ ~τ }> := by\n  sorry\nSuppose we add a new term zap with the following reduction rule                         ---------                  (zap)\n                         t ⟶ zap\nand the following typing rule:                        -----------                 (zap)\n                        Γ ⊢ zap ⦂ τ\nWhich of the following properties of the STLC remain true in\nthe presence of these rules?  For each property, write either\n\"remains true\" or \"becomes false.\" If a property becomes\nfalse, give a counterexample.* Determinism of StepBecomes false. For instance (if true then false else true) ⟶ false\nand (if true then false else true) ⟶ zap.* ProgressRemains true. Every term (including zap) can take a step to zap.* PreservationRemains true. zap can have any type.Suppose instead that we add a new term foo with the following\nreduction rules:                       -----------------                (foo1)\n                        (λx:τ. x) ⟶ foo\n\n                         ------------                   (foo2)\n                          foo ⟶ true\nWhich of the following properties of the STLC remain true in\nthe presence of this rule?  For each one, write either\n\"remains true\" or else \"becomes false.\" If a property becomes\nfalse, give a counterexample.* Determinism of StepBecomes false. The term (λx:Bool. x) true might step\nto either true by the rule Step.appAbs or\nto foo true by the rules Step.app1 and Step.foo1.* ProgressRemains true. We are only adding to the step relation, and\nthis can never damage progress.* PreservationBecomes false. For example,\n∅ ⊢ λx:Bool. x ⦂ Bool → Bool and (λx:Bool. x) ⟶ foo by Step.foo1,\nbut, since we have no typing rules for foo, we cannot prove that\n∅ ⊢ foo ⦂ Bool → Bool.Suppose instead that we remove the rule Step.app1 from the Step\nrelation. Which of the following properties of the STLC remain\ntrue in the presence of this rule?  For each one, write either\n\"remains true\" or else \"becomes false.\" If a property becomes\nfalse, give a counterexample.* Determinism of StepRemains true. Removing reduction rules can only make Step\nmore deterministic.* ProgressBecomes false. For example,\n((λx:Bool → Bool. λy:Bool → Bool. x) (λz:Bool. z)) (λz:Bool. z)\nis well typed, but stuck.* PreservationRemains true. Removing reduction rules can't break preservation.Suppose instead that we add the following new rule to the\nreduction relation:            ----------------------------------        (funnyIfTrue)\n             (if true then t₁ else t₂) ⟶ true\nWhich of the following properties of the STLC remain true in\nthe presence of this rule?  For each one, write either\n\"remains true\" or else \"becomes false.\" If a property becomes\nfalse, give a counterexample.* Determinism of StepBecomes false, for instance:\n(if true then false else false) ⟶ false and\n(if true then false else false) ⟶ true* ProgressRemains true. We are only adding to the step relation, and\nthis can never damage progress.* PreservationBecomes false. For example,\n∅ ⊢ if true then (λx:Bool. x) else (λx:Bool. x) ⦂ Bool → Bool\nand (if true then (λx:Bool. x) else (λx:Bool. x)) ⟶ true\nbut it's not the case that ∅ ⊢ true ⦂ Bool → Bool.Suppose instead that we add the following new rule to the typing\nrelation:                 Γ ⊢ t₁ ⦂ Bool → Bool → Bool\n                        Γ ⊢ t₂ ⦂ Bool\n                ------------------------------       (funnyApp)\n                       Γ ⊢ t₁ t₂ ⦂ Bool\nWhich of the following properties of the STLC remain true in\nthe presence of this rule?  For each one, write either\n\"remains true\" or else \"becomes false.\" If a property becomes\nfalse, give a counterexample.* Determinism of StepRemains true. We are only adding to the typing relation, and\nthis can never damage determinism of Step.* ProgressRemains true. Since the new rule still requires that t₁ is\na function we can still apply Step.appAbs to show progress.* PreservationBecomes false. For example,\n∅ ⊢ (λx:Bool. λy:Bool. x) true ⦂ Bool\nand (λx:Bool. λy:Bool. x) true ⟶ λy:Bool. true\nbut it's not the case that ∅ ⊢ λy:Bool. true ⦂ BoolSuppose instead that we add the following new rule to the typing\nrelation:                        Γ ⊢ t₁ ⦂ Bool\n                        Γ ⊢ t₂ ⦂ Bool\n                      ------------------            (funnyApp')\n                      Γ ⊢ t₁ t₂ ⦂ Bool\nWhich of the following properties of the STLC remain true in\nthe presence of this rule?  For each one, write either\n\"remains true\" or else \"becomes false.\" If a property becomes\nfalse, give a counterexample.* Determinism of StepRemains true. We are not changing the Step relation.* ProgressBecomes false. For instance, true true is a term that\nbecomes typable (at type Bool), but which is stuck.* PreservationRemains true. There are 3 ways t₁ t₂ can reduce. For\nStep.app1 and Step.app2 we can still apply the induction\nhypothesis. To reduce t₁ t₂ using Step.appAbs\nt₁ would need to be a function, but functions don't have\ntype Bool.Suppose we add the following new rule to the typing relation\nof the STLC:                       ------------------------     (funnyAbs)\n                        ∅ ⊢ λx:Bool. t ⦂ Bool\nWhich of the following properties of the STLC remain true in\nthe presence of this rule?  For each one, write either\n\"remains true\" or else \"becomes false.\" If a property becomes\nfalse, give a counterexample.* Determinism of StepRemains true. We're not changing the Step relation.* ProgressBecomes false. For instance if (λx:Bool. false) then false else false\nis a term that would become typable, although it is stuck.* PreservationRemains true. λx:Bool. t doesn't step.\n\nThe STLC typing relation with the funnyAbs rule of stlc_variation7 added,\nand a proof that progress then fails.namespace StlcVar1\n\ninductive HasType : Context → Tm → Ty → Prop where\n  | var (Γ : Context) (x : String) (τ₁ : Ty) (h : Γ[x] = some τ₁) :\n      HasType Γ (.var x) τ₁\n  | abs (Γ : Context) (x : String) (τ₁ τ₂ : Ty) (t₁ : Tm)\n      (h : HasType (x →ₚ τ₂ ; Γ) t₁ τ₁) :\n      HasType Γ <{ λ ~x : ~τ₂ . ~t₁ }> <{ ~τ₂ → ~τ₁ }>\n  | app (Γ : Context) (τ₁ τ₂ : Ty) (t₁ t₂ : Tm)\n      (h₁ : HasType Γ t₁ <{ ~τ₂ → ~τ₁ }>) (h₂ : HasType Γ t₂ τ₂) :\n      HasType Γ <{ ~t₁ ~t₂ }> τ₁\n  | tru (Γ : Context) : HasType Γ <{ true }> <{ Bool }>\n  | fls (Γ : Context) : HasType Γ <{ false }> <{ Bool }>\n  | ite (Γ : Context) (t₁ t₂ t₃ : Tm) (τ₁ : Ty)\n      (h₁ : HasType Γ t₁ <{ Bool }>) (h₂ : HasType Γ t₂ τ₁) (h₃ : HasType Γ t₃ τ₁) :\n      HasType Γ <{ if ~t₁ then ~t₂ else ~t₃ }> τ₁\n  | funnyAbs (x : String) (t₁ : Tm) :\n      HasType ∅ <{ λ ~x : Bool . ~t₁ }> <{ Bool }>\n\ntheorem no_progress :\n    ∃ t τ, HasType ∅ t τ ∧ ¬ t.IsValue ∧ ¬ ∃ t', t ⟶ t' := by\n  exists <{ if (λ x : Bool . false) then false else false }>, <{ Bool }>\n  constructor\n  · apply HasType.ite\n    · apply HasType.funnyAbs\n    · apply HasType.fls\n    · apply HasType.fls\n  constructor\n  · intro hv\n    cases hv\n  · intro ⟨t', hs⟩\n    cases hs with\n    | ifStep _ _ _ _ h => cases h\n\nend StlcVar1\n\n\nend Stlc\n\n\n\n\n","context":"Type Systems\u0009StlcProp: Properties of STLC","header":"6.7. Additional Exercises","id":"/StlcProp/#Type-Systems--StlcProp___-Properties-of-STLC--Additional-Exercises"}});